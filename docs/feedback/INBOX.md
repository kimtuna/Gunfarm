# INBOX — 작업 큐

형식: `- [ ] #<번호> [BUILD|DESIGN] <할 일 한 줄>` (완료하면 `- [x]`로 바꾸고 완료 날짜를 붙인다. 번호는 재사용하지 않는다.)

## 처리 대기

- [x] (2026-09-06) #1 [BUILD] 메인 메뉴 화면: 플레이 / 설정 / 나가기 세 버튼. "나가기"는 실제로 앱을 종료하고, "플레이"와 "설정"은 아직 빈 화면으로 이동만 한다(각각 #2, #3에서 채운다). 프로젝트 진입 씬을 이 메인 메뉴로 바꾼다.
- [x] (2026-09-06) #2 [BUILD] 캐릭터 슬롯 화면(코어키퍼 스타일 3개 슬롯). 빈 슬롯을 고르면 캐릭터 커스터마이징 화면(#4)으로, 캐릭터가 있는 슬롯을 고르면 월드 입장(#5)으로 넘어가는 자리만 만든다. 슬롯 데이터는 `user://` 저장/불러오기로 유지되고, 슬롯 삭제도 된다.
- [x] (2026-09-06) #3 [BUILD] 설정 화면의 해상도 옵션. **해상도를 바꿔도 카메라가 보여주는 월드 범위는 모든 해상도에서 동일해야 한다**(DESIGN.md "카메라 / 해상도" PvP 공정성 규칙). 서로 다른 해상도 2개 이상에서 실제로 같은 월드 범위가 보이는지 스크린샷으로 검증할 것.
- [x] (2026-09-06) #4 [BUILD] 캐릭터 커스터마이징 화면: 이름 입력 + 피부색/머리색/옷색(각각 고정 팔레트에서 선택) + 머리모양 3~4종 선택 + 확정. DESIGN.md "캐릭터 커스터마이징 항목" 참고. **이번 항목은 선택값을 슬롯에 저장하는 데이터/UI까지만** — 실제 캐릭터 그림은 아직 없으므로 미리보기는 색 견본(색 사각형 등)으로 대신하고, 그림이 생기면 별도 항목에서 붙인다.
- [x] (2026-09-06) #5 [BUILD] 월드 생성 1단계 + 입장: `opensimplex` 시드 기반 절차적 지형(땅/바다)과 스폰 지점까지만. 같은 시드는 항상 같은 월드를 만들어야 한다. 자원 배치(나무/채광 포인트/낚시 스팟)는 그 자원을 만드는 바퀴에 붙이므로 **이번 범위가 아니다**. 정한 맵 크기·바다 비율 수치는 DESIGN.md "월드 생성"에 기록할 것.
- [x] (2026-09-06) #6 [BUILD] Godot 이 스크립트마다 만드는 `*.gd.uid` 파일을 커밋할지 `.gitignore` 할지 정한다. 지금은 1바퀴에 만든 3개(`main_menu.gd.uid` 등, 그중 `menu_placeholder.gd.uid` 는 스크립트가 이미 지워진 고아)만 추적되고 나머지는 매 바퀴 untracked 로 쌓여서 `git status` 가 지저분하다. 둘 중 하나로 통일하고, 고아 uid 는 정리할 것.
- [x] (2026-09-06) #7 [BUILD] 그림([DESIGN]) 바퀴가 시작되기 전에, PNG 마다 생기는 `*.png.import` 파일을 커밋할지 정한다. `*.gd.uid` 는 #6 에서 커밋하는 쪽으로 정했고 `.png.import` 도 같은 성격(임포트 메타데이터)이지만, 아직 저장소에 PNG 가 하나도 없어서 #6 범위에서는 건드리지 않았다. 첫 스프라이트가 들어오는 순간 결정이 필요하다 — `game/qa/qa_uid_files.gd` 에 같은 방식의 검사를 붙일지도 같이 정할 것.
- [x] #8 [DESIGN] 플레이어 캐릭터 스프라이트 — **기준색 1벌만** (피부 밝은/머리 검정/옷 풀색), 4방향 × idle. `docs/STYLE_GUIDE.md` 를 먼저 전부 읽을 것. 출발점 구현이 `game/tools/gen_character.py` 에 있으나 **그대로 쓰지 말고 기준을 만족할 때까지 개선한다**. 합격선은 "누가 봐도 퀄리티가 좋다"이고, 미달이면 커밋하지 말고 미완료로 두고 STATUS.md 에 부족한 점을 남긴다. 후보를 여러 개 뽑아 나란히 놓고 비교할 것. PNG 저장 후 반드시 `--import` 재임포트.  (완료 2026-09-06)
- [x] (2026-09-06) #9 [DESIGN] **스프라이트 자동 검사 스크립트를 만들고, 그걸로 #8 의 대비 문제를 고친다.** 지금 [DESIGN] 바퀴는 결과를 매번 눈으로만 확인해서 턴이 100번 넘게 돌고 컨텍스트에 이미지가 쌓인다 — 기계로 판정할 수 있는 것부터 코드로 걸러내면 이미지를 볼 횟수 자체가 줄어든다.
  **(1) `game/qa/qa_sprite_check.py` 를 만든다** (`.venv/bin/python` 으로 실행, numpy/Pillow). PNG 경로를 받아 **짧은 텍스트 리포트**를 찍고 불합격이면 종료 코드 1 을 낸다(길게 쓰지 말 것 — 요점은 토큰 절약이다). 최소한 이걸 본다:
    - **팔레트 준수**: 램프에 없는 색이 섞였는가 (잉크색·투명 제외)
    - **재질 간 대비**: 맞닿은 재질끼리 평균 명도 차이가 기준 이상인가 — **#8 이 셔츠/바지/신발이 다 어두워 한 덩어리로 뭉친 걸 이 검사로 잡아야 한다**
    - **전체 명도 분포**: 너무 어둡거나 너무 납작하지 않은가
    - **실루엣 연결성**: 떨어져 나온 조각이 없는가 (연결 요소 개수)
    - **외곽선**: 존재하는가, 순검정이 아닌가
    - **규격**: 캔버스 크기, 방향 4개의 바운딩박스 크기가 서로 크게 다르지 않은가 (DESIGN.md 「캐릭터 애니메이션」: 생성 호출이 달라도 크기가 달라지면 안 된다)
  **(2) 그 검사로 #8 의 대비를 고친다** — 옷과 바지가 한눈에 구분되고 전체 밝기·채도가 코어키퍼 쪽으로 올라가야 한다. 검은 머리가 외곽선에 먹히지 않게 넣은 명도 바닥 처리가 다른 재질까지 누르고 있는지 의심해볼 것. 고친 idle 로 `docs/design_reference/player_idle_x6.png` 도 갱신한다.
  **(3) `docs/STYLE_GUIDE.md` 「작업 순서」에 이 절차를 박는다** — "먼저 검사 스크립트를 돌리고, **통과했을 때만** 이미지를 눈으로 본다". 앞으로 모든 [DESIGN] 바퀴가 이걸 따르게 하는 게 이 항목의 진짜 목적이다.
- [x] #10 [BUILD] 플레이어를 월드에 실제로 배치한다 — 지금은 `SpawnMarker`(빈 위치 표식)만 있고 플레이어 엔티티가 없어서 월드에 들어가도 캐릭터가 안 보인다. 스폰 지점에 `AnimatedSprite2D`(#8/#9 스프라이트) 를 붙인 플레이어 노드를 만들고, WASD 이동 + 카메라 추적 + 바다 타일 충돌을 붙인다. **DESIGN.md 「서버 권위 / 클라이언트 신뢰」를 처음부터 지킬 것** — 위치가 아니라 입력만 다루는 구조로 만들고, 이동 계산은 노드가 아닌 순수 클래스에 둔다(나중에 서버가 같은 코드를 돌린다).  (완료 2026-09-06)
- [x] (2026-09-06) #11 [BUILD] 커스터마이징 화면의 색 사각형 미리보기(`appearance_preview.gd`)를 #8 스프라이트로 교체하고, 피부/머리/옷 색 선택이 **팔레트 교체**로 반영되게 한다(형태를 다시 그리지 않는다 — STYLE_GUIDE 2번). 머리모양 4종도 실제 스프라이트로 보이게 할 것.  (완료 2026-09-06)
- [x] #12 [DESIGN] 지형 타일 스프라이트 — 땅/바다 2종. 아트 16px × 3배 = 화면 48px(STYLE_GUIDE 1번). 지금은 `terrain_view.gd` 가 색 사각형 + 격자선으로 그리고 있다. 인접한 같은 지형끼리 이어져 보여야 하고, 땅↔바다 경계가 자연스러워야 한다. 캐릭터(#8)와 나란히 놓고 팔레트·픽셀 크기가 어울리는지 확인할 것. (완료 2026-09-06)
- [x] #13 [DESIGN] (완료 2026-09-06) **`qa_sprite_check.py` 에 '자연스러움' 검사를 추가하고, 그 검사로 플레이어 idle 을 다시 만든다.** 사람 피드백: "예쁘지가 않다. 더 짜리몽땅하게. 상하의가 너무 단색이라서 그런 것 같기도 하다. **자연스럽게 부분을 피드백하게 QA 에 넣어달라.**"
  **(1) 검사 추가** — 지금 검사는 팔레트·대비 같은 '틀렸는가'만 본다. '어색한가'를 숫자로 잡는 항목을 더한다. 리포트는 짧게:
    - **단색 판정**: 재질마다 램프 4단계 중 몇 개를 실제로 썼는가, 한 단계가 그 영역의 **70% 를 넘는가** — 넘으면 불합격(상하의가 단색으로 보이는 원인)
    - **상의/하의 색상 차이**: 두 재질의 hue 차이가 충분한가 (명도 차이만으로는 불합격)
    - **실루엣 각짐**: 외곽선에 **5px 넘는 곧은 직선 구간**이 있는가
    - **비율**: 머리/몸통/다리 높이 비가 STYLE_GUIDE 표 범위 안인가 (짜리몽땅 판정), 머리 폭 > 어깨+팔 폭인가
    - **눈**: 흰 하이라이트 픽셀이 있는가, 눈 크기가 3×3 이상인가
  **(2) 그 검사를 통과하도록 idle 을 다시 만든다** — STYLE_GUIDE 의 새 비율표(머리 48% / 몸통 6px / 다리 5px)와 「자연스러움」 절을 전부 만족시킬 것. 옷 포인트(허리띠·소매 끝 등) 1~2개, 볼 홍조, 머리카락 하이라이트도 넣는다.
  **(3) 검사를 먼저 돌리고 통과한 뒤에 눈으로 본다.** walk 는 이번 범위가 아니다(#14) — 모양이 확정된 뒤에 프레임을 만든다.
  `docs/design_reference/player_idle_x6.png` 와 커마 미리보기(#11)도 새 스프라이트로 갱신할 것.
- [x] (완료 2026-09-06) #14 [BUILD] 월드 안의 플레이어에게 **슬롯에 저장된 외형을 입힌다.** #11 로 커스터마이징 화면은 고른 색·머리모양을 그대로 보여주는데, 정작 월드에 들어가면 기준색·짧은머리 고정이다(`player.gd` 가 `PlayerFrames.build()` 를 인자 없이 부른다). 재료는 다 있다 — `SlotStore` 에서 그 슬롯의 `appearance` 를 읽어 `character_sprite.gd` 로 시트를 칠하고, 머리모양에 맞는 시트를 고르면 된다. **슬롯을 거치지 않고 월드 씬을 직접 띄운 경우(자체 QA 가 그렇게 한다)에도 기본 외형으로 멀쩡히 떠야 한다.**
- [x] (2026-09-07) #15 [DESIGN] 플레이어 4방향 × walk(4~6프레임). **#13 에서 확정된 idle 모양을 그대로 쓴다.** 프레임을 순서대로 넘겼을 때 다리/팔이 매끄럽게 이어져야 하고, idle 과 자세가 뚝 끊기면 안 된다(DESIGN.md 「캐릭터 애니메이션」). **연속 프레임 사이의 변화량이 너무 크지 않은지 검사를 `qa_sprite_check.py` 에 추가**해서 끊겨 보이는 애니메이션을 숫자로 잡을 것. Godot 쪽 검증 스크립트는 `game/qa/` 에 남긴다.

- [x] (2026-09-07) #16 [BUILD] **캐릭터가 바라보는 방향을 이동 방향이 아니라 마우스 방향으로 바꾼다** (덕코프 기준). `DESIGN.md` 「조작」의 마우스 조준 항목이 이번에 구체화됐으니 그대로 따를 것. WASD 이동 자체는 그대로다 — 왼쪽으로 가면서 오른쪽을 겨누는 게 가능해야 한다(뒷걸음질/게걸음).
  **(1) 각도는 입력이다** — `DESIGN.md` 「서버 권위 / 클라이언트 신뢰」("입력만 보낸다")를 지켜서, 조준 각도를 이동 방향과 같은 입력 구조체에 담아 `player_motion.gd`(순수 클래스)로 넘긴다. **노드(`player.gd`)는 마우스 좌표 → 각도 변환까지만** 하고, 그 각도를 쓰는 계산(어느 4방향 시트를 그릴지 등)은 코어에 둔다. 근거: 화면 좌표는 클라이언트마다 다르지만 각도는 모두에게 같은 값이다.
  **(2) 스냅과 원본 각도를 둘 다 갖는다** — 시트는 4방향뿐이라 그리는 쪽은 가장 가까운 4방향으로 스냅하지만, **각도 자체는 각도로 보관한다**(나중의 총알 방향·시야 콘이 스냅된 값을 쓰면 안 된다). 스냅 경계에서 캐릭터가 덜덜 떨리지 않게 할 것(경계에 히스테리시스 등 — 방법은 자유, 실제로 마우스를 45도 근처에서 흔들어 확인한다).
  **(3) `qa_player_world.gd` / `qa_player_walk.gd` 의 "W를 누르면 up 애니메이션" 단정이 이번에 거짓이 된다** — 이동 키가 아니라 마우스가 방향을 정하므로 그 단정을 마우스 기준으로 고친다. 이동과 조준이 어긋난 경우(왼쪽으로 걸으며 오른쪽 조준)를 검사에 꼭 넣을 것.

- [x] (2026-09-07) #17 [BUILD] **Esc 를 일시정지 메뉴로 바꾸고, 화면 안의 "뒤로" 버튼을 전부 없앤다.** `DESIGN.md` 「클라이언트 화면 흐름」과 「조작」의 Esc 항목이 이번에 바뀌었다(2026-09-07 사람 결정 — 이전 "뒤로 버튼 + Esc" 결정을 뒤집은 것이다).
  **(1) 월드 안 Esc = 일시정지 메뉴** — 계속하기 / 설정 / 메인 메뉴로 나가기. 설정은 기존 설정 화면(`settings.tscn`)을 띄운다.
  **(2) 월드 시뮬레이션을 멈추지 않는다** — `get_tree().paused` 로 세계를 세우지 말 것. 멀티플레이에서는 한 사람이 Esc 를 눌렀다고 세계가 멈출 수 없다. 대신 **메뉴가 열린 동안 플레이어 입력만 0 으로 넣는다**(시간을 멈추는 게 아니라 입력을 끊는 것). `player_motion.gd` 는 이미 입력만 받으므로 코어는 안 고쳐도 된다.
  **(3) 뒤로 버튼 4개를 지운다** — `world.tscn` / `character_slots.tscn` / `character_customize.tscn` / `settings.tscn` 의 `BackButton` 과 짝이 되는 `_on_back_pressed`. 메뉴 화면들의 Esc 는 지금처럼 한 단계 위로 돌아가는 동작을 유지한다(버튼만 없어지고 길은 남는다).
  **(4) 기존 QA 가 뒤로 버튼을 눌러 화면 전환을 확인하고 있다** — `qa_main_menu` / `qa_character_slots` / `qa_settings` / `qa_character_customize` / `qa_world_entry` 를 Esc 기준으로 고치고, **월드에서 Esc 가 슬롯으로 나가지 않고 메뉴를 여는지**를 새로 검사한다.

- [x] (2026-09-07) #18 [BUILD] **M 키로 여는 전체 맵 + 내 위치 표시.** `DESIGN.md` 「맵 (M)」 절이 이번에 새로 생겼으니 그대로 따를 것(2026-09-07 사람 결정).
  **(1) 화면 위에 겹쳐 여는 전체 맵.** M 또는 Esc 로 닫는다 — **맵이 열려 있을 때 Esc 는 일시정지 메뉴가 아니라 맵 닫기다**(#17 과 순서가 겹치므로 가장 안쪽 창부터 닫히게 할 것).
  **(2) 내 위치를 조준 방향까지 표시한다**(화살표 등 — #16 의 각도를 그대로 쓴다). 점만 찍으면 맵에서 방향을 잃는다.
  **(3) 가본 곳만 보인다** — 플레이어 주변 일정 반경의 타일을 "방문함"으로 기록한다. **시야 콘이 아니라 반경이다**(콘으로 기록하면 마우스를 돌리는 것만으로 맵이 채워지고 부채꼴 자국이 남는다). 정한 반경 수치는 `DESIGN.md` 「맵 (M)」에 적을 것.
  **(4) 기록은 슬롯 저장 데이터에 넣는다**(캐릭터마다 따로, `SlotStore`). 타일당 1비트로 충분하니 타일마다 한 칸씩 쓰는 배열로 두지 말 것 — 「월드 생성」의 맵 크기로 실제 바이트 수를 계산해서 형식을 정하고 그 계산을 DESIGN.md 에 남긴다.
  **(5) 지형은 저장하지 말고 시드로 다시 계산한다** — 기록은 "어디를 봤는가"만 갖는다. 같은 정보를 두 벌 가지면 어긋난다.
  **(6) 다른 플레이어는 표시하지 않는다** — 지금은 자기 위치만이다(「맵 (M)」의 PvP 근거 참고). 나중에 팀원 표시를 붙일 자리만 남겨두되, 클라이언트가 아는 좌표를 그리는 구조로 만들지 말 것.
  검증은 `game/qa/` 에 남긴다: 안 가본 곳이 실제로 안 보이는지 / 돌아다니면 채워지는지 / **저장했다 불러와도 기록이 남는지** / 같은 시드에서 맵 지형이 월드와 일치하는지. 맵을 연 스크린샷을 눈으로 확인할 것.

- [x] (2026-09-07) #19 [DESIGN] **캐릭터를 17px 캔버스로 다시 만든다 — idle 4방향, 기준색 + 머리모양 4종까지만.** (walk 는 #20 이다. `DESIGN.md` 「그래픽 파이프라인」의 "한 항목에 방향 × 모션 × 색을 다 넣지 않는다" 규칙대로 쪼갰다.) 사람 피드백: *"눈이 너무 커서 줄여주고 다리가 너무 롱다리야. 캐릭터를 작게 만들어줘. 머리 크기도 줄여줘. … 지금의 반으로 줄여도 될것같아."* `docs/STYLE_GUIDE.md` 를 먼저 전부 읽을 것 — 1번(크기) / 3번(비율) / 4번(눈·옷 포인트·각짐)이 이번 피드백으로 **이미 고쳐져 있다.**
  **(1) 캔버스 34px → 17px, 씬 스케일은 3배 그대로**(`PlayerFrames.SCALE`). 화면 높이 102px → 51px. **스케일을 낮춰서 줄이지 말 것** — 이유는 `DESIGN.md` 「아이템/오브젝트 크기 표준」에 적혀 있다.
  **(2) 비율을 다시 잡는다** — 머리 47% → 40% 안팎, 다리는 더 짧게, 눈은 1×2~2×2. **머리는 줄이되 어깨보다는 계속 넓어야 한다**(어깨도 같이 좁힌다). 옛 값을 2로 나누지 말고 17px 격자 위에서 후보를 여러 개 그려 비교할 것.
  **(3) `qa_sprite_check.py` 의 SPECS 를 17px 기준으로 다시 잡는다** — 규격 / 비율 / **눈(이번에 상한이 생겼다 — 옛 검사는 하한만 봐서 큰 눈을 통과시킨다)** / 각짐 5px·6px(34px 에서 잰 값이라 17px 에서는 아무것도 안 걸린다) / 옷 포인트 개수. **검사를 먼저 고치고, 통과한 뒤에 눈으로 본다**(STYLE_GUIDE 「작업 순서」).
  **(4) 크기가 절반이 되면서 같이 따라와야 하는 것들**: 커스터마이징 미리보기 배율(`DESIGN.md` 「캐릭터 커스터마이징 항목」의 "6배 / 2배" — 17px 에서는 더 키워야 한다), **`player_motion.gd` 의 몸통 충돌 상자 32×18**(캐릭터 몸 폭이 절반이 되므로 같이 줄인다 — 고친 값을 `DESIGN.md` 「플레이어 이동」 표에 반영하고 `qa_player_world.gd` 를 다시 돌릴 것), `docs/design_reference/player_idle_x6.png` 갱신.
  **(5) 기존 walk 시트 4장은 이번 바퀴에 34px 인 채로 남는다** — idle 과 크기가 어긋난 상태가 되므로, 커밋 전에 `qa_player_walk` 가 그 어긋남으로 실패하는지 확인하고 **실패하면 그건 정상이다**(#20 이 고친다). STATUS.md 에 그렇게 남겨서 다음 바퀴가 당황하지 않게 할 것.

- [x] (2026-09-07) #20 [DESIGN] **17px 캐릭터의 walk 6프레임 4방향 × 머리모양 4종.** #19 에서 확정된 새 idle 모양을 그대로 쓴다(형태를 다시 정의하지 않고 `walk_pose()` 오프셋만 넣는 규칙 유지 — `DESIGN.md` 「캐릭터 애니메이션」). 34px 로 만들어져 있는 지금 walk 시트를 대체한다.
  **(1) `walk_pose()` 의 보폭·스윙 값이 34px 기준이다** — 17px 에서 그대로 쓰면 한 걸음이 몸 폭만큼 벌어진다. 「이어짐」/「고르게」 검사를 먼저 새 캔버스 기준으로 맞춘 뒤 값을 다시 잡을 것.
  **(2) 한 걸음 거리와 재생 속도를 다시 확인한다** — 이동 속도 240(타일 5칸/초)과 10fps × 6프레임은 그대로인데 캐릭터만 절반이 됐으니, 발이 땅에서 미끄러져 보이는지 실제로 걸려보고 판단한다. 바꿨다면 `DESIGN.md` 「캐릭터 애니메이션」 표에 반영.
  **(3) `qa_player_walk.gd` 를 다시 통과시킨다**(#19 에서 크기 어긋남으로 깨져 있다) — 네 방향 × 모든 프레임이 외형대로 칠해지는지 포함.
  `docs/design_reference/player_walk_x6.png` 도 갱신할 것.

- [x] #21 (2026-09-07 완료) [BUILD] **`qa_player_world.gd` / `qa_player_walk.gd` 가 프레임 수로 기다려서 빠른 기계에서 통째로 거짓 실패한다.** (2026-09-07, INBOX #17 바퀴가 발견 — 이 두 검사는 #17 의 변경과 무관하게 **손대지 않은 `main` 에서도 똑같이 실패한다**. #17 커밋 전에 stash 로 확인했다.)
  **(1) 원인은 이미 `docs/GOTCHAS.md` 에 적혀 있는 그것이다** — "`--script` 로 띄운 창은 수직동기화가 없어 프레임이 수백~수천 fps 로 돈다. N프레임 기다리기가 실제로는 몇 ms 라 고정 틱(1/60초)이 한 번도 안 돌 수 있다." 두 파일은 `HOLD_SECONDS`/`_wait_time` 으로 **일부 단계만** 시간 기준이고, 나머지는 `_wait = 3` 프레임이다.
  **(2) 실제 증상**: `qa_player_world` 의 `func(): _aim("up")` 다음 `_check_aim_turns_while_standing` 이 3프레임(≈2ms) 뒤에 재서, 틱이 한 번도 안 돌아 직전 방향인 `idle_left` 가 그대로 나온다. `qa_player_walk` 은 "걷는 동안 프레임이 1 종류만 나왔다"(10fps 애니메이션이 한 칸도 안 넘어감) 와 "키를 뗐는데 walk 로 남아 있다" 가 같은 이유로 10건 난다.
  **(3) 고치는 방법은 정해져 있다** — 기다림을 전부 시간 기준(`_wait_time`)으로 바꾼다. `qa_world_entry.gd` 의 `SETTLE_SECONDS`/`_settle()` 이 이번 바퀴에 그렇게 고친 본보기다. **고친 뒤 이가 있는지 반드시 확인할 것**(예: 조준을 무시하게 일부러 망가뜨려 FAIL 이 나는지) — 지금 이 검사들은 "아무도 안 움직여서" 통과하는 상태가 되기 쉽다.
  **(4) `qa_player_walk.gd` 는 2026-09-07 (INBOX #20) 에 이미 고쳐졌다** — #20 의 "(3) `qa_player_walk.gd` 를 다시 통과시킨다"를 지키려면 그 길밖에 없었다. `HOLD_SECONDS`/`SETTLE_SECONDS` + `Time.get_ticks_msec()` 로 바꿨고, `WALK_FPS` 를 0.02 로 망가뜨려 "그림이 멈춰 있다" FAIL 이 나는 것까지 확인했다. **이 항목에 남은 것은 `qa_player_world.gd` 하나다**(같은 바퀴에 세 번 돌려 한 번 실패 — 여전히 간헐적이다).

- [x] #22 (2026-09-07 완료) [BUILD] **맵을 마우스 휠로 줌인/줌아웃 한다.** `DESIGN.md` 「맵 (M)」 표와 그 아래 「맵 줌 (마우스 휠)」 항목이 이번에 추가됐으니 그대로 따를 것(2026-09-07 사람 결정). 지금은 `map_canvas.gd` 가 256×256 이미지를 정수 2배 고정으로 그린다.
  **(1) 정수 배율만 — 1 / 2 / 4 / 8배**, 휠 한 칸에 한 단계. 소수 배율을 끼우지 말 것(지도도 타일 하나가 픽셀 하나인 도트 그림이라 `STYLE_GUIDE.md` 1번이 그대로 적용된다). 확대/축소는 `draw_texture_rect` 의 대상 사각형을 바꿔서 하고, **텍스처 필터는 nearest 를 유지**한다(선형 보간이 켜지면 배율이 정수여도 뭉개진다).
  **(2) 1배는 월드 전체가 들어오고, 2배가 기본이다.** 2배보다 크면 화면에 다 안 들어가므로 **플레이어를 중심에 두고 잘라서** 보여주고, 지도 가장자리에서는 지도 밖 빈 공간이 나오지 않게 중심을 안쪽으로 물린다.
  **(3) 줌 단계는 지도를 닫았다 열어도 유지하되 저장하지 않는다** — 슬롯의 `explored` 옆에 끼워 넣지 말 것. 게임을 다시 켜면 기본 2배다.
  **(4) 휠 입력이 다른 곳으로 새지 않게 한다** — 지도가 열려 있을 때만 휠을 먹고, 나중에 붙을 핫바 스크롤 등과 겹치지 않도록 지도 화면이 입력을 소비(`set_input_as_handled`)한다.
  검증은 `game/qa/qa_map.gd` 를 넓힌다: 네 배율에서 실제로 그려지는 크기가 1/2/4/8배인지 / 확대했을 때 플레이어가 화면 중앙 근처에 있는지 / **지도 네 귀퉁이에서 지도 밖이 안 보이는지** / 배율을 바꿔도 안 가본 칸이 여전히 안 보이는지. 각 배율의 캡처를 남기고 눈으로 확인할 것.

- [x] #23 (2026-09-07 완료) [BUILD] **인벤토리 + 핫바 — 데이터 구조와 UI 까지.** `DESIGN.md` 「인벤토리 / 장비」와 「인벤토리 안전」, 「아이템 카테고리」, 「아이템 소유권 상태」를 그대로 따를 것. **아이템을 실제로 얻는 경로(채집/드롭/제작)는 이번 범위가 아니다** — 그건 자원과 도구를 만드는 다음 항목들이 붙인다. 이번 바퀴는 **테스트용 아이템을 코드로 넣어서** 창이 제대로 도는지까지만 본다.
  **(1) 슬롯 구성**: 일반 **18칸**(맨 위 **9칸이 곧 핫바** — 별도 UI 가 아니라 인벤토리의 일부다), 장비 **9칸**(모자1/상의1/하의1/신발1/목걸이2/반지2/가방1). 장비의 실제 효과는 범위 밖이라 넣고 빼지기만 하면 된다.
  **(2) 키**: **E** 로 창 열기/닫기, **숫자키 1~9** 로 핫바 칸을 손에 든다. 왼쪽 위 상시 요약 HUD 는 만들지 않는다(DESIGN.md 명시). **Esc 순서에 끼워 넣을 것** — `world.gd` 의 `_unhandled_input` 한 곳에서 정하는 규칙이 이미 있다(설정 → 지도 → 일시정지). 인벤토리도 거기에 넣고, **창이 열린 동안 플레이어 입력을 끊는다**(지도와 같은 처리).
  **(3) 드래그로 옮기기**: 일반↔일반, 일반↔장비, 핫바 포함. **창 바깥으로 끌어다 놓으면 버리기** — 인벤토리에서 사라지고 플레이어 근처 바닥에 드롭된다. 바닥 아이템의 그림/줍기는 이번 범위가 아니므로, **드롭은 "월드에 아이템이 놓였다"는 데이터까지만** 만들고 그리는 건 다음 항목에 넘긴다(자리는 남겨둘 것).
  **(4) 「인벤토리 안전」을 처음부터 지킨다** — 아이템을 넣는 모든 경로는 넣기 전에 공간을 확인하고, 모자라면 **넣을 수 있는 만큼만 넣고 나머지는 원래 자리에 남긴다.** 절대 조용히 사라지면 안 된다. 이 규칙을 **순수 클래스의 반환값으로** 표현할 것(넣은 수량 / 남은 수량을 돌려준다) — 호출한 쪽이 남은 것을 처리하지 않으면 못 넘어가는 모양이어야 한다.
  **(5) 아이템 정의에 「아이템 소유권 상태」 필드를 지금 넣어둔다** — 값은 항상 `로컬` 하나뿐이고 아무 동작도 하지 않는다. 나중에 저장 포맷을 바꾸면 기존 세이브가 깨지기 때문이다.
  **(6) 멀티플레이 대비 — 인벤토리 로직은 노드가 아닌 순수 클래스에 둔다**(`player_motion.gd` / `explored_map.gd` 와 같은 방식). 나중에 서버가 같은 코드를 돌린다: 「서버 권위」의 "재료 소모 / 제작 완료는 전부 서버가 계산한다" 가 이 클래스를 그대로 부르는 모양이어야 한다. **노드는 그리기와 입력만** 한다.
  **(7) 슬롯 저장에 들어간다** — 캐릭터마다 따로(`SlotStore`, 탐험 기록이 들어간 자리와 같다). 저장했다 불러와도 칸 배치와 수량이 그대로여야 한다.
  검증은 `game/qa/` 에 남긴다: 코어(꽉 찬 인벤토리에 넣으면 넘치는 몫이 안 사라지는지 / 겹치기 쌓임 / 저장·불러오기 왕복)와 화면(E 로 열고 닫기, 숫자키로 손에 든 것이 바뀌는지, 드래그로 옮겨지는지, 바깥에 놓으면 사라지고 드롭 데이터가 생기는지, 창이 열린 동안 안 움직이는지). 창을 연 캡처를 눈으로 확인할 것.

- [x] #24 (2026-09-07 완료) [DESIGN] **도끼를 든 캐릭터 — 도구 그림 파이프라인의 첫 사례.** `DESIGN.md` 「새 도구를 추가하는 절차」가 이번에 새로 생겼으니 그 [DESIGN] 쪽 6단계를 그대로 따를 것. **도구는 앞으로 6종이 더 붙는다(총/곡괭이/낫/괭이/물뿌리개/낚싯대) — 이번 바퀴가 그 6번을 결정한다.** 잘 나온 방식이든 값을 치르고 알아낸 함정이든 `docs/STYLE_GUIDE.md` 에 남겨서, 다음 도구가 같은 삽질을 반복하지 않게 할 것.
  **(1) 모션 3종**: `hold_axe`(들고 있기) / `use_axe`(패기) / `walk_axe`(도끼 든 채 걷기). 각 4방향, 머리모양 4종 = **시트 12장**. 프레임 수는 idle/walk 규칙에 맞춰 정하고 `DESIGN.md` 「캐릭터 애니메이션」 표에 적는다.
  **(2) 도끼는 프레임 자체에 그려 넣는다** — 캐릭터 옆에 아이콘으로 띄우지 않는다(「캐릭터 애니메이션」의 못박힌 규칙). `gen_character.py` 에서 **기존 `character()` 에 오프셋만 넣는 방식**을 유지할 것 — 형태를 다시 정의하면 프레임 사이에서 캐릭터 크기가 어긋난다. **17px 캔버스라 도끼에 쓸 수 있는 도트가 몇 개 안 된다** — 자루와 날이 구분되는 최소한의 실루엣을 찾는 게 이번 바퀴의 진짜 어려움이다. 후보를 여러 개 뽑아 나란히 비교할 것.
  **(3) 노드는 `player_frames.gd` 의 `MOTIONS` 에 세 줄 늘리는 것 말고 고치지 않는다.** 안 그래도 되게 되어 있다면 그게 맞는 상태다 — 노드를 고쳐야만 한다면 그 이유를 STATUS.md 에 남길 것(다음 도구 6번에 그대로 반복될 비용이다).
  **(4) `qa_sprite_check.py` 의 `SPECS` 와 `qa_player_walk.gd` 를 세 모션까지 보게 넓힌다.** 안 넓히면 새 모션은 아무도 검사하지 않는다. **`use_axe` 는 idle→사용 이음(「이어짐」)이 특히 중요하다** — 자세가 갑자기 다른 캐릭터처럼 변하면 안 된다.
  **(5) 뒷모습(up)에서 도끼가 몸에 완전히 가려지면 안 된다** — 무엇을 들었는지 알 수 없다. 네 방향 모두에서 도끼가 읽히는지 캡처로 확인할 것.
  **(6) 인벤토리 칸에 보일 도끼 아이템 아이콘도 같은 바퀴에 만든다** — 「아이템/오브젝트 크기 표준」의 화면 최소 16px 하한을 지킨다. #23 인벤토리가 그 아이콘을 쓸 자리를 이미 갖고 있을 것이다.
  **도끼의 실제 동작(좌클릭 벌목·나무·목재 드롭)은 이번 범위가 아니다** — 「새 도구를 추가하는 절차」의 [BUILD] 쪽이고 별도 항목으로 넣는다. 이번 바퀴는 **그림과 애니메이션까지만**이다.

- [x] #25 (2026-09-07 완료) [BUILD] **하단 핫바 + 든 도구가 캐릭터에 보이고, 좌클릭하면 사용 모션이 나온다.** 사람 피드백: *"마인크래프트처럼 하단바에 1~9 인벤토리가 보이게 하고 이걸 들어야지. 1번칸에 도끼가 있고 1번 키를 누르면 도끼가 들려있고 마우스 왼쪽 클릭을 해서 모션까지 나오게."* `DESIGN.md` 「인벤토리 / 장비」의 하단 핫바 항목과 「캐릭터 애니메이션」의 "손에 든 도구가 캐릭터에 그대로 보인다"가 이번에 추가됐으니 그대로 따를 것. **재료는 거의 다 있다** — #23 이 9칸·숫자키·아이콘을, #24 가 `hold_axe`/`use_axe`/`walk_axe` 시트를 이미 만들어뒀고 `player_frames.gd` 가 그 셋을 이미 싣고 있다. 없는 것은 아래 셋이다.
  **(1) 화면 아래에 항상 보이는 핫바** — 인벤토리 맨 위 9칸을 그대로 비춘다. **별도 보관함을 만들지 말 것**: 같은 `inventory.gd` 코어를 보고 그리기만 한다(E 창에서 1번 칸을 바꾸면 하단 핫바도 같이 바뀌어야 한다). 고른 칸을 눈에 띄게 표시하고 빈 칸도 자리를 지킨다(칸이 사라지면 번호와 자리가 어긋난다). 아이콘 그리기는 `inventory_panel.gd` 가 이미 하는 일이니 **그 코드를 다시 쓰고 새로 짜지 말 것**. 지도·인벤토리·일시정지 창이 열리면 핫바는 가려도 된다(판단은 자유, 고른 쪽을 STATUS 에 남길 것).
  **(2) 든 도구가 캐릭터에 보인다** — 도끼를 고르면 `hold_axe`, 걸으면 `walk_axe`, 빈손이면 예전대로 `idle`/`walk`. **`player.gd` 는 지금 idle/walk 만 고른다** — 든 것에 따라 모션 이름 앞부분을 고르게 넓힌다. 시트는 이미 실려 있으니 새로 굽지 않는다. **아직 도끼 하나뿐이라 나머지 6종은 시트가 없다** — 시트가 없는 도구를 들면 빈손 모션으로 두고 조용히 넘어갈 것(에러로 죽지 말 것). `player_frames.gd` 의 `TOOLS` 가 무엇이 있는지 알고 있다.
  **(3) 좌클릭 → 사용 모션** — `use_axe` 가 한 번 재생되고 끝나면 `hold_axe` 로 돌아온다. **대상이 없어도(허공에 대고) 나와야 한다**(「채집 계열」 — 나중에 근접무기로 쓸 계획이라 대상이 있을 때만 나가면 근접 공격이 성립하지 않는다). 재생 중에 또 눌러도 겹쳐 재생되지 않게 할 것. **실제 벌목(나무가 넘어가고 목재가 드롭)은 이번 범위가 아니다** — 월드에 나무가 아직 없다. 이번 바퀴는 **모션까지만**이다.
  **(4) 멀티플레이 대비** — "몇 번 칸을 들었는가"와 "좌클릭했는가"는 **입력**이다(「서버 권위」). 마우스 각도를 `player_motion.gd` 로 넘긴 #16 과 같은 방식으로, 든 칸과 클릭을 같은 입력 구조체에 담는다. **어느 모션을 그릴지 고르는 것은 그리기이므로 노드에 둬도 되지만, "무엇을 들고 있는가"는 플레이어 상태다** — 화면 상태로 두지 말 것.
  **(5) 키 처리는 `world.gd` 의 `_unhandled_input` 한 곳** — 이미 Esc/E/M/숫자키가 다 거기 있다. 좌클릭도 같은 자리에 넣고, **창이 열려 있을 때는 안 먹게** 한다(인벤토리에서 드래그하는 좌클릭과 부딪힌다).
  검증은 `game/qa/` 에 남긴다: 1번 칸에 도끼를 넣고 1을 누르면 `hold_axe` 로 바뀌는지 / 걸으면 `walk_axe` 인지 / 좌클릭에 `use_axe` 가 돌고 끝나면 `hold_axe` 로 돌아오는지 / 빈 칸을 고르면 빈손으로 돌아오는지 / E 창에서 칸을 옮기면 하단 핫바가 따라오는지 / 창이 열린 동안 좌클릭이 안 먹는지. **하단 핫바가 실제로 보이는 캡처와 도끼를 든 채 걷는 캡처를 눈으로 확인할 것.**

<!-- #26~#31 은 도구 1단계(그림 + 모션)다. `DESIGN.md` 「새 도구를 추가하는 절차」의
     1단계 8항목을 그대로 따르므로 항목마다 다시 적지 않는다 — 아래에는 그 도구만의
     특이점만 적는다. 실제 효과(벌목/채광/사냥/낚시/밭일구기)는 2단계이고, 대상
     오브젝트를 만드는 바퀴가 붙인다. -->
- [x] #26 (2026-09-07 완료) [DESIGN] **곡괭이를 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `pickaxe` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점: 채광 자세다. **도끼(#24)와 뚜렷이 구별돼야 한다** — 둘 다 자루 끝에 머리가 달린 도구라 17px 에서 실루엣이 겹치기 쉽다. 도끼는 날이 한쪽으로 넓고, 곡괭이는 양쪽으로 뾰족한 것이 차이다. **두 아이콘과 든 자세를 나란히 놓고 확인할 것.** **실제 효과는 2단계이고 이번 범위가 아니다.**
- [x] #27 (2026-09-07 완료) [DESIGN] **낫을 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `sickle` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점: 채집·수확 자세다. **날이 휘어 있다** — 도끼/곡괭이의 곧은 머리와 달리 곡선이라 오히려 구별이 쉽지만, 17px 에서 곡선은 계단으로 뭉개지기 쉽다. 휨이 읽히는 최소 크기를 찾을 것. 휘두르는 방향은 **가로로 베는 쪽**이다(도끼는 내리찍는다). **실제 효과는 2단계이고 이번 범위가 아니다.**
- [x] #28 (2026-09-07 완료) [DESIGN] **총을 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `gun` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점:
  **(가) 사용 모션 = 발사다.** **양손으로 잡는 유일한 도구**라 드는 자세부터 다른 도구와 다르다 — 한 손은 방아쇠, 한 손은 총열을 받친다. 발사 프레임에는 **총이 뒤로 밀리는 되튐**이 보여야 한다(「총기 스탯」의 반동). 되튐은 캐릭터를 통째로 밀지 말고 팔·총만 움직인다 — 통째로 밀면 「자리」 검사에 걸리고 실제로도 미끄러져 보인다.
  **(나) 조준선은 이번 범위가 아니다.** 2026-09-07 에 「전투」로 조준선이 정해졌지만(총을 들었을 때만 나오는 빨간 선, 가만히 있으면 선명해진다), 그건 그림이 아니라 그리기 로직이고 **탄퍼짐과 한 몸이라 총알을 만드는 바퀴가 같이 붙인다.** 이번 바퀴는 시트만 만든다 — 조준선을 만들지 말 것.
  **(다) 사거리·탄창·재장전(R)·탄종 전환(우클릭)·총알도 전부 2단계다.** 이번 바퀴에 만들지 않는다. 참고로 **이 서버는 PvE 라 총알이 사람을 맞히지 않는다**(2026-09-07, 「전투」) — 사람을 쏘는 자세를 상정하고 그리지 말 것.
- [x] #29 (2026-09-07 완료) [DESIGN] **괭이를 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `hoe` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점: 밭을 일구는 자세다. 내리찍는 동작이라 **도끼와 겹치기 쉽다** — 도끼는 날이 세로로 서 있고 괭이는 날이 가로로 눕는 것이 차이다. 나란히 놓고 확인할 것. 밭 타일 자체는 2단계다. **실제 효과는 2단계이고 이번 범위가 아니다.**
- [x] #30 (2026-09-07 완료) [DESIGN] **물뿌리개를 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `watering_can` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점: **날이 없는 유일한 도구**라 실루엣이 완전히 다르다 — 통과 주둥이. 사용 모션은 내리찍는 게 아니라 **기울여 붓는 동작**이다. 물줄기를 그릴지는 판단에 맡기되, 그렸다면 그것도 프레임 안에 그린다(옆에 따로 띄우지 않는다 — 「캐릭터 애니메이션」). 물 보충 방법은 아직 정해진 바 없고 2단계다. **실제 효과는 2단계이고 이번 범위가 아니다.**
- [x] #31 (2026-09-07 완료) [DESIGN] **낚싯대를 든 캐릭터 — 그림과 모션까지 (1단계).** `DESIGN.md` 「새 도구를 추가하는 절차」의 **1단계 8항목을 그대로 따를 것.** `player_frames.gd` 의 `TOOLS` 에 `rod` 를 넣으면 시트가 실리고, 핫바에서 골라 좌클릭하면 모션이 나오는 배선은 #25 가 도끼로 이미 깔아뒀다 — **도구 이름만 늘어난다.** 이 도구만의 특이점:
  **(가) 사용 모션(`use_rod`)은 만들지 않는다.** 2026-09-07 사람 결정 — 낚시의 피드백은 캐릭터 자세가 아니라 **찌가 날아가 물에 떨어지는 것**이고, 그건 그림이 아니라 던지기 로직이라 2단계다. **`hold_rod` 와 `walk_rod` 는 만든다** — 「캐릭터 애니메이션」의 "도구를 캐릭터 옆에 아이콘으로 띄우지 않는다"(절대 되돌리지 말 것)를 지키려면 낚싯대를 든 게 보여야 하기 때문이다. 그래서 이 바퀴는 **다른 도구보다 시트가 3분의 1 적다**(모션 2종 × 머리모양 4종 = 8장).
  **(나) 실루엣이 다른 도구와 완전히 다르다** — 날이 없고 **가늘고 긴 대**다. 17px 에서 1px 굵기의 긴 선은 외곽선에 먹히거나 「각짐」(곧은 구간)에 걸리기 쉽다. 살짝 휘게 하는 것을 먼저 시도해볼 것.
  **(다) 던지기(거리 조절 · 물 판정 · 찌)는 2단계다** — 규칙은 `DESIGN.md` 「생활 스킬 — 채집 계열」의 낚시 항목에 정해져 있다(좌클릭으로 던지고, 거리는 마우스로 조절하며, **찌가 떨어진 칸이 물이어야** 시작된다). 이번 바퀴에 만들지 않는다. **실제 효과는 2단계이고 이번 범위가 아니다.**
- [x] #32 (2026-09-07 완료) [BUILD] **`qa_player_sprite.gd` 의 「도구를 든 모습을 풀밭에 늘어놓는 판」이 도구 3종부터 화면 밖으로 나간다.** 지금 짜임은 **한 줄에 [맨손 idle · 도구마다 (들고 있기 + 사용 6프레임)]** 을 전부 늘어놓는 것이라 칸 수가 `1 + 도구수 × 7` 로 는다 — 도구 2종(도끼·곡괭이)에 15칸 = 969px 로 아직 1280 안에 들어오지만, **3종이면 22칸 = 1410px 라 뷰포트(1280×720) 밖으로 잘린다.** 도구는 #27~#31 로 다섯 개가 더 붙으므로 그 전에 손봐야 한다. 이 판의 목적은 `STYLE_GUIDE.md` 7번 6항의 "기존 자산과 나란히 놓고 어울리는지 보는 것"이니, **도구마다 캡처를 따로 남기는 쪽**(`81_player_tools_on_grass_<도구>.png`)이 가장 단순해 보이지만 판단은 맡긴다 — 다만 **비교 대상(맨손 idle 과 이미 있는 도구)이 한 장 안에 같이 있어야 한다**는 목적은 지킬 것. (2026-09-07, INBOX #26 바퀴에서 발견. 그 바퀴는 풀밭 넓이가 그림보다 좁던 칸 수 계산 버그만 고쳤다.)

- [x] #33 (2026-09-07 완료) [BUILD] **버린 아이템이 실제로 바닥에 보이고 주울 수 있어야 한다 — 지금은 그냥 사라진다.** 사람이 신고: *"인벤토리에서 밖으로 빼면 아이템이 버려져야 되는데 그냥 없어지는 거 같더라고."* **코드가 고장난 건 아니다** — #23 이 "데이터까지만"으로 범위를 끊었고(`world.gd` 의 `_on_drop_outside` 가 `ground_items.drop()` 을 부르긴 한다), **그림도 줍기도 저장도 없어서** 사람 눈에는 사라진 것과 같다. `DESIGN.md` 「아이템 획득 방식 — 바닥 드롭」에 이번에 저장 규칙이 추가됐으니 그대로 따를 것.
  **(1) 바닥에 보인다** — 버린 자리에 아이템 아이콘이 그려진다(`item_types.gd` 의 `icon`, 없으면 지금의 자리표시). 캐릭터와 앞뒤(Y) 정렬이 어긋나지 않게 할 것.
  **(2) 다가가면 자동으로 주워진다** — 습득 버튼은 없다(좌클릭은 도구 동작이다). **인벤토리가 꽉 차면 넣을 수 있는 만큼만 넣고 나머지는 바닥에 그대로 남긴다** — `inventory.gd` 의 `take_in(뭉치)` 가 이미 그 모양이다(「인벤토리 안전」). 버리자마자 도로 주워지는 일이 없게 할 것(잠깐의 유예 등 — 방법은 자유).
  **(3) 저장된다** — 월드를 나갔다 들어와도 버린 자리에 그대로 있어야 한다. 지금 `ground_items` 는 월드에 들어올 때마다 새로 만들어져서 통째로 날아간다. 슬롯 저장(`slot_store.gd` — `explored`/`inventory` 옆)에 자리를 만들 것.
  **(4) 바닥에 오래 놓인 것을 언제 치울지 정하고 `DESIGN.md` 에 적는다** — 영원히 두면 월드가 지저분해진다. **데스드롭 상자의 30분과 같은 값을 쓸 이유는 없다**(그건 죽음 페널티의 강도이고 이건 청소 주기다).
  검증은 `game/qa/` 에 남긴다: 버리면 바닥에 보이는지 / 다가가면 주워지는지 / **꽉 찬 인벤토리에서 넘치는 몫이 바닥에 남는지** / 나갔다 들어와도 남아 있는지. 바닥에 놓인 것이 보이는 캡처를 눈으로 확인할 것.

- [x] #34 (2026-09-07 완료) [BUILD] **총알 — 투사체가 실제로 날아간다. + 조준선.** `DESIGN.md` 「전투」의 2026-09-07 결정을 그대로 따를 것.
  **(1) 총알은 투사체다.** 쏘면 실제로 날아가고 피할 수 있다. **즉시판정(히트스캔)으로 만들지 말 것** — 그 차이가 이 항목의 전부다. 사거리 **800**을 다 날아가면 사라지고, 벽 등 막힌 지형에 닿아도 사라진다.
  **(2) 총알 시뮬레이션은 순수 클래스에 둔다**(「시뮬레이션 구조」/「서버 권위」) — 나중에 서버가 화면 없이 같은 코드를 돌린다. 노드는 그리기만 한다. **한 틱 이동량이 타일(48)보다 훨씬 작아야 한다** — 크면 벽을 통째로 건너뛴다(`player_motion.gd` 가 같은 함정을 이미 겪었다, `docs/GOTCHAS.md`).
  **(3) 조준선** — 총을 들고 있을 때만 조준 방향으로 빨간 선이 뻗고, **가만히 있으면 선명해지고 움직이면 흐려진다.** 그 선명도가 곧 정조준이고 **탄퍼짐과 같은 값**이다(따로 계산하지 말 것). 쏘면 반동으로 정조준이 풀리고 멈추면 다시 모인다. 총을 내리면 선도 사라진다.
  **(4) 각도는 스냅된 4방향이 아니라 원본 각도를 쓴다** — `player_motion.gd` 에 이미 그 자리가 주석으로 남아 있다("총알/시야 콘처럼 정확한 방향이 필요한 쪽은 여기를").
  **(5) 지금은 맞을 대상이 없다** — 동물이 아직 없고 **이 서버는 PvE 라 사람도 안 맞는다.** 그래서 이번 바퀴의 명중 판정은 **지형에 막히는 것과 사거리 소진까지**다. 대상에 맞았을 때의 처리(데미지)는 자리만 남기고 다음 항목이 채운다.
  **탄창·재장전·탄종 전환은 #35 다.** 이번 바퀴는 좌클릭하면 무한히 나간다.

- [x] #35 (2026-09-07 완료) [BUILD] **탄창 8발 + R 재장전 + 우클릭 탄종 전환 + 총 UI.** 사람 요청: *"기본탄이랑 마취탄으로 마우스 우클릭으로 총알 변경이 되게, 총에 관한 UI는 총을 들었을 때만."* 수치는 `DESIGN.md` 「총기 스탯」에 다 있다.
  **(1) 탄창 8발.** 다 쏘면 좌클릭해도 안 나간다. **R 로 재장전**(약간의 시간 소요). 예비 탄약은 없다 — R 을 누르면 항상 가득이다.
  **(2) 우클릭으로 탄종 전환**(기본탄 ↔ 마취탄). **탄종마다 잔여 발수를 따로 관리한다** — 기본탄을 다 쓰고 마취탄으로 바꿔도 마취탄 탄창은 가득이어야 한다(하나의 수를 공유하면 안 된다).
  **(3) 총 UI 는 총을 들었을 때만 나온다** — 「총기 스탯」이 이미 "탄창에 남은 발수는 총을 들고 있을 때만 화면에 표시한다"고 정해뒀다. 다른 도구를 들거나 빈손이면 사라진다. **상시 HUD 를 늘리지 말 것**(「인벤토리 / 장비」— 상시로 두는 것은 핫바 하나다). 지금 든 탄종도 같이 보여야 한다.
  **(4) 연사 0.5초 간격**(초당 2발)을 지킨다 — 좌클릭을 아무리 빨리 눌러도 그보다 빨리 안 나간다.
  **(5) 멀티 대비**: 남은 발수와 든 탄종은 화면 상태가 아니라 **플레이어 상태**다 — 서버가 "이 발사가 유효한가"를 판정하려면 알아야 한다(「서버 권위」).

- [x] #36 (2026-09-07 완료) [BUILD] **플레이어 체력 + 죽음 + 리스폰.** `DESIGN.md` 「체력 / 죽음 / 리스폰」 그대로.
  **(1) 최대 체력 100.** 「시뮬레이션 구조」대로 순수 클래스에 둔다.
  **(2) 죽으면 리스폰 지점에서 되살아난다** — 지금은 **월드를 처음 만들 때의 스폰 좌표**다(침대는 건축 시스템과 같이 오므로 이번 범위가 아니다. 나중에 침대가 그 값을 덮어쓸 수 있게 **리스폰 지점을 값 하나로 분리해둘 것**).
  **(3) 아직 죽을 방법이 없다** — 동물이 없고 PvE 라 사람도 안 맞는다. 그래서 **자체 QA 가 코드로 데미지를 주어 검증한다**(`take_damage()` 를 직접 부른다). 실제 게임에서 죽는 일은 동물이 생긴 뒤부터다 — 그 상태를 STATUS 에 분명히 남길 것(다음 바퀴가 고장으로 오해하지 않게).
  **(4) 인벤토리는 이번에 건드리지 않는다** — 죽을 때 아이템이 어떻게 되는지는 #37 이다. 이번 바퀴는 되살아나기만 한다.

- [x] #37 (2026-09-07 완료) [BUILD] **데스드롭 상자 + 월드 설정.** `DESIGN.md` 「데스드롭 상자」와 「월드 설정」 그대로.
  **(1) 죽으면 그 자리에 상자가 생기고 인벤토리 내용물이 전부 들어간다.** #33 의 바닥 아이템과 **다른 오브젝트다**(그건 낱개, 이건 담는 것). 건축의 설치형 상자와도 다르다(그건 타이머가 없다).
  **(2) 타이머 기본 30분.** 다 되면 상자와 내용물이 **함께** 사라진다.
  **(3) 열면 타이머가 멈춘다.** 근거를 문서가 들고 있다 — *"정해진 시간은 「찾아올 시간」이지 「꺼낼 시간」이 아니다."* 창을 닫으면 다시 흐르고, **열어둔 채 멀리 떠나면 닫힌 것으로 친다**(안 그러면 영영 안 사라진다).
  **(4) 월드 설정 화면을 만들고 타이머를 거기 둔다** — 그래픽 설정(`user://settings.json`, 기계 단위)과 **다른 자리**다. 월드 설정은 **그 슬롯에 저장된다.** 일시정지 메뉴에서 열 수 있게 할 것.
  **(5) 미정 하나를 정하고 적을 것**: 다른 플레이어가 남의 데스드롭 상자를 열 수 있는가. PvE 협동이라 파티원이 대신 주워다 주는 게 자연스럽지만, 그대로 두면 남의 짐을 가져갈 수도 있다.

- [x] #38 (2026-09-07 완료) [DESIGN] **바닥에 놓인 아이템의 도트 크기가 월드와 어긋난다(2배)와, 아이콘이 없는 아이템의 자리표시 상자가 "떠 있는 궤짝"처럼 보인다.** (2026-09-07, INBOX #33 바퀴에서 발견 — 기능은 다 됐고 이건 그림 문제라 [DESIGN] 으로 넘긴다.)
  **(1)** 바닥 아이템은 인벤토리 아이콘(아트 17px)을 **2배**로 그린다 — 34px 라 `DESIGN.md` 「아이템/오브젝트 크기 표준」의 하한 16px 을 넘고 플레이어(51px)의 2/3 라 비율은 맞지만, **도트 하나가 화면에서 2px** 이라 캐릭터·지형의 3px 과 다르다. 3배(51px)로 올리면 도끼가 사람만 해지므로, 제대로 고치려면 **바닥용 스프라이트를 더 작은 캔버스(예: 11~12px)에 따로 굽고 3배로 그리는** 쪽이다. 실제 캡처(`76_ground_tool.png`)에서는 어색해 보이지 않았으니 급하지는 않다.
  **(2)** 아이콘이 아직 없는 아이템(목재/돌 등)은 인벤토리와 같은 자리표시(색 사각형 + 두 글자)를 34px 상자로 그린다. 인벤토리 칸 안에서는 자연스럽지만 **풀밭 위에 놓이면 궤짝처럼 보인다.** 원재료 아이콘이 생기면 저절로 없어질 문제다 — 그 전에 손볼지는 판단에 맡긴다.

- [x] #39 (2026-09-07 완료) [BUILD] **총알이 물에 막히지 않게 한다 — 「총알을 막는가」를 「걸을 수 있는가」에서 떼어낸다.** 사람이 정했다(2026-09-07): *"당연히 물에도 총알이 쏴지지. 한 칸 물이 있다고 해서 거기에 막히면 안 되잖아."* 근거는 `DESIGN.md` 「전투」에 적혀 있다.
  **(1) `bullets.gd` 의 `blocked_at()` 이 이동 판정을 그대로 쓰고 있다** — 그래서 물에 막힌다. 총알용 판정을 따로 둔다. **`player_motion.gd` 의 이동 판정은 건드리지 말 것**(걸어서 물을 건널 수 있게 되면 안 된다).
  **(2) 지금은 총알을 막는 지형이 하나도 없다.** 벽도 나무도 아직 없기 때문이다(「플레이어 이동」의 "지금 막는 것은 바다 타일뿐"). 그러니 이번 바퀴 뒤 총알은 **사거리 800 을 다 날아가야만 사라진다** — 그게 정상이다. STATUS 에 분명히 남길 것(다음 바퀴가 고장으로 오해하지 않게).
  **(3) 나중에 벽·나무가 총알을 막을 자리를 남긴다** — 「전투」가 "새 오브젝트는 자기가 총알을 막는지 스스로 정한다"로 정해뒀다. 그 오브젝트를 만드는 바퀴가 한 줄 적으면 되는 모양이어야 하고, 이번에 그 자리만 만든다(껍데기를 미리 채우지는 말 것).
  검증은 `game/qa/qa_bullets.gd` 를 넓힌다: **물 위로 쏘면 총알이 지나가는지** / 사거리를 다 쓰면 사라지는지 / **플레이어는 여전히 물에 못 들어가는지**(이동 판정을 안 건드렸다는 확인).
- [x] #40 (2026-09-07 완료) [DESIGN] **데스드롭 상자 스프라이트.** (2026-09-07, INBOX #37 바퀴에서 남긴 것 — 기능은 다 됐고 그림만 자리표시다.)
  지금 상자는 `game/scripts/death_box_node.gd` 가 `draw_rect` 로 그린 **궤짝 자리표시**다(나무 몸통 + 쇠 띠 두 줄 + 자물쇠 + 그 위의 남은 시간 막대). 바닥 아이템의 자리표시(`ground_item_node.gd`)와 같은 성격이고, 도트 단위(화면 3px)와 크기(48 × 42 = 아트 16 × 14px)는 이미 지키고 있다.
  **(1)** Pillow 로 아트 16 × 14px 스프라이트를 굽고(`gen_character.py` 옆에 상자용 생성기), `death_box_node.gd` 가 그걸 그리게 바꾼다 — 크기·원점(밑동) 약속은 그대로 두면 짚는 자리(`death_boxes.gd` 의 `box_rect`)를 안 건드려도 된다.
  **(2) 남은 시간 막대는 그림이 아니라 UI 다** — 스프라이트로 굽지 말고 지금처럼 코드로 그린다(값에 따라 길이가 변한다).
  **(3)** 「열려 있는 동안 테두리가 밝아지는 것」도 지금은 코드다. 뚜껑이 열린 프레임을 따로 굽는 쪽이 더 나을 수 있다 — 판단에 맡긴다.
  **(4)** 「건축」의 설치형 상자가 생기면 **둘이 구별돼야 한다**(데스드롭은 타이머가 있고 꺼내기만 되는 것이다). 지금 실루엣을 그때 재사용할지도 같이 정할 것.

- [x] #41 (2026-09-07 — **완료가 아니라 `LATER.md` 로 옮김.** 조건("열 수 있는 대상이 둘 이상")이 안 차서 큐를 막고 있었다) [BUILD] **여는 좌클릭이 도구를 쓰는 좌클릭과 겹치는 것 — 대상이 늘어나면 규칙을 한 곳으로 모을 것.** (2026-09-07, INBOX #37 바퀴에서 나온 것이다. 지금은 고장이 아니다.)
  「조작」에 적었듯 좌클릭 하나가 "도구 쓰기"와 "여는 것"을 겸하고, 지금은 `world.gd` 의 `_unhandled_input` 이 **데스드롭 상자만** 먼저 물어본다(`_try_open_death_box`). 상자·제작대·문이 생기면 그 물음이 여러 줄로 늘어난다 — 그때 **"커서 아래에 있고 손이 닿는, 열 수 있는 것"을 한 번에 답하는 자리**를 만들고 거기로 모을 것. 지금 미리 만들면 대상이 하나뿐이라 껍데기만 늘어난다.

- [x] #42 (2026-09-07 완료) [BUILD] **버린 아이템이 발밑에 놓여 캐릭터에 완전히 가려진다.** (2026-09-07, INBOX #38 바퀴에서 발견 — 고장이 아니라 **지금 코드가 그렇게 놓는 것**이다.)
  `world.gd` 의 `_on_drop_outside` 가 버린 뭉치를 **플레이어의 좌표 그대로**에 놓는다(`ground_items.drop(stack, (%Player).global_position)`). `DESIGN.md` 「바닥 드롭」은 *"플레이어 근처 바닥에 드롭"* 이라고만 적어뒀으니 어긋난 것은 아니다.
  **그런데 INBOX #38 에서 바닥 그림이 34px 정사각형에서 더미(30 × 24px)로 바뀌면서, 버린 것이 캐릭터 뒤에 통째로 숨는다** — 실제로 자체 QA 가 "버린 자리에 목재가 안 보인다"로 먼저 잡았고, 지금은 검사 쪽에서 아이템을 잠깐 옆으로 옮겨 그림을 확인한다(`qa_ground_items.gd` 의 `SHOW_OFFSET`).
  **판단이 필요한 이유**: 버린 것이 안 보이면 "버려졌나?"를 확인할 방법이 그 자리를 벗어나는 것뿐이다. 고친다면 **발밑이 아니라 바라보는 방향 앞쪽 한 칸 남짓**에 놓는 쪽이 자연스러운데, 그러면 (가) 물 위에 떨어질 수 있고 (나) 「버린 사람이 그 자리를 벗어나야 다시 주워진다」의 "그 자리"가 플레이어 자리와 달라진다. **어느 쪽이든 `DESIGN.md` 「바닥 드롭」에 근거를 적고 나서 고칠 것.**

- [x] #43 (2026-09-07 완료) [BUILD] **자체 QA 두 벌이 「그때 사람 손이 마우스를 어디 뒀는가」에 따라 통과하거나 실패한다.** (2026-09-07, 미완료 항목이 #39/#41 둘 다 조건 미달이라 비어 있던 바퀴에 전체 자체 QA 를 돌려보다 발견했다.)
  `qa_ground_items.gd` 는 **아래를 보고 버렸다고 전제**하고("바라보는 쪽(기본은 아래)") 아이템이 캐릭터 앞(아래)에 놓였는지를 보는데, **바라보는 방향은 실제 마우스 자리가 정한다**(`player.gd` 의 `aim_angle_for`). 창을 띄워 도는 QA 가 커서를 한 번도 안 옮기므로, 그 순간 OS 커서가 창 중심보다 위에 있으면 캐릭터는 위를 보고 아이템도 위로 간다 — 「앞으로 안 갔다」로 **거짓 실패**한다. 반대로 커서가 아래 있으면 방향을 한 번도 안 정해보고 **거짓 통과**한다.
  `qa_inventory.gd` 도 같은 뿌리다. 창이 열려 있는 동안 조준이 얼어붙으므로(`input_enabled`), 창을 열기 전 커서 자리가 그대로 버려지는 방향이 된다 — 위로 버려지면 뒤의 「창이 열린 동안 안 움직인다」가 `move_up` 으로 아이템 쪽으로 걸어가서 도로 주워지고, 저장/불러오기 검사가 「버린 아이템이 다시 들어왔다」로 실패한다.
  **INBOX #42 바퀴에 들어온 것이다** — 그 전에는 버린 것이 발밑에 놓여서 방향이 결과를 안 바꿨다. 고친 뒤로도 그 바퀴에서는 통과했는데, 그때 커서가 우연히 창 아래쪽에 있었기 때문이다.
  **고침**: 두 스크립트 모두 버리기 전에 `Input.warp_mouse()` 로 조준을 못 박는다(`_aim_at()` — `qa_player_world.gd` 와 같은 방식이라 노드의 "마우스 → 각도" 변환까지 그대로 지나간다). **먼저 정반대(위)에 두었다가 아래로 옮긴다** — 그래야 그 warp 가 실제로 먹었는지까지 확인된다.

- [x] #44 (2026-09-07 완료 — **아무것도 채택하지 않았다. 판단 재료만 만들었다**: `docs/design_reference/canvas_26_vs_17.png` + `game/tools/try_canvas.py`. 갈래·비용·바퀴 수 추정은 `docs/STATUS.md`. **사람이 (가)26px 로 옮긴다 / (나)17px 로 남는다 를 정해야 다음 항목이 나온다**) [DESIGN] **캔버스 26px 를 시험만 해본다 — 판단 재료를 만드는 것이지 바꾸는 것이 아니다.** 사람 요청(2026-09-07): 참고작(Fields of Mistria) 수준의 디테일이 가능한지 보려고 **한 바퀴만** 써서 확인한다. **이번 바퀴는 아무것도 채택하지 않는다.**
  **(1) 게임 자산을 절대 건드리지 말 것.** `game/assets/sprites/` 의 지금 17px 시트를 덮어쓰지 않는다. 게임은 이 바퀴 뒤에도 **지금 그대로 17px 로 돌아야 한다** — 자체 QA 를 돌려 그것을 확인할 것. `DESIGN.md` 와 `STYLE_GUIDE.md` 도 고치지 않는다(아직 정해진 게 없다 — 사람이 결과를 보고 정한다).
  **(2) `gen_character.py` 가 캔버스 크기를 인자로 받게 한다.** 지금은 `N = 17` 이 모듈 상수라 26px 을 구우려면 파일을 고쳐야 한다. **비율값들이 17px 에 맞춰 손으로 조율된 값이라 `N` 만 26 으로 바꾸면 안 된다** — 어디가 픽셀 절대값이고 어디가 비율인지 갈라내는 게 이 바퀴의 실제 일이다. 갈라낸 결과를 STATUS 에 적을 것(채택하면 그게 그대로 이사 계획이 된다).
  **(3) 26px 후보를 굽고 지금 것과 나란히 놓는다.** 비교 이미지를 `docs/design_reference/canvas_26_vs_17.png` 로 남긴다. **두 배율 다 보여줄 것**: ① 실제 게임에서 보이는 크기(지금 17×3=51px, 새것 26×2=52px — **화면 크기를 맞추려면 배율이 3배에서 2배로 내려간다**), ② 6배쯤 확대한 것. idle 4방향과 도끼 든 자세를 포함한다.
  **(4) 늘어난 칸으로 무엇을 할 수 있는지 실제로 보여줄 것** — 픽셀만 늘리고 같은 그림을 그리면 시험의 의미가 없다. 17px 에서 못 넣었던 것을 넣어본다: 표정(눈에 흰자·눈썹), 옷 주름이나 단추, 머리카락 결, 손가락 구분 등. **무엇을 넣었고 무엇은 26px 에서도 여전히 안 되는지**를 STATUS 에 적을 것.
  **(5) 채택했을 때 따라오는 비용을 STATUS 에 적는다** — 최소한 이것들이다: 지형 타일을 24px 로 다시 굽기(`gen_terrain.py`, **배율이 2배로 내려가므로 타일 아트도 16→24px 이어야 도트 크기가 맞는다** — STYLE_GUIDE 1번), 도구 7종 × 3모션 × 머리모양 4종 시트 전부 재생성, `qa_sprite_check.py` 의 SPECS 재조정, 충돌 상자·미리보기 배율. **몇 바퀴짜리인지 추정치를 적을 것.**
  **(6) 판단은 사람이 한다.** 이 바퀴는 "좋아 보인다/아니다"를 결론내지 말고 **비교 이미지와 사실만** 남긴다.

- [x] #45 (2026-09-07 완료 — **아무것도 채택하지 않았다. 판단 재료만 만들었다**: `docs/design_reference/canvas_32_vs_17.png` + `game/tools/try_canvas32.py`. 게임 자산 104장은 바이트 단위로 그대로다. 비용·바퀴 수는 `docs/STATUS.md`. **정해주실 것은 `#46 [ASK]`**) [DESIGN] **캐릭터 32px 을 이미지로만 시험한다 — 채택이 아니다.** 사람 요청(2026-09-07 저녁): 사람 그림이 제일 중요하니 **먼저 눈으로 보고** 정하겠다. **#44 와 똑같은 방식이다** — 게임 자산을 한 바이트도 바꾸지 않고 비교 이미지와 사실만 남긴다. 도구는 `game/tools/try_canvas.py` 와 `gen_character.py` 의 `canvas_scale(n)` 이 이미 있다(#44 가 만들었다).
  **(1) 게임 자산을 건드리지 말 것.** `game/assets/sprites/` 의 17px 시트를 덮어쓰지 않는다. 이 바퀴 뒤에도 게임은 지금 그대로 돌아야 하고, 자체 QA 로 확인할 것. **`DESIGN.md`/`STYLE_GUIDE.md` 도 고치지 않는다** — 이미 32px 로 적혀 있는데(사람이 비율 2:1 을 정했고 타일 16px 에서 32px 이 산술적으로 따라온다), **그림이 실제로 좋은지는 아직 안 정해졌다.** 그 판단이 이 바퀴의 목적이다.
  **(2) 왜 32px 인가 — 이미 정해진 부분**: `DESIGN.md` 「크기 표준」의 **캐릭터는 타일 두 칸**. 타일 아트가 16px 이고 배율 3배를 안 바꾸므로 캐릭터 아트는 32px 밖에 없다. **스타듀밸리와 같은 규격이다**(타일 16×16 / 캐릭터 16×32 — 2026-09-07 확인). 화면에서 캐릭터 96px, 타일 48px.
  **(3) 늘어난 칸을 실제로 써볼 것.** 픽셀만 늘리고 같은 그림을 그리면 **지금보다 나쁘다** — #44 가 26px 에서 실측했다(2×1 눈이 앞머리 아래 어두운 데 묻혀 눈이 사라졌다). 32px 에서 넣어볼 것: **흰자**, **옷깃 + 허리띠 둘 다**, **앞섶 단추**, **볼 홍조**, 그리고 **눈썹** — #44 가 "얼굴이 다섯 줄은 돼야 눈썹이 된다"고 했고 32px 이면 그 조건이 찬다. **되면 「째려보는 인상」이 안 되게** 할 것(STYLE_GUIDE 「자연스러움」이 금지). **손가락**도 되는지 볼 것(26px 에서는 손이 2px 이라 못 갈랐다).
  **(4) 비율은 유지한다.** 머리가 전체의 몇 %인지는 그대로 두고 **늘어난 칸을 디테일에 쓴다** — 머리를 더 크게 만드는 데 쓰지 않는다. 옛 17px 값을 두 배로 곱하지 말고 32px 격자에서 후보를 비교해 고를 것.
  **(5) 비교 이미지** `docs/design_reference/canvas_32_vs_17.png` — ① **지형 타일 위에 올린 실제 게임 크기**(지금 51px/타일 48px 과 새것 96px/타일 48px 을 나란히 — **이번 변경의 목적이 「캐릭터가 타일 두 칸」이므로 이게 가장 중요하다**), ② 6배 확대, ③ 얼굴 확대(무엇이 되고 무엇이 안 되는지). idle 4방향을 넣을 것. 도끼 든 자세도 넣으면 좋지만 필수는 아니다.
  **(6) 채택하면 따라오는 것을 STATUS 에 적는다** — 걷기 6프레임, 도구 7종, 아이콘·바닥·상자, 엔진 상수(`CELL` 17→32, 충돌 상자, 미리보기 배율), `qa_sprite_check.py` SPECS, 자체 QA 22벌. **몇 바퀴인지 추정할 것.** #44 가 26px 기준으로 이미 재놨으니 그 차이만 적으면 된다.
  **(7) 결론을 내지 말 것.** 사람이 이미지를 보고 정한다. 다음 항목으로 `[ASK]` 를 남기되 선택지에 **"그냥 17px 로 둔다"**(= 비율 2:1 을 포기한다)도 넣을 것.

- [ ] #46 [ASK] **캐릭터를 32px 로 옮길지 사람이 정해 주세요.** `docs/design_reference/canvas_32_vs_17.png` 를 보고 아래에서 하나 골라 이 줄을 고쳐 주세요(2026-09-07, INBOX #45 가 만든 판단 재료입니다. #45 는 **아무것도 채택하지 않았고** 게임 자산 104장은 그대로입니다).
  **보실 것**: 이미지 1번 띠가 이 결정의 전부입니다 — 진짜 지형 타일(48px) 위에 지금 것(51px = 1.06칸)과 32px(96px = **정확히 2.00칸**)을 나란히 세워 뒀습니다. 3번 띠는 얼굴에 무엇이 들어가는지(흰자 대신 **3×2 눈 + 하이라이트**, **눈썹**, 옷깃, 볼 홍조 2칸), 4번 띠는 32px 에서도 **안 되는 것**(손가락)입니다.
  **(가) 32px 로 옮긴다** — `DESIGN.md`·`STYLE_GUIDE.md` 가 이미 적어둔 대로가 됩니다(캐릭터는 타일 두 칸). 배율 3배와 타일 16px 은 안 바뀌므로 도트 크기 단위도 안 깨집니다. 대가는 **8~10바퀴** 동안 캐릭터·도구·상자가 섞인 채로 굴러가는 것입니다(지형 타일은 안 건드립니다 — 26px 안과 다른 점입니다).
  **(나) 그냥 17px 로 둔다 — 「캐릭터는 타일 두 칸」을 포기한다.** 자산 104장이 전부 그대로고 한 바퀴도 안 씁니다. 대신 **`DESIGN.md` 「크기 표준」과 `STYLE_GUIDE.md` 1번을 17px 로 되돌려 적는 바퀴가 한 번 필요합니다**(지금 두 문서는 32px 로 적혀 있어서, 안 고치면 다음 바퀴들이 문서와 어긋난 자산을 보게 됩니다). 그리고 벽 한 칸이 캐릭터를 통째로 가리는 문제는 **건축을 만드는 바퀴가 다시 만나게 됩니다**.
  **(다) 26px** — #44 가 만든 `canvas_26_vs_17.png` 쪽입니다. 화면 크기를 지금(51px)에 맞추려고 **배율을 3배 → 2배로 내려야** 해서 지형 타일까지 24px 로 다시 구워야 하고(7~9바퀴), 그러고도 타일 두 칸이 안 됩니다. **(가)가 나온 뒤로는 고를 이유가 없어 보입니다** — 남겨두는 것은 사람이 26px 그림을 더 좋아할 수 있어서입니다.
  (가)를 고르시면 **#45 의 STATUS 에 적힌 1~7 을 그 순서대로 쪼갠 항목**이 필요합니다 — 「그래픽 파이프라인」의 *"여러 방향 × 여러 모션을 한 항목에 다 넣지 않는다"* 그대로입니다.
