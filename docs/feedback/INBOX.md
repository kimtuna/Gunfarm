# INBOX — 작업 큐

형식: `- [ ] #<번호> [BUILD|DESIGN] <할 일 한 줄>` (완료하면 `- [x]`로 바꾸고 완료 날짜를 붙인다. 번호는 재사용하지 않는다.)

## 처리 대기

- [x] (2026-09-06) #1 [BUILD] 메인 메뉴 화면: 플레이 / 설정 / 나가기 세 버튼. "나가기"는 실제로 앱을 종료하고, "플레이"와 "설정"은 아직 빈 화면으로 이동만 한다(각각 #2, #3에서 채운다). 프로젝트 진입 씬을 이 메인 메뉴로 바꾼다.
- [x] (2026-09-06) #2 [BUILD] 캐릭터 슬롯 화면(코어키퍼 스타일 3개 슬롯). 빈 슬롯을 고르면 캐릭터 커스터마이징 화면(#4)으로, 캐릭터가 있는 슬롯을 고르면 월드 입장(#5)으로 넘어가는 자리만 만든다. 슬롯 데이터는 `user://` 저장/불러오기로 유지되고, 슬롯 삭제도 된다.
- [x] (2026-09-06) #3 [BUILD] 설정 화면의 해상도 옵션. **해상도를 바꿔도 카메라가 보여주는 월드 범위는 모든 해상도에서 동일해야 한다**(DESIGN.md "카메라 / 해상도" PvP 공정성 규칙). 서로 다른 해상도 2개 이상에서 실제로 같은 월드 범위가 보이는지 스크린샷으로 검증할 것.
- [x] (2026-09-06) #4 [BUILD] 캐릭터 커스터마이징 화면: 이름 입력 + 피부색/머리색/옷색(각각 고정 팔레트에서 선택) + 머리모양 3~4종 선택 + 확정. DESIGN.md "캐릭터 커스터마이징 항목" 참고. **이번 항목은 선택값을 슬롯에 저장하는 데이터/UI까지만** — 실제 캐릭터 그림은 아직 없으므로 미리보기는 색 견본(색 사각형 등)으로 대신하고, 그림이 생기면 별도 항목에서 붙인다.
- [ ] #5 [BUILD] 월드 생성 1단계 + 입장: `opensimplex` 시드 기반 절차적 지형(땅/바다)과 스폰 지점까지만. 같은 시드는 항상 같은 월드를 만들어야 한다. 자원 배치(나무/채광 포인트/낚시 스팟)는 그 자원을 만드는 바퀴에 붙이므로 **이번 범위가 아니다**. 정한 맵 크기·바다 비율 수치는 DESIGN.md "월드 생성"에 기록할 것.
