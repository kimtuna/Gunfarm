# CHARACTER — 캐릭터 그림과 모션을 만드는 법

> 이 문서는 **캐릭터 한 벌을 처음부터 끝까지 만드는 절차**다. 그림체 기준 전반은
> `STYLE_GUIDE.md`, 게임 설계는 `DESIGN.md`, 이 기계에서 무엇을 어떤 명령으로 돌리는지는
> 저장소 루트의 `CLAUDE.md` 에 있다.
>
> **2026-09-08 에 파이프라인이 통째로 바뀌었다.** 그 전에는 절차 생성
> (`gen_character.py` 가 수식으로 도트를 찍는 방식)이었고, AI 그림은 참고 자료로만
> 썼다. 지금은 **AI 그림 한 장을 부품으로 잘라 관절로 조립한다.** 왜 바꿨는지와
> 무엇을 값 치르고 알아냈는지가 이 문서의 절반이다 — 되돌리지 말 것.

---

## 0. 한눈에

```
ComfyUI (SDXL + 픽셀아트 LoRA + ControlNet OpenPose)
   │   뼈대 그림으로 자세·비율·방향을 지시한다
   ▼
1024px 시트 한 장 = 2×2 칸에 네 방향                ← 참고 자료가 아니라 **리그의 원본**이다
   │   docs/design_reference/farmer_sheet4.png 에 저장소 안으로 넣어둔다
   │   game/tools/rig.py : 배경 제거 → 외곽선 벗기기 → 10부품으로 자르기
   ▼
부품 레이어 (머리 · 몸통 · 위팔/아래팔 ×2 · 허벅지/정강이 ×2)
   │   관절 각도표(idle / walk / hold_<도구> / use_<도구>)
   ▼
1024px 에서 조립 (회전 · 크기 · 이동)      ← 색 옆에 **재질 번호**를 같이 옮긴다 (5-b)
   │   **맨 마지막에** 96px 로 줄이고 재질마다 기준색 램프로 갈아끼우고 외곽선을 두른다
   ▼
game/assets/sprites/player_<모션>_<스타일>.png   (행 = 방향, 열 = 프레임)
```

핵심 한 줄: **자르고 → 돌리고 → 마지막에 줄인다.** 도트는 마지막 한 번에서만
만들어지므로, 중간의 회전 각도가 아무리 어중간해도 결과는 깨끗한 도트다.

---

## 1. 크기 — 여기가 어긋나면 뭘 해도 지저분하다

| 것 | 아트 px | 씬 배율 | 화면 px |
|---|---|---|---|
| 캐릭터 한 칸 | **96** | 1 | 96 |
| 지형 타일 한 칸 | **48** | 1 | 48 |

- **캐릭터는 타일 두 칸**이다 (`DESIGN.md` 「아이템/오브젝트 크기 표준」).
- **도트 하나의 화면 크기가 캐릭터와 지형에서 같아야 한다** — 둘 다 배율 1이라 1px 이다.
- 배율은 **정수만** 쓴다. 64px 칸은 배율 1.5라 못 쓴다.
- 캐릭터 칸을 바꾸면 `player_frames.gd` 의 `scale_of()` 가 배율을 다시 계산하고,
  **지형도 같이 바꿔야 한다** (`terrain_tiles.gd` 의 `TILE_ART`/`SCALE`, `gen_terrain.py` 의 `TILE`).

### 왜 96인가 (2026-09-08, 사람 결정)

48px 로 줄여봤더니 **얼굴이 죽었다.** 머리가 캐릭터의 1/4이라 48px 에서 얼굴이 7px,
눈이 1px 이 된다. 1px 짜리 눈이 성립하려면 **손으로 그 1px 을 찍어야** 하지,
큰 그림을 평균 내어 얻을 수 있는 것이 아니다. 96px 이면 얼굴이 24px, 눈이 3px 이라 산다.

---

## 2. 그림 주문하기 (ComfyUI)

띄우는 법과 경로는 `CLAUDE.md` 에 있다. 워크플로는 API 로 `POST /prompt` 한다.

### 반드시 지킬 것

1. **그림자를 금지한다.** 프롬프트에 `no shadow, no cast shadow`, NEG 에
   `drop shadow, cast shadow, ground shadow, floor shadow, shadow under feet`.
   - 왜: AI 가 발밑에 그리는 바닥 그림자는 96px 로 줄일 때 **회색 옷과 색으로 구별되지
     않아 프로그램으로 뗄 수 없다.** 떼려다 회색 멜빵바지와 흰 셔츠가 같이 날아갔다.
     안 그리게 하는 것이 유일한 길이다.
2. **손을 명시한다.** `both hands visible, clear hands hanging at the sides` 와
   `arms straight down along the body` 를 **함께** 넣는다.
   - 왜: 팔을 내리라고만 하면 AI 가 손을 안 그리고 소매를 뭉갠다(실측). 손을 얻으려고
     팔을 벌려 뽑은 뒤 도트에서 팔을 내려봤는데, 위팔이 몸통에 묻혀 있어 아래팔만
     밀리고 어깨에 판자가 붙은 꼴이 됐다. **자세는 주문할 때 정한다 — 나중에 픽셀로
     고치지 않는다.**
3. **뼈대(ControlNet OpenPose)로 자세·비율·방향을 지시한다.** 프롬프트로는 안 된다.
   - **비율은 뼈대가 정한다.** "3 head tall" 이라고 써도 뼈대의 머리가 전체의 10%면
     5등신이 나온다. 뼈대의 머리를 h/3 로 그려야 3등신이 나온다.
   - **방향도 뼈대가 정한다.** 옆모습은 어깨·골반 폭을 접고 두 팔을 **같은 쪽**으로
     보내야 한다. 폭이 같으면 프롬프트에 `side view` 를 넣어도 정면을 그린다.
4. **네 방향을 한 캔버스에 받는다** — 방향마다 따로 뽑지 않는다(아래 3-b).
   그리고 **칸마다 무엇을 그릴지 자리를 집어 말한다**: 뒷모습은 뼈대로는 안 나오고
   `bottom left rear view …` 라고 써야 나온다(2026-09-08 실측).
5. **뽑은 뒤 그림자가 남아 있으면 그 장은 버린다.** 고치려 들지 않는다.

### 하지 말 것 — 값을 치르고 알아낸 것

- **모션 프레임을 AI 로 뽑지 않는다.** ControlNet 으로 걷기 6프레임을 뽑아 재봤더니
  프레임 사이 차이의 **28%가 머리에 있었다** — 셔츠 색과 그림자가 프레임마다 변했고
  정작 다리는 안 움직였다. 96px 에서 그건 걷기가 아니라 깜빡임이다.
  **AI 는 「자세 하나」만 만든다. 움직임은 리그가 만든다.**
- **큰 그림을 그냥 축소하지 않는다.** 축소는 도트가 아니라 「축소한 사진」이 된다
  (실측: 그냥 축소 593색 / 평평한 이웃 31%, 알파 가중 축소 + 팔레트 제한 15~32색).

---

## 3. 리그 (`game/tools/rig.py`)

### 부품

머리 · 몸통 · 위팔L/R · 아래팔L/R · 허벅지L/R · 정강이L/R — 10개.
아래팔에 손이, 정강이에 신발이 포함된다.

### 자르는 방법

**뼈대를 우리가 만들었으므로 관절 좌표를 이미 안다** — 그림에서 찾을 필요가 없다.
픽셀마다 가장 가까운 뼈에 준다. 단, **부품마다 반경이 있다.**

> **반경이 핵심이다.** 그냥 "가장 가까운 뼈"로 나누면 위팔이 가슴을 절반 먹는다
> (어깨 관절이 가슴 바로 옆이다) — 그 상태로 팔을 움직이면 몸통에 구멍이 난다.
> 값은 **원본의 자세**에 달렸다: 팔을 내린 자세에서는 뼈가 팔의 좌우 한가운데를
> 지나므로 반경 = 팔 두께의 절반이다. **반경은 픽셀이 아니라 키에 대한 비율로 적는다** —
> 아래 3-b 「반경은 픽셀이 아니라 키에 대한 비율이다」.

### 자르기 전에 외곽선을 벗긴다

**반드시.** 외곽선이 그려진 채로 자르면 소매를 따라가던 검은 선이 위팔에 딸려가고,
팔을 돌리는 순간 그 선이 어깨 한복판에 박힌다. 테두리는 **합친 뒤** 새 실루엣에
다시 두른다(`gen_player.add_ink`).

### 원본은 「팔 내린」 자세다 — T자가 아니다

T자로 해봤더니 idle 을 만들려고 팔을 90도 돌리는 순간 어깨가 망가졌다. 소매는
**가로로 뻗은 팔에 맞게** 그려진 곡선이라 세로로 세우면 어깨에 홈을 낸다.
팔을 내린 그림을 쓰면 **가장 많이 보이는 자세(idle · 정면 걷기)의 회전이 0도**가 되고,
옆모습 걷기도 ±10도 안쪽이다. 도구 자세처럼 팔을 크게 드는 것만 큰 회전을 쓴다.

---

## 3-b. 4방향을 한 장에 받는다 (2026-09-08, INBOX #56 — **이 절을 통째로 다시 썼다**)

**방향마다 그림을 따로 뽑지 않는다.** 한 캔버스(1024×1024)의 2×2 칸에 네 자세를 넣고
**한 번에** 뽑으면 정의상 같은 캐릭터다 — 머리색·옷 색·옷 모양·몸 비율이 어긋날 자리가
없다. `game/tools/comfy.py` 의 `SHEET_CELLS` 가 그 배치이고, `rig.load_sheet()` 가 같은
좌표로 칸을 읽는다. **두 곳이 어긋나면 엉뚱한 자리를 자른다.**

### 뼈대는 자세를 정하고, **어느 쪽을 보는가는 글이 정한다**

이게 이 절에서 가장 값비쌌던 발견이다.

- 프롬프트에 `front view, side view, back view, side view` 라고 **나열만** 하면
  **네 칸이 전부 얼굴**로 나온다. 씨앗 여섯 개(1111·2222·…)에 뼈대 세 가지 —
  ① 그대로 ② 뒷모습에서 코를 뺀 것 ③ 거기에 좌우(왼팔/오른팔 색)까지 바꾼 것 —
  을 다 시험해서 **한 장도 뒤를 안 봤다.**
- `top left front view …, bottom left rear view seen from behind showing only the
  back of the head and hair, faceless from behind, …` 처럼 **칸의 자리를 집어 말한**
  순간 세 장 중 세 장이 제대로 나왔다. `comfy.GRID` 가 그 글이다.
- **OpenPose 규약(뒤에서 본 사람은 코 없이 귀 둘)은 그대로 지킨다** — 맞는 표기라서
  지키는 것이지, 그것만으로는 뒷모습이 안 나온다.

### `reference sheet` 라고 하면 소품이 따라온다

그 말이 뒷모습을 얻는 열쇠인데, 대가로 SDXL 이 인물 옆에 **화분·항아리·얼굴
아이콘·지구본**을 같이 그린다. 두 겹으로 막는다:

1. `comfy.NEG` 에 소품 이름을 넣는다.
2. 그래도 남으면 **`rig._only_figure()` 가 칸마다 골반에 닿은 덩어리 하나만 남긴다.**
   소품 픽셀은 반경이 없는 머리·몸통이 통째로 가져가므로, 안 지우면 **걷기에서 항아리가
   머리를 따라 흔들린다.**

### 그래도 안 맞는 것

- **`right` 는 여전히 `left` 를 좌우반전해 쓴다.** 오른쪽 칸은 AI 가 뒤로 살짝 돌린
  3/4 를 자주 그려서 왼쪽 칸과 짝이 안 맞는다 — 반전이 공짜로 정확하다.
- **옆모습이 정면보다 말랐다**는 것은 한 장에 받아도 그대로다. 뼈대에서 어깨·골반을
  접는 것이 옆모습의 조건이라(안 접으면 정면을 그린다) 폭이 줄어드는 것을 못 막는다.

### 반경은 픽셀이 아니라 **키에 대한 비율**이다

`PARTS` 의 반경은 원래 34/30/78 **px** 이었는데, 그 값은 **키 680px 짜리 그림 한 장**
기준이었다. 한 장에 넷을 넣으면서 한 인물의 키가 420px(`comfy.FIG_H`)이 됐고, 같은
픽셀 값을 그대로 쓰면 반경이 몸에 비해 1.6배가 되어 **위팔이 가슴을 문다.** 지금은
비율로 적고 `split()` 이 키를 곱한다 — 인물 크기가 바뀌어도 따라온다.

---

## 4. 모션 — 관절 각도표

부품 변환은 `{"rot": 도, "scale": 배, "dy": px, "dx": px}` 다. 자식은 부모를 물려받는다.

### 방향마다 흔드는 축이 다르다

| 방향 | 무엇을 쓰나 | 왜 |
|---|---|---|
| down / up (정면·뒤) | **원근 축약** (`dy`, `scale`) | 정면으로 걸어오는 것은 팔다리가 **깊이 축**으로 흔들리는 것이다. 회전으로 그리면 팔벌려뛰기가 된다. |
| left / right (옆) | **회전** (`rot`) | 옆모습에서는 깊이 축이 곧 화면의 좌우다. |

### 큰 동작은 무릎과 팔꿈치가 한다

어깨와 허벅지만 움직이면 몸이 통나무로 보인다. 실제로 걷을 때 화면에서 제일 많이
움직이는 것은 **정강이와 아래팔**이다.

```
허벅지 15  <  무릎 36        어깨 6  <  팔꿈치 20      (정면, 1024px 기준)
엉덩이 8도 <  무릎 26도      어깨 7도 < 팔꿈치 22도    (옆)
```

- 굽힌 정강이는 카메라 쪽으로 접히므로 **짧아 보이게** 한다(`scale` 을 줄인다).
  안 줄이면 무릎이 안 굽고 다리가 통째로 뜬 것처럼 보인다.
- 무릎과 팔꿈치는 **한쪽으로만 접힌다**(사람 관절이 그렇다) — `max(0.0, ...)`.

### 딛는 발은 절대 안 움직인다 — 정면도 옆도

앞다리를 아래로 내리거나 두 다리를 다 돌리면 실루엣의 아랫줄이 프레임마다 바뀌고,
발을 맞추느라 **몸 전체가 밀려서 머리가 흔들린다**(실측: 정면 36%, 옆 14%).
**흔드는 쪽만 움직인다** — `max(0, s)` / `max(0, -s)`.

### 발 맞추기는 「밀기」가 아니라 「자르기」다

그래도 신발 끝이 몇 px 넘칠 수 있다. 프레임을 통째로 밀면 그 밀린 양(1024px 기준
몇 px)이 96px 로 줄일 때 **위쪽 한 줄의 커버리지를 뒤집어 머리가 1px 흔들린다**
(실측: 아랫줄 편차 5px 탓에 변화의 14%가 머리에 갔다). 제일 높은 아랫줄에 맞춰
그 아래를 **잘라낸다** — 없어지는 것은 흔드는 발의 신발 끝뿐이라 96px 에서 안 보인다.

### 도구 자세

`hold_<도구>` (1프레임) · `use_<도구>` (6프레임, 한 번만 재생) · `walk_<도구>` (6프레임).
**자세는 도구를 안 가린다** — 「든다 / 휘두른다」 두 동작이다.
`rig.TOOLS` 에 이름 한 줄을 늘리면 시트 3장이 같이 구워지고,
`player_frames.gd` 의 `TOOLS` 에도 같은 줄을 늘리면 게임이 자동으로 싣는다.

---

## 4-b. 손에 쥔 도구 (2026-09-08, INBOX #66)

**도구는 부품 하나다 — 아래팔의 자식**(`rig.TOOL_PART`). 손 좌표에 그림을 얹고 끝내면
팔이 도는 동안 도끼가 제자리에 뜬 채로 남는다. 사슬에 매달아두면 `compose()` 의
*"자식은 부모를 물려받는다"* 가 그대로 **자루가 팔과 따로 놀 수 없다**를 보장한다.
(`hand_point()` 는 이제 도구를 얹는 데 안 쓴다 — 손이 어디로 가는지 재보는 자리다.)

| 것 | 값 |
|---|---|
| 그림 | **`gen_character.py` 의 도구 7종을 그대로** 쓴다. 새로 안 그린다 |
| 크기 | 설계 단위 하나 = 칸에서 **4.4px**(`rig.TOOL_UNIT`) — 도끼 21 × 28 · 총 42 × 12 |
| 해상도 | **리그 공간(1024px)** 에서 굽는다 — 도트는 마지막 축소 한 번에서만 생긴다 |
| 쥐는 손 | 언제나 **화면 오른쪽 손**(`armR`). 방향이 바뀌어도 같은 쪽이라야 덜 어지럽다 |
| 앞뒤 | `down`/`left`/`right` 는 몸 앞, **`up` 은 몸 뒤**(`rig.TOOL_BEHIND`) |

### 되돌리지 말 것

- **96px 도트를 만들어 손에 붙이지 않는다.** 팔 각도만큼 돌리는 순간 뭉갠다(3절과 같은
  이유). 리그 해상도로 구워 **1024px 에서 얹고** 마지막에 한 번 줄인다.
- **도구를 넣고 테두리를 재지 않는다**(`bake_rows`). 도끼를 들었다는 이유로 테두리가
  높아지면 **그 시트에서만 캐릭터가 작아진다** — `DESIGN.md` 「캐릭터 애니메이션」이
  금지한 것이다. 세로는 몸으로 재고, 가로만 **선 축을 두고 좌우 같은 만큼** 넓힌다
  (한쪽만 넓히면 칸 안에서 캐릭터가 옆으로 밀려 발밑이 노드 원점을 벗어난다).
- **그 축은 테두리의 한가운데가 아니다** (2026-09-08, INBOX #71). 테두리에는 **뻗은
  팔이 들어간다** — 도구를 든 자세는 오른팔이 옆으로 나가므로 축이 그만큼 오른쪽으로
  밀리고, 몸통·다리는 칸 안에서 왼쪽으로 밀린다. 실측으로 **정면 -5.5 ~ -7.5px,
  뒷모습 -5.0 ~ -7.0px** 였고(옆모습은 팔이 앞뒤로 겹쳐 테두리가 안 넓어져 멀쩡했다),
  화면에서는 **빈손 ↔ 도구를 오갈 때 캐릭터가 6px 순간이동**하는 것으로 보였다.
  축은 **쉼 자세(변환 없음, 도구 없음)의 한가운데**로 잡는다 — 모션과 무관하게
  방향마다 한 값이라 22장이 저절로 같은 자리에 선다. 관절(`J['hipC']`)을 쓰지 않는
  이유는 그림이 뼈대에 정확히 얹혀 있지 않아서이고, 쉼 자세로 재면 **이미 맞아 있던
  idle 이 한 픽셀도 안 움직인다**(idle 이 곧 쉼 자세다). 고친 뒤 실측은 정면·뒷모습
  **±0.5px**, 옆모습은 시트끼리의 벌어짐이 2.1 → **1.7px** 로 함께 줄었다.
- **`hold` 는 팔꿈치를 접는다**(`HOLD_ELBOW` 34도). 팔을 곧게 내린 채로는 자루가
  팔뚝과 **같은 선 위에 서서** 팔을 통째로 덮는다 — 손이 안 보이니 「쥐고 있다」가 아니라
  「팔에 판자를 붙였다」로 읽힌다. 접으면 팔과 자루가 손에서 갈라지는 **V 자**가 되고
  그 갈라짐이 곧 손이다. (옛 17px 생성기가 손만 한 칸 밀면 됐던 것은 거기서 손이
  1px 짜리 점이었기 때문이다 — `STYLE_GUIDE.md` 「도구를 든 손은 몸에서 한 칸 바깥으로」.)
  **정면에서도 회전을 쓰는 유일한 자리다** — 이건 깊이 축의 흔들림이 아니라 화면 안에서
  실제로 팔을 벌리고 접는 것이라 「팔벌려뛰기」 금지와 부딪히지 않는다.
- **크게 도는 것은 어깨가 아니라 도구다.** 옆에서 본 도끼질에서 화면을 가로지르는 것은
  **날이 그리는 호**다 — 어깨로 그리려면 회전이 120도를 넘어야 하고 그 각도에서는 소매가
  어깨에서 떨어져 나간다. 어깨는 조금(26도), 도구는 크게(104도) 돌린다.
- **휘두르기는 `hold` 에서 시작해 한쪽으로만 간다**(`_strike`). `hold` 를 가운데 두고
  왕복하면 위로 더 들 자리가 없고(칸 위 테두리), `hold` → `use` 로 넘어가는 순간 자세가
  튄다. 총의 되튐과 같은 모양이다.
- **「쓴다」만은 도구마다 다르다**(`rig.USE_STYLE`) — 자세와 갈리는 자리다.
  총은 **돌지 않고 뒤로 밀렸다 돌아오고**(돌리면 총구가 땅을 본다), 물뿌리개는
  기울이기만 한다. 근거는 `DESIGN.md` 「캐릭터 애니메이션」의 INBOX #28 · #30 항목이다.
- **`walk_<도구>` 는 걷기의 그 팔을 통째로 버린다**(`walk_tool`). 키마다 덮으면
  `hold` 에 없는 키(`dy`)만 살아남아 도구가 걸음마다 위아래로 흔들리고, 도구는 머리
  옆·위까지 올라오는 물건이라 그 흔들림이 그대로 「머리가 움직인다」가 된다(실측 낫 18.8%).
- **옆모습은 자루를 더 눕힌다**(`TOOL_LEAN` -16도). 팔을 앞으로 들어 손이 얼굴 앞에
  오는데 거기서 자루를 세우면 **자루가 얼굴을 가로지른다.**
- **좌우를 뒤집으면 회전의 부호도 뒤집힌다**(`tool_draw_angle` 의 `-sx*arm`). 그림은
  거울에 비추는데 팔 회전은 거울과 무관하게 캔버스에서 돈다 — 부호를 안 뒤집으면
  옆모습에서 자루가 **가슴을 가로지르는 가로 막대**가 된다(실제로 그렇게 나왔다).

### 걸음표는 여섯 값이 모두 달라야 한다

```python
STRIDE = [1.0, 0.55, -0.15, -1.0, -0.55, 0.15]
```

사인으로 만들면 `sin(60°) = sin(120°)` 이라 두 쌍이 **같은 그림**이 되고, 앞뒤로
0 을 두 번 넣어도 그 두 장이 같아진다. **이 실수를 하루에 세 번 했다.**

---

## 5. 굽기 (`bake_rows`)

**시트 한 장의 모든 칸이 같은 테두리와 같은 팔레트를 쓴다.**

- **같은 테두리(box)**: 프레임마다 제 몸에 맞춰 자르면 축소 배율이 달라져 캐릭터가
  들썩이고, 방향마다 크기가 달라진다.
- **같은 팔레트**: 프레임마다 색을 따로 줄이면 팔레트가 미세하게 달라져 **모든 픽셀이
  조금씩 변한다** — 그건 걷기가 아니라 깜빡임이다(실측 56%).

줄이는 것은 `gen_player.downscale()` 의 **알파 가중 평균**이다 — 배경을 먼저 떼고
캐릭터 픽셀만 평균 낸다. 배경째 축소기에 넣으면 테두리에 회색 후광이 낀다.

---

## 5-b. 재질 나누기 — 커스터마이징이 걸리게 (2026-09-08, INBOX #68)

**AI 그림에는 램프가 없다.** 색이 그림에 구워져 있어서 「이 픽셀이 피부인가 옷인가」를
색만 보고는 모르고, 그래서 피부색·머리색·옷색을 골라도 아무것도 안 바뀌었다.

리그는 그 답을 이미 갖고 있다 — **부품을 우리가 나눴으므로 어느 픽셀이 머리·몸통·팔·
다리인지 안다.** 거기서 재질을 갈라 굽기 직전에 **기준색 램프**로 갈아끼운다. 그래서
디스크의 PNG 는 문서가 줄곧 말해온 그대로 **기준색 1벌**(밝은 피부 / 검정 머리 /
풀색 옷)이 되고, `character_sprite.gd` 의 색 바꿔치기가 **한 줄도 안 고친 채** 되살아난다.

| 것 | 값 |
|---|---|
| 재질 | 피부 · 머리 · 셔츠 · 바지 · 신발 · **눈** · 자루 · 날 (`rig.MATS`) |
| 갈리는 근거 | **부품**(어디에 났나) + **대표색**(무슨 색인가) 둘 다 — `rig.MAT_PARTS` / `rig.SEEDS` |
| 씨앗을 뽑는 곳 | **정면 한 장뿐** (`material_protos(rigs, ref="down")`) |
| 갈아끼우는 색 | `gen_character.palette()` 의 기본 인자 — `character_palettes.gd` 의 BASE 와 **같은 함수** |
| 단 수 | 재질마다 **딱 4단계** (눈은 잉크 + 반짝임) |

### 되돌리지 말 것

- **색을 먼저 갈고 옮기지 않는다 — 라벨을 같이 옮긴다.** 1024px 에서 램프 색으로
  칠해두면 마지막 축소가 이웃을 평균 내어 **램프에 없는 중간색**을 만든다. 색 바꿔치기는
  색이 **정확히** 같아야 걸리므로 그 픽셀은 영영 옷을 안 갈아입는다. 그래서 재질 번호를
  R 채널에 적은 레이어를 `compose()` 로 **같은 변환**에 태우고, 축소도 같은 격자에서
  **최빈값**으로 한다(`gen_player.downscale_labels`, 격자는 `_grid()` 하나가 정한다).
- **22장을 한 번에 줄인다.** 시트마다 따로 줄이면 팔레트가 미세하게 달라지는데, 그러면
  걷기 시트만 색이 안 바뀐다. 재질별 램프는 상수라 22장이 저절로 같은 색을 쓴다 —
  덤으로 「정면 팔레트를 네 방향에 씌운다」(옛 `quantize_all(ref_cells=...)`)가 필요
  없어졌다. 방향끼리 옷 색이 어긋날 자리가 아예 사라진다.
- **씨앗은 정면에서만 뽑는다.** 옆모습은 어깨를 덮은 머리가 소매 자리에 있고 뒷모습에는
  얼굴이 없다 — 방향마다 뽑으면 씨앗이 통째로 빨강이 된다(실측). 인물이 한 명이므로
  정면에서 뽑은 대표색이 세 방향에 그대로 맞는다.
- **부품 제한이 절반을 한다.** 색만으로 가르면 어두운 빨강 머리와 어두운 갈색 신발이
  섞인다. 「머리」가 몸통·위팔까지 도는 것은 긴 머리가 어깨에 내려오기 때문이고,
  「신발」이 몸통에 있는 것은 **멜빵의 가죽끈**이 신발과 같은 가죽이기 때문이다.
- **셔츠는 머리 부품에도 난다.** 머리 뼈가 목에서 끝나므로 **깃**이 머리 부품에 들어온다.
  안 열어두면 깃이 「피부도 머리도 아닌 것」이 되어 눈으로 칠해진다 — 실제로 턱 밑에
  검은 띠가 생겼다.
- **눈은 겨루지 않고 덮어쓴다.** 대표색으로 겨루게 두면 흰 반짝임이 흰 셔츠와, 청록
  눈동자가 멜빵과 붙는다. 코 관절 둘레 띠에서 **피부에서도 머리에서도 먼 것**이 눈이다
  (`rig._eye_mask`). 뒷모습은 따로 끌 필요가 없다 — 거기는 띠 안이 전부 머리라
  「머리에서 멀다」가 성립하지 않아 저절로 아무것도 안 잡힌다.
  **눈을 안 갈라두면 눈이 머리색을 따라간다** — 금발을 고르면 눈이 사라진다.
- **자루·날은 재질이되 램프가 고정이다**(`DESIGN.md` 「캐릭터 애니메이션」의
  *"도구 색은 커스터마이징을 따라가지 않는다"*). 도구 픽셀은 몸 재질과 겨루지 않고
  자루·날 둘 중 가까운 쪽으로만 갈린다(`rig.classify_tool`).

---

## 5-c. 레이어로 나눈다 — 페이퍼 돌 (2026-09-10 사람 결정, 아직 코드에 없다)

**지금은 몸·옷·도구가 한 장으로 합쳐져 구워진다.** 색은 실행 중에 바꿀 수 있지만
(`5-b`), **모양이 다른 옷**(긴팔·코트·갑옷)은 못 넣는다. 시트를 나누면 조합이
곱해진다 — 지금 22장 × 머리모양 4종 = 88장인데, 옷 5벌을 시트로 나누면 440장이다.

### 이 방식에는 이름이 있다 — **페이퍼 돌(paper doll)**

같은 장르(농사 게임)에 사실상의 표준 자산이 있고, 2026-09-10 에 실제로 어떻게
만드는지 찾아봤다 — [Mana Seed "Farmer Sprite System"](https://seliel-the-shaper.itch.io/farmer-base).

| | 그쪽 |
|---|---|
| 레이어 수 | *"제대로 쓰려면 **페이퍼 돌 레이어 20장**까지 필요할 수 있다"* |
| 머리모양 | 17종 |
| 옷 | 상의 · 하의 · 액세서리 · 신발을 각각 따로 |
| 캐릭터 크기 | 32px, **64×64 칸 안에 중앙 정렬** |
| **철칙** | *"모든 머리·옷 요소가 **자기 시트에, 몸 스프라이트와 똑같이 배치**된다."* |

**철칙이 전부다.** 옷 시트를 열면 몸 시트와 프레임 자리가 픽셀 단위로 같다. 그래야
그냥 겹치면 맞는다 — 별도 정렬 데이터가 없다. **우리 22장은 이미 같은 격자·같은
테두리로 굽고 있으므로**(`5. 굽기`) 이 철칙은 저절로 지켜진다.

**한 가지는 우리가 낫다.** 그쪽은 도구를 *"별도 시트에서 붙이고 직접 위치를 맞춘다."*
우리는 `tool` 이 `armR_lower` 의 자식이라 **팔이 돌면 따라 돈다**(`4-b`).

### 바꿀 곳은 두 군데뿐이다

부품 10개와 **재질 라벨은 이미 픽셀을 따라다닌다**(`5-b`). 합쳐지는 것은 마지막
두 단계다:

```
부품 10개 + 재질 라벨          ← 이미 분리돼 있다
   ↓ bake_rows()               ← 한 장으로 합친다        ★ 재질 마스크를 걸어 레이어별로
   ↓ add_ink()                 ← 합쳐진 실루엣에 잉크    ★ 옷은 레이어마다
   → player_walk_farmer.png
```

**출발점은 여섯 장**이다 — 재질이 다섯(`skin`/`hair`/`shirt`/`pants`/`boot`)에 도구 하나:

```
① 몸   ② 머리   ③ 상의   ④ 하의   ⑤ 신발   ⑥ 도구
```

Mana Seed 의 20장은 액세서리·양말·효과까지 나눈 것이다. **필요해질 때 늘린다.**

### 외곽선 — 옷과 도구를 다르게 다룬다

**표준 답은 「레이어마다 자기 외곽선을 그려 넣는 것」이고, 대신 순검정을 안 쓴다**
([Lospec, 색을 쓴 외곽선](https://lospec.com/articles/pixel-art-outlines-part-2-using-color/)).
`STYLE_GUIDE` 가 이미 그렇게 돼 있다 — 잉크 `#261C2C`, *"순검정은 화면에서 툭
튀어나온다."*

| | 잉크를 언제 두르나 | 왜 |
|---|---|---|
| **옷 레이어** | **레이어마다** | 소매 끝·옷깃이 **보여야** 한다. 경계선이 곧 옷이다 |
| **도구** | **합친 뒤 한 번** (지금 그대로) | 도구는 몸 **앞을 지나간다** — 미리 두르면 스티커가 된다(`4-b`) |

### 새 옷의 그림은 어디서 오나 — 아직 안 정했다

**분리해도 새 옷은 안 생긴다.** 밑그림(`farmer_sheet4.png`)에는 반팔 셔츠 한 벌만
그려져 있고, 재질 분류는 **거기 있는 픽셀을 떼어낼 수 있을 뿐 없는 긴팔을 만들지
못한다.** 두 길이 있다:

- **(가) 옷마다 밑그림을 받는다** — ComfyUI 로 같은 캐릭터에 다른 옷. 대가는 **일관성**:
  `farmer_sheet4.png` 의 네 번째 칸만 포니테일이 된 것이 그 증거다(3-b).
- **(나) 옷을 절차 생성으로 그린다** — 뼈대가 있으니 몸통·팔 뼈를 따라 코드로 그린다.
  도구가 이미 그렇게 돈다. 소매 길이는 숫자 하나다.

**이 결정은 실제로 옷을 만들 때 한다.** 다만 레이어 구조는 그 전에 넣어야 한다 —
안 넣고 구우면 나중에 22장을 다시 굽는다.

---

## 6. 합격 기준 — 기계가 잰다

```
.venv/bin/python game/qa/qa_character_sheets.py     # 시트 자체
/Applications/Godot.app/Contents/MacOS/Godot --path game --script qa/qa_player_world.gd   # 게임 안
```

`qa_character_sheets.py` 가 보는 것:

| 검사 | 기준 | 왜 |
|---|---|---|
| 규격 | 칸 96px, 행 4개 | 어긋나면 시트가 조각난다. |
| 알파 | 반투명 픽셀 없음 | 도트는 알파 0 아니면 255다. |
| 발밑 | 한 방향 안의 모든 프레임에서 아랫줄이 같음 | 다르면 걸으며 들썩인다. |
| 잘림 | 칸 테두리에 닿은 **잉크 아닌 픽셀 0개** | 넘으면 그 자리에서 외곽선이 끊겨 도구가 잘린 것으로 보인다. 낫처럼 위로 넓은 날, 총처럼 가로로 긴 도구가 걸리는 자리다(2026-09-08, INBOX #66). **잉크색은 상수로 안 적는다** — 실루엣 테두리에서 가장 많이 쓰인 색을 잉크로 삼는다(아래 8절). |
| 머리 | 변화의 **8% 이하** | 걷기에서 머리는 움직이면 안 된다. 넘으면 몸이 통째로 밀리고 있다. **머리는 「목」에서 자르되 그 목은 맨손 idle 에서 잰다**(2026-09-08, INBOX #66) — 시트마다 제 첫 프레임에서 재면 낫·낚싯대의 날이 `top` 을 끌어올리고 자루가 폭 곡선의 골짜기를 지워서, 띠가 어깨·팔까지 삼켜 **팔만 흔들어도 걸린다**(실측 17.0%). 머리가 어디까지인지는 사람의 성질이지 무엇을 들었느냐가 아니고, 모든 시트가 같은 테두리로 구워지므로 한 번 재면 22장에 그대로 쓴다. `use_`/`hold_` 는 팔이 머리 위로 가는 것이 그 모션의 내용이라 이 검사를 건너뛴다. |
| 이어짐 | **0.02 ~ 0.45** | 작으면 죽은 프레임, 크면 순간이동이다. |
| 고르게 | 최대÷최소 **≤ 3.5** | 몇 장이 붙어 있고 남은 자리에서 건너뛰는 것을 잡는다. |
| 색맞음 | 방향 합집합 팔레트 ≤ 한 방향 최대의 1.35배 | 갈리면 방향을 바꿀 때 옷 색이 바뀐다. |

`qa_player_world.gd` 는 **게임 안에서** 발밑이 노드 원점에 오는지(0.0px), 걸을 때
`walk_<방향>` 이 실제로 재생되는지를 본다. **`--headless` 를 붙이면 캡처가 안 된다**
(`docs/GOTCHAS.md`).

**`qa_sprite_check.py` 는 캐릭터 시트를 안 본다** — 그쪽 스펙은 절차 생성기의
팔레트를 원본으로 삼는데 캐릭터가 거기서 나오지 않는다. 캐릭터는 위 스크립트가 본다.
2026-09-08 (INBOX #58) 에 그 스크립트에서 **캐릭터 22장을 걷어냈다** — 그전에는
"등록된 스펙이 없다"로 매번 22줄이 불합격이었다.

- **두 스크립트가 스프라이트 폴더를 이름으로 나눠 갖는다.** `qa_sprite_check.py` 는
  폴더를 훑다가 **`qa_character_sheets.sheet_paths()` 가 실제로 여는 파일만**
  건너뛰고, 건너뛴 장수를 찍는다. **패턴을 양쪽에 따로 적지 않는다** — 저쪽 패턴
  (`player_*_farmer.png`)에 안 걸리는 시트가 생기면(머리모양이 늘어
  `player_idle_ponytail.png` 등) **양쪽에서 동시에 빠져 아무도 안 보는 PNG** 가 된다.
  지금은 그런 파일이 `qa_sprite_check.py` 에서 불합격으로 잡히고, *"저쪽이 이 이름을
  못 잡는다"* 고 어디를 고칠지까지 찍는다(실제로 넣어보고 확인했다).
- **아이템 아이콘·바닥 그림·상자는 그대로 `qa_sprite_check.py` 가 본다** — 그것들은
  여전히 `gen_character.py` / `gen_box.py` 가 굽기 때문이다(아래 8절).
- **지형 타일 칸 크기도 생성기에서 읽는다**(`gen_terrain.TILE`). 같은 날 같은 이유로
  깨져 있었다 — 아트가 16 → 48px 이 되는 동안 검사에만 `16` 이 박혀 있어서
  **그림은 멀쩡한데** 지형 시트가 첫 줄에서 통째로 불합격이었다.

**`--headless` 를 붙이면 캡처가 안 된다** (`docs/GOTCHAS.md`).
**PNG 를 새로 저장했으면 `--import` 전에 `game/.godot/imported/player_*.png-*` 를 지운다** —
안 지우면 옛 텍스처가 그대로 로드된다.

---

## 7. 캐릭터 한 벌을 새로 만드는 절차

1. **뼈대 한 장**을 만든다 — 2×2 칸에 정면/옆/뒤/옆, 팔은 내린 자세.
   `.venv/bin/python game/tools/comfy.py` 가 `~/ComfyUI/input/pose_sheet4.png` 에 넣는다.
2. ComfyUI 에 **씨앗만 바꿔가며 여러 장** 주문한다 (`comfy.py order`).
   그림자 금지 · 손 명시 · **칸의 자리를 집어 말하는 `GRID`** 가 전부 들어가야 한다.
3. 뽑은 것들을 나란히 놓고 고른다 — **그림자가 있는 장 / 뒷모습에 얼굴이 있는 장 /
   손이 손목에서 끊긴 장은 버린다.** 고친 뒤 쓰려 들지 않는다.
4. 고른 시트를 **`docs/design_reference/farmer_sheet4.png` 로 저장소에 넣고**
   `rig.py` 의 `SHEET` 가 그것을 가리키게 한다. `~/ComfyUI/output/` 만 가리키면
   그 폴더를 비우는 순간 캐릭터를 다시 못 굽는다.
5. `.venv/bin/python game/tools/rig.py` 로 부품 나눔을 눈으로 확인한다
   (`~/Desktop/rig_parts.png`). 팔이 가슴을 물었으면 반경을 줄인다.
6. `.venv/bin/python -c "import sys;sys.path.insert(0,'game/tools');import rig;rig.build_sheets()"`
   — idle · walk · 도구 7종 × 3모션 = **22장이 10초 안에** 나온다.
7. `game/.godot/imported/player_*` 를 지우고 `--import`.
   **안 지우면 옛 텍스처가 그대로 로드된다** (`docs/GOTCHAS.md`).
8. `qa_character_sheets.py` 와 `qa_player_world.gd` 를 돌린다. 둘 다 종료 코드 0 이어야 한다.

---

## 8. 아직 안 된 것

- ~~커스터마이징(피부·머리색·옷색)이 동작하지 않는다~~ → **2026-09-08 (INBOX #68) 에
  되살아났다** (위 5-b 절). 부품 + 대표색으로 재질을 갈라 굽기 직전에 기준색 램프로
  갈아끼우므로, 시트가 다시 **기준색 1벌**이 되고 게임 쪽 색 바꿔치기가 그대로 걸린다.
  건너뛰던 검사 셋(`qa_character_sprite` 의 3~6번, `qa_player_appearance` 의 「기준색이
  안 남았는가」)도 같이 살아났다.
  - **기본 외형이 그림과 다르게 보이는 것은 고장이 아니다.** 리그 원본
    (`farmer_sheet4.png`)은 빨강 머리 · 청록 멜빵이지만 기본 선택지는 검정 머리 ·
    풀색 옷이라(`DESIGN.md` 「캐릭터 커스터마이징 항목」), 게임에 뜨는 것은 언제나
    **고른 색으로 칠한 것**이다. 원본에 가까운 조합은 「적갈색 + 주홍색」쯤이다.
- 머리모양은 한 벌(`farmer`)뿐이다.
- **자체 QA 다섯 벌은 지금 있는 것만 보게 좁혔다** (2026-09-08, INBOX #57 — 그전에는
  `player_idle_<머리모양>.png` 4종과 램프 표를 찾다가 통째로 불합격이었다).
  `qa_player_sprite` `qa_player_walk` `qa_character_sprite` `qa_player_appearance`
  `qa_character_customize` — 전부 **머리모양 이름과 칸 크기를 박아두지 않고** 화면이
  내놓는 목록(`character_appearance.gd`)과 시트에서 읽는다. **지운 검사는 없다**:
  - 「머리모양이 3~4종인가」 → **「내놓는 머리모양마다 시트가 있는가」**. 시트 없는
    선택지를 늘리면 그 자리에서 걸린다(실제로 넣어보고 확인했다).
  - 팔레트 교체 검사는 **기준색 램프가 시트에 실제로 있을 때만** 돈다. **2026-09-08
    (INBOX #68) 에 램프가 생기면서 실제로 되살아났다**(위 5-b 절) — 그전에는 한 픽셀도
    없어 건너뛰며 이유를 찍었다.
  - 「고른 머리모양이 기본과 다른가」는 머리모양이 **두 벌 이상일 때만** 묻는다.
- **잉크색을 QA 에 상수로 적지 않는다.** 팔레트가 ComfyUI 그림에서 나오므로 색을
  적어두면 그림을 바꾼 바퀴가 반드시 잊는다. 대신 **실루엣 테두리가 한 색인지**를
  재고 그 색을 잉크로 삼는다(22장 전부 `#1a0f18` 한 색이었다). `STYLE_GUIDE.md` 의
  「외곽선은 순검정을 쓰지 않는다」는 그 테두리 색에만 적용한다 — 눈동자처럼 **안쪽**의
  한두 픽셀은 그 규칙의 대상이 아니다.
- ~~아이템 아이콘과 바닥 그림은 아직 옛 17px/12px 이라 캐릭터와 도트 크기가 안 맞는다~~
  → **2026-09-08 (INBOX #67) 에 맞췄다.** 아이콘 51px · 바닥 36px · 상자 48 × 42px 이고
  **셋 다 배율 1**이라 도트 하나가 캐릭터·지형과 같은 화면 1px 이다(화면 크기는 안
  바뀌었다). 그것들은 **여전히 `gen_character.py`/`gen_box.py` 가 굽고
  `qa_sprite_check.py` 가 본다** — 절차 생성기가 통째로 죽은 것이 아니라 **캐릭터만**
  거기서 떠난 것이다. 도구 형태의 원본이 거기라, 손에 든 도구(4-b)와 아이콘·바닥
  그림이 **같은 `ICONS` 설정에서 나온다.**
  - **`gen_character.py` 를 `python gen_character.py` 로 돌리지 말 것.** 그 `__main__`
    은 아직 절차 생성기 시절 그대로라 `player_<모션>_<머리모양>.png` 를 머리모양 4종에
    대해 굽는다 — 지금 시트는 리그가 굽는 `player_*_farmer.png` 라, 돌리면 **아무도
    안 보는 PNG 88장**이 생긴다. 아이콘만 다시 구울 때는 `tool_icon()`/`ground_icon()`
    을 직접 부른다.
- ~~도구가 손에 안 들려 있다~~ → **2026-09-08 (INBOX #66) 에 들렸다** (위 4-b 절).
  다만 **총은 한 손으로 든다** — 리그의 자세는 도구를 안 가리는데(4절) 옛 17px
  생성기는 총만 두 손으로 잡았다(`DESIGN.md` 「캐릭터 애니메이션」 INBOX #28).
  받치는 손을 붙이려면 자세가 도구를 알아야 하므로, **그게 필요한지는 사람이 정한다.**
- 방향끼리 **몸 비율**이 다르다(옆모습이 더 말랐다). 색과 옷 모양은 맞췄지만 비율은 못 맞췄다.
- **가로로 넓은 도구를 들면 사람이 8px 작아진다** (2026-09-08, INBOX #71 을 고치며
  실측 · INBOX #72 로 넘겼다). 칸에서 사람 키(`gen_player._grid` 의 `th`)가 보통 94px
  인데 **총은 86px, `use_sickle` 은 87px** 이다 — 상자가 세로보다 넓어지면 `_grid` 가
  가로 상한(`cell - 2*pad`)에 맞춰 **세로를 깎기** 때문이고, 그러면 `DESIGN.md`
  「캐릭터 애니메이션」의 *"프레임끼리 캐릭터가 차지하는 크기가 달라지면 안 된다"*
  가 깨진다. **#71 이 만든 것이 아니다** — 총은 그전에도 85~86px 이었고, 축을 옮기면서
  낫 하나가 더 걸렸을 뿐이다. 뿌리는 **96 × 96 정사각 칸에 가로로 긴 도구가 안
  들어가는 것**이라, 칸을 바꿀지 도구를 줄일지는 그 항목에서 정한다.
- ~~`qa_sprite_check.py` 의 `SPECS` 가 아직 지워진 절차 생성기를 가리킨다~~ →
  **2026-09-08 (INBOX #58) 에 걷어냈다** (위 6절). 캐릭터 스펙 함수들(`_idle_spec` /
  `_walk_spec` / `_tool_spec`)은 실측값이라 코드에 남겨뒀지만 **아무 시트에도 등록돼
  있지 않다** — 되살릴 때 그 숫자를 그대로 쓰지 말 것(17px/32px 칸 전제다).
- ~~지형 검사의 「캐릭터대비」는 아직 옛 셔츠 색을 본다~~ → **2026-09-08 (INBOX #59)
  에 실제 시트를 보게 고쳤다.** `qa_sprite_check.py` 의 `_character_fill()` 이
  `sheet_paths()` 가 주는 시트 22장을 열어 **외곽선을 뺀 몸 픽셀의 명도**를 모으고,
  그중 **풀과 35 이상 갈리는 넓이가 몸의 40% 이상인가**를 본다(지금 60%).
  - **몸 평균 명도로 재지 않는다** — 잉크(19)와 하이라이트(251)를 같이 평균 내는
    값이라 94.5 가 나오고, 풀(101)과 6.5 밖에 안 떨어져 **멀쩡한 그림이 불합격**한다.
    중앙값도 아니다(그러면 캐릭터의 *전부* 가 갈리라는 요구가 된다 — 옛 검사가 물은
    것은 셔츠 **한 벌**이었다). **넓이로 재는 것이 그 물음의 일반화다.**
  - **잉크색은 상수로 적지 않는다**(위 「잉크색을 QA 에 상수로 적지 않는다」와 같은
    자리) — 실루엣 테두리에서 가장 많이 쓰인 색으로 찾는다.
  - 이 검사가 불합격하면 **합격선을 늦추지 말 것.** 그건 풀색과 옷색이 실제로
    가깝다는 그림 문제라, 지형 색을 바꿀지 옷 색을 바꿀지는 사람이 정한다.

---

## `DESIGN.md` 에서 옮겨온 것 (2026-09-08)

> DESIGN.md 가 매 바퀴 읽는 파일이라 세부를 여기로 옮겼다. 거기에는 제목과
> 한 줄 요약이 남아 있다.

## 캐릭터 애니메이션 (중요 규칙 — 절대 되돌리지 말 것)

- **캐릭터는 정지 이미지 한 장이 아니라, 방향별로 여러 프레임(그림 여러 장)을 빠르게
  갈아끼우는 방식(2D 게임의 표준적인 "스프라이트 시트" 애니메이션 — 뼈대/스켈레톤
  애니메이션이 아니다)으로 움직인다.** Godot의 `AnimatedSprite2D`(`SpriteFrames`)를 쓴다.
  최소한 방향별 idle(1프레임 이상)과 walk(4~6프레임) 애니메이션이 있어야 한다.
- **서로 다른 생성 호출로 만든 프레임끼리 캐릭터가 차지하는 크기(픽셀 비율)가 달라지면
  안 된다** — 프레임을 만들고 나면 반드시 같은 세트 안에서 캐릭터 크기를 직접 비교해서
  맞출 것.
- **애니메이션 프레임 사이의 움직임이 자연스럽게 이어져야 한다.** 걷기 프레임을 순서대로
  넘겨봤을 때 다리/팔이 매끄럽게 움직이는 것처럼 보여야지, 중간에 자세나 크기가 뚝뚝
  끊겨 보이면 안 된다. idle에서 사용(발사/채광 등) 모션으로 바뀔 때도 자세가 갑자기
  다른 캐릭터처럼 변하지 말고 자연스럽게 이어지는 느낌이어야 한다 — 앞으로 만드는 모든
  도구/오브젝트 애니메이션에 항상 적용한다.
- **도구는 캐릭터 옆에 별도 아이콘을 띄우는 방식을 쓰지 않는다.** 대신 도구를 든
  캐릭터의 "들고 있는 모션"과 "실제로 쓰는 모션" 프레임 자체에 도구를 쥔 손 모양을
  그려 넣는다 — 도구별로 캐릭터 전체가 다시 그려진 애니메이션 프레임 세트(방향별)가
  생긴다 — 어떤 도구가 있고 각각 무엇을 하는지는 「생활 스킬 — 채집 계열」의 도구 표가
  원본이고, **새 도구를 붙이는 순서는 「새 도구를 추가하는 절차」에 있다.**
  도구별 애니메이션에는
  "들고 있기"/"사용하기" 말고도 반드시 "그 도구를 든 채 걷기" 세트도 있어야 한다.
  근거: 아이콘을 캐릭터 옆에 띄우고 위치만 옮기는 방식은 캐릭터 자체가 정적이고 도구가
  손과 무관하게 붙어 있어서 생기는 구조적 문제였다 — 코드 조정(오프셋/z_index)으로는
  한계가 있고, 캐릭터+도구를 하나의 그림으로 통합해서 다시 그려야 한다.

(2026-09-07, INBOX #15 — 걷기를 실제로 만들면서 정한 구체값이다.)

| 항목 | 값 |
|---|---|
| 걷기 프레임 | **한 바퀴 6프레임** (`gen_character.py` 의 `WALK_FRAMES`) |
| 재생 속도 | **14fps** (`player_frames.gd` 의 `WALK_FPS`) — 0.43초에 두 걸음 |
| 시트 | `player_walk_<스타일>.png`, 행 = 방향 / 열 = 프레임 (idle 과 같은 규칙, 아트 **96px** 칸 — 2026-09-08 에 17px 에서 바뀌었고 이제 `rig.py` 가 굽는다) |

(2026-09-07, INBOX #24 — 첫 도구(도끼)를 실제로 만들면서 정한 구체값이다.
**남은 도구 6종도 이 값에서 출발한다.**)

| 항목 | 값 |
|---|---|
| 도구 하나가 붙이는 모션 | **3종** — `hold_<도구>`(1프레임) / `use_<도구>`(6프레임) / `walk_<도구>`(6프레임) |
| 사용 모션 재생 속도 | **12fps** (`player_frames.gd` 의 `USE_FPS`) — 0.5초에 한 번 내려친다 |
| 시트 | `player_<모션>_<스타일>.png` — **스타일 1종(`farmer`) × 3모션 = 도구 하나당 3장.** 2026-09-08 에 머리모양 4종이 1종이 되면서 12장에서 줄었다 |
| 아이템 아이콘 | `item_<도구>.png` (**51px** 한 칸 — `gen_character.py` 의 `ICON_N`. 2026-09-08 에 17px 에서 바뀌었다) |
| 바닥에 놓인 그림 | `ground_<도구>.png` (**36px 한 칸** — `gen_character.py` 의 `GROUND_N`. 2026-09-08 에 12px 에서 바뀌었다) |
| 도구 목록이 적힌 곳 | `gen_character.py` 의 `TOOLS` / `player_frames.gd` 의 `TOOLS` / `qa_sprite_check.py` 의 `TOOL_NAMES` **세 곳뿐이다** |

- **사용 모션이 걷기와 같은 6프레임인 것은 그림 사정이다** — 4장으로 줄이면 도끼가
  한 프레임에 도는 각도가 커져 「이어짐」 상한을 넘고, 무엇보다 올라갈 때와 내려올
  때가 **같은 그림**이 되는 자리가 생긴다(걷기에서 위상을 반 칸 밀었을 때와 같은 실패).
- **재생만 12fps 로 걷기(14)보다 느리다** — 도끼질이 걸음보다 무겁다. 더 빠르면
  도끼가 순간이동하는 것처럼 보인다.
- (2026-09-07, INBOX #26 — **두 번째 도구(곡괭이)를 붙이면서 알게 된 것**이다.
  남은 5종도 여기서 출발한다.)
  - **도구가 늘어도 고치는 곳은 「도구 목록이 적힌 곳」 세 곳뿐이었다.** 곡괭이는
    거기에 이름을 넣고 `gen_character.py` 에 머리 모양 하나를 더한 게 전부다 —
    `player.gd`·`world.gd`·씬은 한 줄도 안 고쳤다(INBOX #25 가 깔아둔 배선 그대로).
  - **도구끼리 실루엣이 겹치지 않는지는 그림 쪽 문제다** — 도끼와 곡괭이는 둘 다
    자루 끝에 머리가 달렸지만, **도끼는 한쪽으로 넓은 쐐기 / 곡괭이는 좌우 대칭
    갈고리**로 갈랐다. 자세한 것은 `docs/STYLE_GUIDE.md` 「손에 쥔 도구」.
  - **사용 모션은 도구마다 다르다.** 곡괭이는 도끼보다 덜 크게 휘두르고 앞으로
    찍는다(바위벽을 향해 치는 동작이라 머리 위로 넘기지 않는다) — 값은
    `gen_character.py` 의 그 도구 설정에만 있고, 재생 속도(`USE_FPS` 12)와
    길이(`USE_TICKS` 30틱)는 도구 공통이다.
  - **자체 QA 에 도구 이름을 박아두지 않는다.** `qa_hotbar.gd` 가 "시트가 없는
    도구"의 예로 곡괭이를 박아뒀다가 이번 바퀴에 거짓 실패를 냈다 — 지금은
    아이템 표에서 그때 남아 있는 것 하나를 골라 쓴다.
- (2026-09-07, INBOX #27 — **세 번째 도구(낫)를 붙이면서 알게 된 것**이다.)
  - **휜 날은 `capsule` 토막을 이어 붙여 그리면 안 된다.** 곡괭이 갈래는 그렇게
    그렸지만 그건 굵기가 일정해도 되는 갈고리였다 — 날은 **가운데가 두껍고 양끝이
    뾰족해야** 날로 읽힌다. 낫은 **원에서 어긋난 원을 도려낸 초승달**(`crescent`)
    이다. 자세한 것은 `docs/STYLE_GUIDE.md` 「손에 쥔 도구」.
  - **도구가 길면 사용 모션이 칸에 안 들어간다.** 자루 + 머리가 6px 을 넘으면
    자루가 수평에 가까워지는 순간 날이 칸 밖으로 나간다(「잘림」). 낫은 도끼보다
    팔꿈치를 훨씬 깊게 접어(`swing_tuck` 4.0 vs 2.2) 해결했다 — 자루를 짧게
    하는 것만으로는 모자랐다.
  - **가로베기와 내리찍기는 각도 진폭이 아니라 손이 가는 방향으로 갈린다.**
    도끼는 자루가 82도 ↔ -60도를 돌지만, 낫은 44도 ↔ -24도만 돌고 대신 **손이
    몸 앞을 가로지른다**(`swing_fwd` 가 음수 — 날이 내려갈수록 손이 앞으로).
- (2026-09-07, INBOX #28 — **네 번째 도구(총)를 붙이면서 알게 된 것**이다.
  **양손으로 잡는 첫 도구**라 여기서 틀이 한 번 넓어졌다.)
  - **양손 도구는 `tool_pose()` 가 두 손을 같이 움직인다**(`support`). 지금까지는
    한 손만 도구를 잡는다는 전제였다 — 받치는 손은 **자기 어깨에서 조금만**
    움직이고(크게 옮기면 팔이 몸을 가로지르는 막대가 되어 「각짐」에 걸린다),
    되튐에도 두 손이 같이 밀린다(한 손만 밀리면 총이 손에서 빠져 보인다).
  - **쥐는 손이 도구마다 다를 수 있다**(`hand`). 총구가 화면 오른쪽을 보므로
    방아쇠 손은 **뒤쪽(화면 왼쪽)** 손이다 — 다른 셋은 오른손 하나로 든다.
  - **옆모습에만 다른 값을 줄 수 있다**(`reach_side` / `drop_side`). 앞/뒷모습은
    어깨가 7px 라 손을 몸 밖으로 내보내야 자루가 안 묻히지만, **가로로 긴 도구**는
    옆모습에서 그만큼 더 내밀면 총구가 칸 밖으로 나가고(「잘림」) 허리띠 줄을
    통째로 덮는다(「비율」이 몸통과 다리를 가르는 줄을 잃는다).
  - **되튐은 한쪽으로만 가는 모션이다**(`swing_bias`). 도끼·곡괭이·낫은 자루가
    가운데를 두고 위아래로 왕복하지만, 총은 **`hold` 자세에서 뒤로 밀렸다 돌아올
    뿐**이다 — 앞으로도 나가게 두면 총이 노를 젓는 것처럼 보인다. 위상도 밀어서
    **0번과 5번 프레임이 쉬는 자세**가 되게 한다(`swing_phase`). 그래야
    `hold` → `use` → `hold` 로 넘어가는 순간 총이 튀지 않는다.
  - **되튐은 캐릭터를 밀지 않는다** — 팔과 총만 움직인다(「자리」 검사가 몸이
    칸 안에서 미끄러지는 것을 잡는다). 탑다운에서 "위로 튀는" 반동은 없다
    (위 「총기 스탯」의 2026-09-07 정정과 같은 이유다).
- (2026-09-07, INBOX #29 — **다섯 번째 도구(괭이)를 붙이면서 알게 된 것**이다.
  **도끼와 같은 원시 도형(`wedge`)을 쓰는 첫 도구**라, 겹치지 않게 만드는 값이
  전부 여기서 나왔다.)
  - **도끼와 갈리는 것은 날의 방향이다.** 도끼는 쐐기의 축이 자루를 따라 내려가
    잘린 날 변이 **자루와 나란히 선다(세로)**. 괭이는 축이 자루에 **수직**이라
    그 변이 **가로로 눕는다** — 밭을 미는 날이다.
  - **날만 눕혀서는 안 됐다.** 쐐기를 눕혀 자루 끝에 바로 붙이면 17px 에서 그냥
    **가로로 뻗은 쇠막대**(자/칼)로 읽힌다. 괭이를 괭이로 만드는 것은 날이 아니라
    **꺾임**이다 — 자루가 곧게 서고, 그 끝에서 쇠가 한 칸 바깥으로 나갔다가
    직각으로 내려온다. 그래서 괭이머리는 **목(`capsule`) + 날(`wedge`)** 두
    토막이고, 둘 사이에 자루가 보이는 **빈 칸이 한 줄** 남는다(곡괭이 갈래 끝의
    "한 칸 빈틈"·낫의 오목한 안쪽과 같은 장치다).
  - **날을 걸어 내리는 길이가 도끼·곡괭이와 갈라놓는 나머지 절반이다.** 한 줄만
    내리면 갈고리라 곡괭이 갈래 한 짝처럼 보이고, 두 줄을 내려 **아랫변이 곧은
    널판**이 되어야 괭이로 읽힌다.
  - **사용 모션은 도끼보다 덜 들고 더 깊이 내린다**(0도 ± 62도 대 도끼 11도 ±
    71도). 괭이는 머리 위로 넘기는 도구가 아니라 가슴께에서 들어 **발 앞의 땅**
    을 찍는 도구다 — 마지막 프레임에서 날이 발 앞에 닿는다.
- (2026-09-07, INBOX #30 — **여섯 번째 도구(물뿌리개)를 붙이면서 알게 된 것**이다.
  **날이 없는 첫 도구**라, 다섯 도구가 공유하던 "막대 + 그 끝의 얇은 쇳덩이"라는
  틀이 여기서 처음 깨졌다.)
  - **머리를 손 위에 올리지 않는다 — 손 아래에 매단다**(`hold` 각도가 음수인 첫
    도구다). 다섯 도구는 전부 자루를 세워 머리가 어깨~머리 높이에 오는데, 통을
    거기 올리면 **얼굴 옆의 쇳덩어리**라 무엇을 들었는지 안 읽힌다. 통은 들고
    다니는 것이라 허리 아래에 매달려야 물통으로 읽힌다.
  - **그 자리가 17px 칸에서 유일하게 넓은 곳이기도 하다.** 어깨 옆은 세 칸
    남짓이라 통(3px)과 주둥이(2px)를 같이 넣을 수 없고, 다리 옆은 다섯 칸이라
    둘 다 들어간다. 어깨 옆에 욱여넣은 후보는 전부 **주둥이가 통에 먹혀** 그냥
    쇳덩이가 됐다.
  - **가는 관과 그 끝의 살수구를 따로 그리면 실루엣이 조각난다.** `capsule`(관)
    + `wedge`(살수구)로 나눈 후보는 축소 뒤에 살수구가 **대각선으로만 붙은 한 칸**
    이 됐다(실루엣 검사는 4-이웃으로 센다). 뿌리에서 끝으로 벌어지는 `wedge`
    **하나**로 그리면 관과 살수구가 한 붓에 나온다.
  - **사용 모션은 총과 같은 한쪽짜리 모션이다**(`swing_bias`) — 내리찍기가 아니라
    `hold` 자세에서 기울었다 돌아오는 붓기다. 다만 **통이 손 아래 3px 에 매달려
    있어서 각도만 바꾸면 통이 몸 안으로 들어온다** — 기우는 동안 손을 그만큼
    바깥으로 밀어야 한다(`swing_fwd` 2.8, 통과 구간은 위로 「잘림」·아래로
    "휘두르는 것처럼 보임"에 막혀 있다).
  - **물줄기는 그리지 않았다** (INBOX #30 이 판단에 맡긴 것이다). 17px 에서 물은
    1px 짜리 줄이라 **재질 하나를 새로 늘려야 하는데**(「그래픽 파이프라인」의
    재질별 4단계 램프), 그 대가로 얻는 게 프레임 6장 중 2~3장의 한두 칸이다.
    게다가 **물이 떨어지는 자리는 방향마다 다르다** — 앞모습에서 "앞"은 화면
    아래(=자기 발밑)라 물을 자기 발에 붓는 그림이 된다. 실제 물주기 효과가
    2단계라 그때 밭 타일과 같이 정하는 쪽이 맞다.
- **낚싯대**(2026-09-07, INBOX #31 — 「그래픽 파이프라인」의 절차대로 붙인 일곱 번째
  도구이자 마지막 도구다):
  - **사용 모션(`use_fishing_rod`)이 없는 유일한 도구다**(사람 결정). 낚시의 피드백은
    캐릭터 자세가 아니라 **찌가 날아가 물에 떨어지는 것**이고 그건 그림이 아니라
    던지기 로직이라 2단계다. 위 도구 표에서 낚싯대만 "대상 없이 휘두르기 X" 인 것과
    같은 자리다 — **그래서 이 도구만 시트가 2장(모션 2종 × 스타일 1종)이다.**
    `hold`/`walk` 는 만든다: 그게 없으면 낚싯대를 든 게 안 보여서 위 "도구를 캐릭터
    옆에 아이콘으로 띄우지 않는다"가 깨진다.
  - **사용 모션이 없는 도구는 좌클릭해도 자세가 안 바뀐다.** 없는 `use_` 로 갔다가
    빈손 모션으로 물러나면 0.5초 동안 손에 든 낚싯대가 사라지는데, 그건 바로 위
    규칙과 정면으로 어긋난다. 목록은 `player_frames.gd` 의 `NO_USE_TOOLS` 이고
    생성기(`gen_character.py` 의 `has_use()`)와 짝이다.
  - **머리가 없는 유일한 도구다.** 앞의 여섯은 전부 "자루 + 그 끝의 쇳덩이(또는 통)"
    인데 낚싯대는 **자루가 그대로 도구 전체**다 — 갈리는 자리가 머리 모양이 아니라
    **길이와 굵기의 변화**다.
  - **릴 두 칸이 낚싯대를 낚싯대로 만든다**(총의 탄창 한 칸과 같은 성격이다).
    쇠가 자루 끝이 아니라 **손 옆**에 있는 유일한 도구이기도 하다 — 없으면 실루엣이
    통째로 휜 나뭇가지·활이 된다.
  - **줄과 찌는 그리지 않았다.** 줄은 17px 에서 1px 짜리 선이라 재질을 새로 늘려야
    하는데, 무엇보다 **던지지 않은 낚싯대의 줄은 대에 붙어 있어서** 대와 겹친 한 줄이
    되어 보이지 않는다. 찌가 날아가는 것은 2단계이므로 그때 같이 정한다(물뿌리개의
    물줄기와 같은 판단이다).
- (2026-09-08, INBOX #66 — **도구가 다시 손에 들렸다.** 같은 날 캐릭터 파이프라인이
  절차 생성기에서 리그로 바뀌면서(`docs/CHARACTER.md`) 도구가 시트에서 빠져 있었다:
  자세만 있고 도끼를 들어도 빈손으로 팔만 들었다. 절차와 값은 `docs/CHARACTER.md`
  4-b 절이 원본이고, 여기 적는 것은 **설계 쪽 결정** 셋뿐이다.)

  | 항목 | 값 |
  |---|---|
  | 도구가 사는 자리 | 리그의 **부품 하나**(아래팔의 자식) — 팔이 도는 만큼 같이 돈다 |
  | 그림 | `gen_character.py` 의 도구 7종 **그대로**, 칸만 96px 짜리로 키워 굽는다 |
  | 방향 | `down`/`left`/`right` 는 몸 앞, **`up` 은 몸 뒤** |

  - **도구 그림을 새로 그리지 않는다.** 「같은 도구가 사는 자리는 이제 셋이다 — 손 ·
    칸 · 바닥」(`docs/STYLE_GUIDE.md`)이 그대로 지켜지려면 형태가 한 곳에서 나와야 한다.
    바뀐 것은 **칸 크기 하나**이고, 그건 `gen_character.canvas(n, unit)` 이 이미 하는
    일이다. **그래서 INBOX #67(아이콘·바닥 그림의 도트 크기)은 이 그림을 그대로 쓴다.**
  - **뒷모습에서 도구는 몸에 가리되 사라지지는 않는다.** 뒤에서 본 사람이 앞으로 든
    도구는 몸에 가려야 자연스러운데(#66 (3)), 위 「새 도구를 추가하는 절차」 5번이
    *"뒷모습에서 도구가 몸에 완전히 가려지면 무엇을 들었는지 알 수 없다"* 고 못 박아
    둔다. 손이 몸 밖에 있어서 **자루와 머리는 어깨 옆·위로 그대로 보인다** — 두 규칙이
    같이 지켜지는 자리다.
  - **자세는 도구를 안 가리지만 「쓴다」는 도구마다 다르다.** 「든다」는 도구 7종이 같은
    자세를 쓴다(`docs/CHARACTER.md` 4절). 반면 사용 모션은 위 INBOX #26·#28·#30 이
    이미 도구마다 정해뒀고 그게 그대로 살아 있다 — **총은 돌지 않고 뒤로 밀렸다
    돌아오며**(돌리면 총구가 땅을 본다), **물뿌리개는 기울이기만 한다.** 목록은
    `rig.py` 의 `USE_STYLE` 이다.
  - **총을 한 손으로 든다** — INBOX #28 이 정한 *"양손 도구는 두 손이 같이 움직인다"*
    가 지금은 안 지켜진다. 받치는 손을 붙이려면 「든 자세」가 도구를 알아야 하는데,
    그건 위 *"자세는 도구를 안 가린다"* 와 부딪힌다. **어느 쪽을 택할지는 사람이 정한다**
    (`docs/STATUS.md`).
- **도구 색은 커스터마이징을 따라가지 않는다.** 자루(나무)·날(쇠) 램프는 고정색이라
  어떤 외형을 골라도 같은 도끼다 — 팔레트 교체가 기준색 램프에 있는 색만 갈아끼우기
  때문에 저절로 그렇게 된다(`qa_player_walk.gd` 가 픽셀로 확인한다).

- **손에 든 도구가 캐릭터에 그대로 보인다** (2026-09-07 사람 결정). 핫바에서 도끼를
  고르면 캐릭터가 `hold_axe` 로 서고, 걸으면 `walk_axe`, 좌클릭하면 `use_axe` 가
  한 번 재생된 뒤 다시 `hold_axe` 로 돌아온다. 빈손이면 예전대로 `idle`/`walk` 다.
  - **사용 모션은 대상이 없어도 나온다**(낚싯대 제외 — 「생활 스킬 — 채집 계열」).
  - **멀티플레이 대비**: "몇 번 칸을 들었는가"와 "좌클릭했는가"는 **입력**이다
    (「서버 권위」의 "입력만 보낸다"). 서버가 좌클릭의 결과(무엇을 캤는가)를
    계산하려면 그 시점에 무엇을 들고 있었는지를 알아야 하므로, 든 칸은 화면 상태가
    아니라 플레이어 상태로 다룬다.
  - (2026-09-07, INBOX #25 — 실제로 만들면서 정한 구체값이다.)
    | 항목 | 값 |
    |---|---|
    | 입력에 실리는 것 | `player_input.gd` 의 `hotbar`(든 **칸 번호**) + `use`(좌클릭) |
    | 든 칸이 사는 곳 | `player_motion.gd` 의 `held_slot` — 코어가 들고 있는 플레이어 상태다 |
    | 한 번 쓰는 시간 | **30틱 = 0.5초** (`player_motion.gd` 의 `USE_TICKS`) |
    - **입력에 담는 것은 아이템 id 가 아니라 칸 번호다.** 서버는 그 칸에 무엇이 들어
      있는지 자기 인벤토리에서 직접 보므로, 클라이언트가 "나는 도끼를 들었다"고
      주장할 자리가 없다.
    - **어느 모션을 그릴지는 노드가 정한다**(그리기다). 우선순위는 **쓰는 중 → 걷는 중
      → 서 있기**이고, **시트가 없는 도구를 들면 조용히 빈손 모션으로 물러난다** —
      도구 7종 중 그림이 있는 것만 `player_frames.gd` 의 `TOOLS` 에 있고, 나머지를
      들어도 에러로 죽지 않는다.
    - **`USE_TICKS` 는 그림 길이(`use_<도구>` 프레임 수 ÷ `USE_FPS`)와 같아야 한다** —
      어긋나면 짧은 쪽이 끝난 뒤 도끼가 멈춘 그림으로 얼어 있거나 모션이 잘린다.
      `qa_hotbar.gd` 가 둘을 직접 견준다.
    - **사용 모션만 도는(loop) 애니메이션이 아니다**(`player_frames.gd` 의 `plays_once`).
      돌게 두면 `hold_` 로 넘어가기 전에 두 번째 스윙이 시작되어 그림이 튄다.
    - **좌클릭 키는 `world.gd` 의 `_unhandled_input` 한 곳**이고 **창이 하나라도 열려
      있으면 안 먹는다** — 인벤토리에서 아이템을 끄는 좌클릭과 부딪힌다.
    - 검증은 `game/qa/qa_hotbar.gd`.
- **모션이 늘어나도 노드는 안 고친다.** 어떤 모션이 있고 몇 fps 인지는
  `player_frames.gd` 의 `MOTIONS` 한 곳에만 있다 — 거기에 한 줄 늘리면 시트가 같이
  실려서 `<모션>_<방향>` 애니메이션이 생기고, 색 바꿔치기(`character_sprite.gd` 의
  `sprite_frames()`)도 자동으로 그 모션까지 칠한다. 도구별 모션은 이 자리로 붙인다.
- **걷기 시트도 반드시 같이 칠해야 한다.** idle 만 칠하면 걷기 시작하는 순간 캐릭터가
  기준색으로 되돌아간다 — `qa_player_walk.gd` 가 네 방향 × 모든 프레임을 픽셀로 견준다.
- **한 걸음은 타일 1칸쯤이다.** 이동 속도 240(타일 5칸/초)에 14fps × 6프레임이면
  초당 약 4.7걸음이다. 더 느리게 재생하면 발이 땅에서 미끄러지는 것처럼 보인다.
  - (2026-09-07, INBOX #20) **재생 속도를 10 → 14fps 로 올린 것은 캔버스가 34 → 17px
    로 절반이 됐기 때문이다.** 그릴 수 있는 보폭도 절반이 됐는데(다리가 세 줄뿐이다)
    이동 속도 240 은 그대로라, 10fps 로는 한 걸음이 **캐릭터 키의 1.4배**(타일 1.5칸)가
    되어 걷는 게 아니라 미끄러지는 것으로 보인다. 걸음 빈도는 다리 길이의 제곱근에
    반비례하므로(길이가 절반이면 √2 배) 10 × 1.41 ≈ 14 이고, 그러면 한 걸음이 캐릭터
    키만큼으로 줄어든다. **이동 속도 240 은 건드리지 않았다** — 그건 전투/이동 감각
    쪽 값이라 그림 사정으로 바꿀 값이 아니다.
- **17px 걷기에는 몸이 가라앉는 바운스가 없다**(2026-09-07, INBOX #20 — `CFG` 의
  `bob=0`). 도트에서 몸을 내릴 수 있는 최소 단위가 1px 인데, 캔버스가 절반이 되면서
  그 1px 이 몸 높이의 1/14 가 됐다 — 목이 사라지고 머리가 어깨에 얹혀서 **튀는 게
  아니라 주저앉는** 것으로 보인다. 위아래 움직임은 **발이 뜨는 것**(`lift`)이 맡는다.
- 프레임을 만드는 자세 계산은 `gen_character.py` 의 `walk_pose()` 한 곳에 있고,
  **형태 정의는 프레임마다 다시 쓰지 않는다** — 같은 `character()` 에 오프셋만 넣는다.
  그래서 프레임 사이에서 캐릭터가 차지하는 크기가 어긋날 수가 없다(위 두 번째 규칙).
- 검증은 `game/qa/qa_sprite_check.py` 의 「이어짐」(그림 자체 — 연속 프레임 사이의
  변화량이 너무 크거나 너무 작지 않은지, idle 에서 걷기로 넘어갈 때 뚝 끊기지 않는지)과
  `game/qa/qa_player_walk.gd`(엔진 쪽 — 애니메이션이 실렸는지, 칠해졌는지, 키에 맞춰
  도는지). 새 모션을 만드는 바퀴는 둘 다 넓혀서 그 모션도 보게 할 것.
