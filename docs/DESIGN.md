# DESIGN.md — 무엇을 만드는가

> 이 문서는 초기 기획서입니다. 한 번 쓰고 나면 거의 고치지 않습니다.
> 방향이 바뀌면 여기를 고치고, 왜 바뀌었는지 한 줄 남겨두세요.
> **새로 열리는 세션이 이 프로젝트에 대해 아는 것은 이 md 파일들이 전부다.**
> 이번 바퀴와 무관해 보이는 절이라도 전체를 다 읽는다.

## 무엇을

2D 탑다운 도트 그래픽 멀티플레이 게임. **힐링(생활: 채집/농사/목장)** 과 **PvP(총기 전투)**
두 축을 함께 가진다. 그래픽은 스타듀밸리 같은 도트 스타일. 전투 감각의 기준은 "덕코프".

## 왜

**게임 이름: `Gunfarm` (건팜)**
**한 줄 피치: "당신의 농장은 병참기지다."**

힐링(생활)과 전투를 그냥 나란히 놓은 게 아니다 — **생활이 전투를 먹여살리는 구조**로
붙인다. 농장에서 기른 것으로 만든 버프 음식/탄약/장비를 들고 위험한 곳에 나가고, 거기서
얻은 재료로 다시 농장의 생산을 올린다. 두 축이 서로의 자원 층이 된다.

**이 문서의 개별 결정이 서로 충돌할 때는 이 문장을 기준으로 판단한다.** 특히 자동화는
그 자체가 목표가 아니라 **원정을 위한 보급선**이다 — 자동화가 끝판이 되어 플레이어가
화면을 안 보게 되면 방향이 틀어진 것이다.

**대상 관객은 둘이고, 어느 한쪽도 버리지 않는다.** 생활만 즐기려는 플레이어와 전투를
원하는 플레이어. 생활만 하는 플레이어가 "결국 전투를 강요당한다"고 느끼는 순간
설계가 실패한 것이다.

**범위 밖**: 스토리/세계관. 서사는 지금 없고, 있어야 할 이유가 생기기 전까지 만들지 않는다.

## 기술 스택

- **엔진**: Godot 4 (GDScript), 내장 고수준 멀티플레이(High-level Multiplayer API) 사용.
  근거: 씬/리소스가 텍스트 기반이라 git diff가 깨끗하고, 에디터 GUI 없이 CLI만으로도
  프로젝트를 다루기 쉬워서 사람 없는 헤드리스 루프에 적합하다.
- 도트 그래픽은 파이썬 Pillow로 절차적 생성해서 PNG로 만든 뒤 Godot에 텍스처로
  임포트한다. 외부 API는 쓰지 않는다.

## 범위 안

### 클라이언트 화면 흐름
- **메인 메뉴**: 플레이 / 설정 / 나가기 세 버튼.
- **플레이** → 캐릭터 슬롯 화면(3개 슬롯, 코어키퍼 스타일). 빈 슬롯을 고르면 캐릭터
  커스터마이징 화면으로 이동하고, 커스터마이징을 확정하면 월드가 만들어지며 입장한다.
  이미 캐릭터가 있는 슬롯을 고르면 그 캐릭터로 바로 월드에 입장한다.
- **설정** → 해상도 등 그래픽 옵션. 아래 "카메라 / 해상도" 참고.
- **나가기** → 애플리케이션 종료.
- **모든 화면에서 돌아가는 길은 Esc 하나다. 화면 안의 "뒤로" 버튼은 두지 않는다.**
  (2026-09-07 사람 결정 — 이전 2026-09-06 결정은 "뒤로 버튼 + Esc" 였다. 바꾼 이유는
  아래 「조작」의 Esc 항목 참고: 게임 안에서 Esc 가 일시정지 메뉴가 되면서, 메뉴
  화면에만 뒤로 버튼이 남으면 같은 키의 의미가 화면마다 달라진다. 갇히지 않는다는
  원래 목적은 Esc 가 모든 화면에서 동작하는 것으로 지킨다.)
  - **메뉴 화면**(캐릭터 슬롯 / 커스터마이징 / 설정)에서 Esc 는 **한 단계 위 화면으로**
    돌아간다.
  - **월드 안**에서 Esc 는 돌아가지 않고 **일시정지 메뉴**를 연다(아래 「조작」).
  - (2026-09-07, INBOX #17) 뒤로 버튼을 지운 자리에는 **"Esc — 돌아가기" 안내 글**만
    둔다(버튼이 아니라 라벨이다). 근거: 되돌아가는 유일한 길이 눈에 안 보이면 갇힌
    것처럼 느껴진다. 각 화면의 자체 QA 가 뒤로 버튼이 되살아나지 않았는지
    (이름으로도 글자로도) 함께 검사한다.

### 캐릭터 커스터마이징 항목
(2026-09-06 결정 — 이전에는 "아직 미정"이었다. 첫 캐릭터 화면을 실제로 만들어야 해서 정했다.)

- **색상 3가지**: 피부색 / 머리색 / 옷색. 각각 정해진 팔레트에서 고른다(자유 색상
  선택이 아니다 — 아래 근거).
- **머리모양**: 3~4종 중 선택.
- 이름 입력 + 확정 버튼. 확정하면 슬롯에 저장되고 월드가 만들어지며 입장한다.
- 근거: 색상 축은 "그래픽 파이프라인"의 **기준색 1개를 먼저 완성하고 나머지 색으로
  확장한다**는 규칙과 그대로 맞아떨어진다 — 기준색 세트를 만들어두면 나머지 색은
  팔레트 교체로 확장된다. 자유 색상(컬러 피커)을 주지 않는 것도 같은 이유다:
  제한 팔레트를 벗어나면 "같은 게임처럼 보이는가"(이질감 기준)가 깨진다.
- **머리모양은 모양마다 4방향 × 모든 모션 프레임이 따로 필요하다** — [DESIGN] 항목을
  쪼갤 때 이 비용을 감안할 것.

(2026-09-06 결정 — 화면을 실제로 만들면서 정한 구체값이다. `game/scripts/character_appearance.gd`
가 원본이고, 여기 표는 그 요약이다.)

- **팔레트에 담는 것은 색이 아니라 id 문자열이다.** 저장 파일에는 `light`/`black`/`grass`
  같은 id 만 들어간다 — 나중에 스프라이트를 팔레트 교체로 만들 때 그 id 가 그대로
  팔레트 키가 되고, 팔레트 색을 손봐도 기존 세이브가 안 깨진다.
  | 항목 | 선택지 |
  |---|---|
  | 피부색 (5) | 밝은 / 중간 / 구릿빛 / 짙은 / 가장 짙은 |
  | 머리색 (6) | 검정 / 갈색 / 금발 / 적갈색 / 은발 / 쪽빛 |
  | 옷색 (6) | 풀색 / 하늘색 / 흙색 / 자주색 / 잿빛 / 주홍색 |
  | 머리모양 (4) | 짧은머리 / 단발머리 / 긴머리 / 묶은머리 |
- **이름은 최대 12자**, 앞뒤 공백은 버린다. 공백뿐인 이름으로는 확정할 수 없다(확정
  버튼이 잠긴다).
- 각 항목의 **첫 번째 선택지가 기본값**이고, 좌/우 버튼이 양끝에서 되감긴다.
- (2026-09-08, INBOX #57) **선택지가 하나뿐인 항목은 좌/우 버튼을 잠근다.** 같은 날
  캐릭터 파이프라인이 바뀌면서 머리모양 시트가 한 벌(`farmer`)만 남았는데
  (`docs/CHARACTER.md` 8절), 그 상태로 화살표를 열어두면 눌러도 제자리라 **화면이
  고장 난 것처럼 보인다.** 항목을 감추지는 않는다 — 무엇을 고를 수 있는지(그리고 지금
  무엇인지)는 여전히 보여야 한다. 위 표의 「머리모양 (4)」는 **목표이지 지금 있는
  것이 아니다**: 화면은 언제나 `character_appearance.gd` 의 목록만 내놓고, 자체 QA 도
  개수가 아니라 **내놓는 것마다 시트가 있는가**를 잰다.
- **확정하면 슬롯에 저장되고 곧바로 월드로 들어간다**(위 "클라이언트 화면 흐름" 그대로).
  확정하지 않고 뒤로/Esc 로 나가면 아무것도 저장되지 않는다.
- (2026-09-06, INBOX #11) **미리보기는 실제 게임 스프라이트다**
  (`game/scripts/appearance_preview.gd`). 앞모습을 크게 하나 + 나머지 세 방향을 작게
  늘어놓는다 — 뒷모습이 없으면 「묶은머리」처럼 **뒤에서만 보이는 차이**를 확인할 수
  없다. 배율은 정수만 쓴다. **화면에서 차지하는 크기(204px / 68px)를 적어두고 배율은
  칸에서 고른다**(2026-09-07, INBOX #46 — 그전에는 배율 12배/4배를 손으로 적어뒀는데,
  캔버스가 바뀔 때마다(34 → 17 → 32px) 사람이 다시 계산해 적어야 해서 어긋나기 쉬웠다.
  지금 idle 칸 32px 에서는 **6배 / 2배**가 골라진다). 바지/신발 색은 옷색 하나에서 함께 나온다
  (팔레트를 늘리지 않고 상하의를 구분 — 아래 INBOX #13 항목 참고).
- (2026-09-06, INBOX #8) **기준색 1벌의 스프라이트가 실제로 나왔다** —
  `game/assets/sprites/player_idle_<머리모양>.png` (아트 **32px** 칸 — 34 → 17 →
  32px 로 오갔다, 아래 「아이템/오브젝트 크기 표준」 참고. **행 = 방향**
  down/left/right/up, 열 = 프레임. 지금은 idle 1프레임뿐이고 걷기는 열이 늘어난다).
  **칸 크기는 시트마다 다를 수 있다** — 2026-09-07 (INBOX #46) 에 사람이 32px 을
  게임에서 직접 보고 정하기로 하면서 **idle 만 32px 로 굽고 걷기·도구 시트는 17px 로
  두었다.** 그래서 `player_frames.gd` 는 칸 크기를 상수로 믿지 않고 **시트 높이 ÷
  방향 4개**로 시트마다 읽는다(`cell_of()`), 발밑 보정도 칸마다 다시 넣는다.
  - (2026-09-08, INBOX #47) **32px idle 을 비율부터 다시 그렸다.** 사람이 스타듀
    농부 스프라이트를 주며 *"이거 똑같이 만들어봐"* — #46 은 칸만 키우고 그림은
    그대로여서 **가로 18 × 세로 28 = 1 : 1.56** 이었다(스타듀는 16 × 32 = **1 : 2**).
    그때는 **16 × 32** 에 머리 40% 였다.
  - (2026-09-08, INBOX #49) **같은 날 목표가 다시 바뀌었다 — 이제 기준은 사람이
    ComfyUI 로 뽑아 고른 농부 시안**(`docs/design_reference/ref_farmer_ai.png`,
    *"엄청 잘 나왔는데? 내가 원했던 거긴 해"*)이다. #47 이 스타듀를 보고 잡았던 값 중
    **두 개가 뒤집혔다**: 머리가 40% → **53%**(참고 자료의 머리가 14 / 28 줄 = 50% +
    목 한 줄), 그리고 **「머리가 어깨보다 넓다」를 다시 지킨다**(16 vs 12). 세로비도
    1 : 2 → **1 : 1.78**(18 × 32)이고 몸통 5 / 다리 6 / 신발 3px 다.
    **참고 자료에 있고 우리에게 없던 것**으로 **멜빵**(상의 위에 바지 재질 두 줄 —
    재질을 새로 늘리지 않는다) · **잘록한 목** · **두껍게 감싸는 관자놀이 머리** ·
    **뒷머리 결 두 갈래**가 들어갔다. 구체값과 근거는 `docs/STYLE_GUIDE.md` 3번의
    「32px idle 의 실측값」에 있다. **걷기·도구 시트 18장은 여전히 17px 이다**(#46 의
    의도된 어긋남 — 이번에도 안 고쳤다).
  - **발밑 줄도 시트마다 다르다.** 17px 시트는 아래 한 줄이 비어 있어 설계 공간의
    15.6번째 줄이지만, **32px idle 은 칸을 다 써서 발밑이 칸의 아래 모서리**다
    (`player_frames.gd` 의 `feet_y()`). 화면에서 캐릭터 96px = 지형 타일 정확히
    두 칸이고, 이제 **그림이 실제로 그 96px 를 다 채운다.**
  만드는 코드는
  `game/tools/gen_character.py` 이고, 그 안의 `palette(skin, hair, cloth)` 에 위 표의
  16진 색을 넣으면 나머지 색은 **램프만 갈아끼워** 나온다(형태를 다시 그리지 않는다).
  검증은 `game/qa/qa_player_sprite.gd`(엔진 쪽 규격·캡처)와
  `game/qa/qa_sprite_check.py`(그림 자체의 팔레트·대비·명도, `.venv/bin/python` 으로 실행).
- (2026-09-06, INBOX #13) **바지·신발 색은 옷색에서 색상(hue)까지 옮겨서 만든다.**
  옷색을 어둡게만 쓰면 상의와 하의의 색상이 같아서 몸이 기둥 하나로 뭉친다
  (`docs/STYLE_GUIDE.md` 「자연스러움」). **바지다운 색 두 개(흙빛/데님)** 중 옷색에서
  먼 쪽에 붙이고 옷색을 조금 섞는다(`gen_character.py` 의 `pants_color()`) — 옷색
  6종 어느 것을 골라도 상의와 하의가 다른 색상이 된다. 팔레트는 늘지 않는다(여전히
  옷색 하나가 셔츠/바지/신발을 정한다).
- (2026-09-06, INBOX #13) **볼 홍조(`blush`)는 피부색이 함께 정하는 한 단계짜리
  램프다** — `character_palettes.gd` 의 `SKIN` 이 피부 램프와 같이 들고 있고,
  팔레트 교체도 같이 따라간다(피부색을 바꾸면 홍조도 바뀐다).
- (2026-09-06, INBOX #9) **상의/하의를 가르는 방식은 "옷색 × 비율"이 아니라 재질별
  목표 명도다**(`gen_character.py` 의 `bands_for()`). 미리보기의 색 사각형과 달리
  스프라이트에는 음영 4단계가 들어가서, 비율로 어둡게 하면 옷색에 따라 상의와 값이
  겹치거나 반대로 신발이 외곽선보다 어두워져 검은 구멍이 됐다. 지금은 **옷색의 명도에서
  셔츠 명도를 정하고(×1.45, 138~168 로 자름) 바지 = 셔츠 × 0.70, 신발 = 셔츠 × 0.45**
  로 계단을 코드가 보장한다 — 옷색 6종 어느 것을 골라도 상의/하의/신발이 갈린다.
  `PANTS_DARKEN`/`SHOES_DARKEN` 은 이제 "무슨 색을 어둡게 쓸 것인가"(색상)만 정한다.
- **피부색·머리색에는 이 고정 밴드를 쓰지 않는다 — 하한만 준다**(피부 88, 머리 50).
  이 둘은 플레이어가 고른 색의 명도 자체가 의미이기 때문이다(금발은 밝고 검정은
  어둡다). 실제로 한 번 고정 밴드로 만들었더니 은발도 「가장 짙은」 피부도 전부 같은
  값으로 나와서 커스터마이징이 통째로 무의미해졌다.
- (2026-09-06, INBOX #11) **색은 실행 중 팔레트 교체로, 머리모양은 시트를 나눠서
  만든다.** 5(피부) × 6(머리) × 6(옷) × 4(머리모양) = 720 벌을 구워둘 수는 없다.
  - **시트는 머리모양마다 한 장**이다(형태가 다르므로 다시 그려야 한다).
    경로 규칙은 `player_frames.gd` 의 `sheet_path(모션, 머리모양)` 한 곳에만 있다.
  - **색은 PNG 픽셀의 색을 통째로 갈아끼워서** 만든다 — 기준색 램프의 색 →
    고른 색의 램프의 색(`game/scripts/character_sprite.gd`). 재질별 4단계 램프가
    한 벌로 바뀌므로 음영이 그대로 남는다. 형태는 건드리지 않는다(알파 불변).
  - 램프 색 자체는 **생성기가 뽑아준 `game/scripts/character_palettes.gd`** 에 있다
    (자동 생성 — 손으로 고치지 말 것). `make_ramp()` 를 GDScript 로 옮겨 적으면
    반드시 생성기와 어긋나므로, 계산한 값을 그대로 내려보내는 쪽을 골랐다.
    **밴드가 항목마다 독립**이라(피부 램프는 피부색만, 옷 램프는 옷색만 본다)
    5×6×6 조합이 아니라 5+6+6 줄이면 된다.
  - 기준색 램프 20색은 **서로 겹치면 안 된다** — 겹치면 "이 픽셀이 무슨 재질인가"가
    모호해져 바꿔치기가 엉뚱한 재질을 칠한다. `qa_character_sprite.gd` 가 검사한다.
- (2026-09-06, INBOX #14) **월드 안의 플레이어도 같은 길로 외형을 입는다** — `world.gd`
  가 슬롯의 `appearance` 를 `player.gd` 의 `appearance` 에 넣으면, 그 자리에서
  `character_sprite.gd` 가 칠한 시트를 `SpriteFrames` 로 잘라 갈아끼운다
  (`CharacterSprite.idle_frames()`). 커스터마이징 화면의 미리보기와 월드의 캐릭터가
  **같은 함수의 결과**라 서로 어긋날 수가 없다.
  - **빈 외형(`{}`)은 기본 외형이다.** 슬롯을 안 거치고 월드 씬을 직접 띄워도(자체 QA 와
    개발 중 씬 단독 실행이 그 경로다) 캐릭터가 멀쩡히 떠야 하기 때문이다.
  - 검증은 `game/qa/qa_player_appearance.gd`.
- 검증은 `game/qa/qa_character_customize.gd`(화면)와 `game/qa/qa_character_sprite.gd`
  (팔레트 교체 — 램프 표 동기화 / 색 충돌 / 빠짐 없이 칠해지는지 + 극단 조합 캡처).

### 월드 생성
(2026-09-06 결정 — 이전에는 아무 서술이 없었다. 월드 입장을 만들려면 필요해서 정했다.)

- **절차적 생성**. 시드 기반으로 지형을 만든다(심플렉스 노이즈 — 실제로 쓰는 구현은 아래 표 참고).
- **1단계(먼저 할 것)**: 땅/바다 지형과 스폰 지점까지만. 나무·채광 포인트·채집 포인트
  같은 자원 배치는 **그 자원을 실제로 만드는 바퀴에 같이 붙인다** — 아직 없는 것을
  미리 배치할 수 없기 때문이다.
- 맵 크기·바다 비율 등 구체적 수치는 구현 재량으로 정하고, 정한 값을 여기에 기록한다.
- 같은 시드는 항상 같은 월드를 만들어야 한다(서버가 월드를 재현할 수 있어야 하므로).

(2026-09-06 결정 — 1단계를 실제로 구현하면서 정한 구체값이다. `game/scripts/world_gen.gd`
가 원본이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 지도 크기 | **256 × 256 타일** (= 12288 × 12288 월드 단위, 가로로 논리 해상도 약 8.5 화면) |
| 타일 한 칸 | **48 월드 단위** (아트 16px × 씬 스케일 3) |
| 지형 종류 | 땅 / 바다 **2종만** (자원·해변 등은 그걸 만드는 바퀴가 붙인다) |
| 바다 비율 | **약 45~48%** (자체 QA 합격선은 40~55%) |
| 노이즈 | `FastNoiseLite` simplex-smooth + FBM 5옥타브, frequency 0.022 |
| 섬 모양 | `노이즈 × 2.5 + 0.70 − (중심에서의 거리)³ > 0` 이면 땅 |

- **타일 크기 48은 캐릭터와 같은 도트 크기 단위에서 나왔다** — 캐릭터도 타일도 씬
  스케일이 **3배**라, 아트 픽셀 하나가 화면에서 똑같이 3px 이 된다("아이템/오브젝트
  크기 표준" 참고). 나중에 격자 건축이 이 48 격자 위에 올라간다.
  **여기서 맞춰야 하는 것은 캐릭터의 전체 크기가 아니라 배율(3배)이다** — 2026-09-07 에
  캐릭터 캔버스는 34 → 17 → **32px** 로 오갔지만(화면 102 → 51 → **96px**) 배율이
  줄곧 3배라 이 단위 일치는 한 번도 깨지지 않았다. **타일 크기 48 은 바뀌지 않는다**
  (캐릭터가 타일 두 칸 → 한 칸 남짓 → 다시 두 칸으로 오갔을 뿐이다 — 지금 두 칸인
  것은 idle 뿐이다, INBOX #46).
  (2026-09-06 정정 — 전에는 "아트 32px × 1.5배"로 적혀 있었다. 화면 크기는 같지만
  **소수 배율은 화면에서 아트 픽셀 하나가 1px, 2px 로 들쭉날쭉해져 도트가 뭉개진다** —
  배율은 정수여야 한다는 `docs/STYLE_GUIDE.md` 1번에 맞춰 아트 해상도 쪽을 고쳤다.)
- **`opensimplex`(파이썬)가 아니라 Godot 내장 `FastNoiseLite` 를 쓴다.** 월드는 게임이
  돌면서 시드로 만들어져야 하는데(캐릭터마다 시드가 다르다) 파이썬은 실행 시점에 없다.
  `opensimplex` 는 파이썬으로 그림/지도를 미리 뽑아볼 때 쓰는 도구다.
- **섬 하나 모양이다.** 노이즈 높이에서 중심으로부터의 거리 감쇠를 빼서, 지도 가장자리는
  항상 바다가 된다(테두리 4칸은 무조건 바다). 근거: 월드 밖으로 걸어나갈 수 있으면
  경계 처리를 따로 만들어야 한다. 결과는 "만과 반도가 있는 큰 섬 하나 + 내륙 호수 여러 개
  + 앞바다 작은 섬 몇 개"이고, 육지의 85% 이상이 본섬 하나에 몰려 있다.
- **감쇠보다 노이즈가 세야 한다.** 노이즈 배율이 작으면 거리 감쇠가 지형을 지배해서
  시드를 바꿔도 똑같이 생긴 **밋밋한 원형 섬**만 나온다(실제로 한 번 그렇게 만들었다가
  고쳤다). 파라미터를 손볼 때 자체 QA 의 "해안선 복잡도"(바다에 닿은 땅 칸 / 전체 땅 칸,
  지금 약 10%, 합격선 7%)를 반드시 같이 볼 것 — 완전한 원이면 2~4% 밖에 안 나온다.
- **스폰 지점은 "가장 큰 육지 덩어리 안에서 지도 중심에 가장 가까운 칸"** 이고, 주변
  8칸이 전부 땅인 칸을 우선한다. 근거: 노이즈는 1~2칸짜리 외딴 섬을 얼마든지 만든다 —
  단순히 "중심에서 가장 가까운 땅"으로 고르면 사방이 바다인 점 하나에 갇힌 채 시작할 수 있다.
- **월드 시드는 캐릭터를 만드는 시점에 정해서 슬롯에 저장한다**(`slot_store.gd` 의
  `world_seed`). 첫 입장 때 정하면 그때까지 슬롯이 "월드 없는 캐릭터" 상태로 남고, 같은
  슬롯으로 다시 들어왔을 때 같은 월드가 나온다는 보장을 여러 군데서 따로 지켜야 한다.
  시드 `0` 은 "아직 안 정해짐"이라 쓰지 않는다 — 그런 슬롯으로 들어가면 그 자리에서
  시드를 정해 저장한다.
- 생성 코어(`world_gen.gd`)는 노드를 상속하지 않는 순수 클래스다("시뮬레이션 구조" 참고) —
  서버가 화면 없이 같은 월드를 재현할 수 있어야 하므로. 이 파일 안에서는 전역 난수
  (`randi()` 등)를 쓰지 않는다. 월드 하나 만드는 데 약 180ms.
- (2026-09-06, INBOX #12) **지형은 타일 스프라이트로 그린다** —
  `game/assets/sprites/terrain_tiles.png` 한 장(아트 16px 칸 4096개)을
  `game/scripts/terrain_view.gd` 가 화면에 들어오는 칸만 떠다 붙인다. 칸 번호 규칙은
  `game/scripts/terrain_tiles.gd` 한 곳에만 있고, 그림을 만드는 코드는
  `game/tools/gen_terrain.py` 다.
  - **한 칸에 밑그림과 해안이 같이 구워져 있다** — 칸 번호가
    `무늬변주 × 512 + (중심이 땅이면 256) + 이웃 8칸의 땅 비트마스크` 라서, 그리는
    쪽은 칸마다 그리기 한 번이면 되고 반투명 오버레이를 겹치지 않는다.
  - **무늬 변주는 좌표 해시로 고른다**(8종). `x % 3` 같은 규칙으로 고르면 같은
    무늬가 일정 간격으로 되풀이돼서 풀밭이 벽지처럼 보인다.
  - **무늬의 양은 개수가 아니라 칸 넓이당 비율로 정한다** (2026-09-08, INBOX #60).
    한때 풀포기·물결 개수가 상수로 박혀 있었는데, 아트 칸이 16 → 24 → 48px 로
    커지는 동안 그 개수가 안 따라와서 **넓이당 무늬가 9분의 1** 이 됐다 — 땅 한가운데
    칸 픽셀의 **98.9% 가 한 색**이라 풀밭이 초록 평면이었다(바다도 같았다).
    지금은 `gen_terrain.py` 의 `GRASS_TUFTS` / `SEA_RIPPLES` 가 **1000px² 당 개수**로
    적혀 있어서 칸 크기를 바꾼 바퀴가 잊어도 밀도가 따라온다.
  - **"무늬가 있는가"와 "무늬가 보이는가"는 다르다.** 「변주」는 8종의 그림이
    서로 다르기만 하면 통과하고(점 두 개를 다른 자리에 찍어도 8종이다),
    「채도」·「평균명도」는 오히려 단색일 때 가장 안정적으로 통과한다 — 그래서
    위 상태가 세 검사를 전부 초록불로 지나갔다. `qa_sprite_check.py` 의
    **「무늬」**가 가장 넓은 한 색의 비율 상한과 칸 안 명도의 표준편차 하한을 함께 본다.
  - **해안선은 타일 사각형이 아니다** — 거리장을 흐려서 모서리를 둥글리고, 타일 한
    칸 주기의 잡음으로 살짝 흔든다(주기가 타일과 같아야 이웃 타일과 이어진다).
    흔들림은 **땅이 커지는 쪽으로 치우쳐** 있다: 그림의 땅이 논리 타일보다 작아지면
    물 위를 걷는 것처럼 보이기 때문이다(충돌은 여전히 타일 단위다).
  - **격자선은 뺐다.** 인접한 같은 지형이 이어져 보여야 하는데 48px 마다 선이
    그이면 그게 안 된다. 건축이 올라갈 때 필요한 격자는 배치 미리보기로 그린다.
  - 지형 램프 색은 생성기가 `game/scripts/terrain_palettes.gd` 로 내려보낸다
    (자동 생성 — 손으로 고치지 말 것). 화면의 픽셀이 땅인지 바다인지 판정하는
    자체 QA 가 이 표를 쓴다.
  - 검증은 `game/qa/qa_sprite_check.py`(그림 자체 — 팔레트/이음매/변주/캐릭터와의
    명도차)와 `game/qa/qa_terrain_view.gd`(엔진 쪽 — 칸 번호 규칙과 캡처).
- 검증은 `game/qa/qa_world_gen.gd`(생성 로직 — 헤드리스)와 `game/qa/qa_world_entry.gd`
  (화면). 앞의 것은 **실행 사이의 재현성까지** 본다(지문을 `user://` 에 남겨 다음 실행과 비교).

### 카메라 / 해상도 (PvP 공정성 규칙)

> **시야는 이미 스타듀와 같다 — 「멀리 안 보인다」를 해상도로 해결하려 들지 말 것.**
> (2026-09-08 실측) 같은 크기 스크린샷에서 타일 화면 크기가 **스타듀 113px · 우리
> 115px**, 한 화면에 들어오는 칸이 **스타듀 30.5 × 18.1 · 우리 29.9 × 18.1** 로
> 사실상 같다. 캐릭터도 둘 다 약 2칸이다.
> 그런데도 우리 화면에서 캐릭터가 커 보인다면 원인은 카메라가 아니라 **화면에
> 캐릭터 말고 아무것도 없다는 것**이다 — 스타듀는 같은 6×6칸 안에 나무·길·절벽·
> 덤불이 다 들어 있어서 농부가 여럿 중 하나로 보인다. 크기를 견줄 대상이 없으면
> 화면에 하나뿐인 물체가 화면을 대표한다.
> 두 번째 원인은 **지형에서 칸이 안 읽히는 것**이다(무늬가 잘고 타일마다 변주가 없다).
> 고칠 자리는 「월드 오브젝트」와 「지형 무늬」이지 논리 해상도가 아니다.

- 설정에서 해상도를 바꿀 수 있다.
- **해상도를 높인다고 화면에 더 멀리(더 넓게) 보이면 안 된다.** 해상도 변경은 화면
  비율(종횡비)과 선명도에만 영향을 줘야 하고, 카메라가 보여주는 실제 월드 범위(시야)는
  모든 해상도에서 동일해야 한다. 근거: PvP에서 해상도를 높인 플레이어가 더 멀리 보여서
  유리해지는 것을 막기 위함 — 화면 크기가 곧 정보 우위가 되면 공정하지 않다.

(2026-09-06 결정 — 위 규칙을 실제로 구현하면서 정한 구체값이다.)

- **논리 해상도(= 보이는 월드 범위)는 1440×810 으로 고정한다.** 이 값이 곧 시야다.
  (**2026-09-08 사람이 1280×720 에서 넓혔다.** 캐릭터가 타일 두 칸(96px)이 된 뒤
  화면에서 커 보인다는 이야기가 나왔는데, 실측하니 **캐릭터 크기는 스타듀와 같고**
  (둘 다 타일 2칸) **카메라가 좁은 것**이 원인이었다 — 1280×720 은 26.5×15.0 타일을
  보여주고 스타듀는 약 30×17 타일이다. 1440×810 이면 **30.0×16.9 타일**이 되어
  스타듀와 같아지고, 캐릭터가 화면 세로에서 차지하는 비율도 13.2% → **11.9%** 로
  스타듀 실측값과 일치한다. **캐릭터를 다시 그리는 대신 숫자 하나로 고친 것이다.**)
  창 크기는 이 그림을 확대/축소해서 보여줄 뿐이다.
- Godot 설정은 stretch **mode=`canvas_items`, aspect=`keep`** 이다. `expand`는 창을
  넓히면 월드가 더 보여서 위 규칙을 깬다 — **`keep` 에서 벗어나지 말 것.** 16:9 가
  아닌 창(사용자가 창틀을 끌어 늘린 경우 포함)에는 검은 여백이 생기고, 보이는 월드
  범위는 그대로다.
- **고를 수 있는 해상도**: 1280×720 / 1600×900 / 1920×1080 / 2560×1440 (전부 16:9 —
  논리 해상도와 비율이 같아야 여백 없이 꽉 찬다). 지금 모니터에 안 들어가는 크기는
  목록에서 빠진다.
- **창의 기본 크기는 1280×720 이다 — 논리 해상도(1440×810)와 다르다.**
  (2026-09-08, INBOX #48 을 구현하며 정했다.) 논리 해상도를 넓히면서 1440×810 이
  「고를 수 있는 해상도」 목록에 없는 값이 됐는데, 목록에 없는 크기를 기본값으로 두면
  설정 화면이 **고를 수 없는 값을 보여주게 된다.** 그래서 기본 창은 목록의 첫 값으로 두고,
  `settings_store.gd` 가 `BASE_SIZE`(논리 해상도)와 `DEFAULT_SIZE`(기본 창 크기)를 따로
  갖는다. `project.godot` 의 `window/size/window_*_override` 가 `DEFAULT_SIZE` 와 같아야
  창이 한 크기로 떴다가 메인 메뉴에서 다른 크기로 튀지 않는다.
- 설정은 `user://settings.json` 에 저장되고, 진입 씬(메인 메뉴)이 시작할 때 창에
  반영한다.
- 검증은 `game/qa/qa_settings.gd` — 해상도마다 카메라가 덮는 월드 사각형이 같은지
  자동 확인하고, `game/qa/world_range_probe.tscn`(시야 경계 안/밖 표식)으로 눈으로도
  볼 수 있게 캡처를 남긴다. 카메라를 건드리는 바퀴는 이걸 다시 돌려볼 것.

### 조작
- 이동: WASD
- **시야/조준 방향: 마우스로 회전.** (2026-09-07 사람이 구체화 — 기준은 덕코프다.)
  - **캐릭터가 바라보는 방향은 이동 방향이 아니라 마우스 방향이다.** WASD 로 왼쪽으로
    가면서 마우스는 오른쪽을 겨눌 수 있다(뒷걸음질/게걸음). 이동 키가 방향을 정하던
    이전 동작을 대체한다.
  - 스프라이트는 4방향뿐이므로, 조준 각도를 **가장 가까운 4방향으로 스냅**해서 어느
    시트를 그릴지 고른다. 조준 각도 자체는 **각도 그대로** 보관한다 — 총알 방향과
    시야 콘(「시야 / 전장의 안개」)은 스냅된 4방향이 아니라 이 각도를 쓴다.
  - **스냅 경계에는 히스테리시스 10도를 둔다**(2026-09-06 이후 구현 시 정한 값,
    `player_motion.gd` 의 `FACING_HYSTERESIS`). 지금 보고 있는 방향은 45+10도까지
    붙잡고, 그걸 넘겨야 다음 방향으로 바뀐다. 근거: 45도 경계에 마우스를 올려두면
    손이 조금만 떨려도 두 시트가 매 프레임 번갈아 나와 캐릭터가 덜덜 떤다.
  - **멀티플레이 대비**: 조준 각도는 위치가 아니라 **입력**이다. 「서버 권위 / 클라이언트
    신뢰」의 "입력(이동 방향 / 조준 각도 / 버튼)만 보낸다" 그대로, 이동 방향과 같은
    입력 구조체에 담아 `player_motion.gd`(순수 클래스)가 받는다. **노드는 마우스
    좌표를 각도로 바꾸는 일까지만 하고, 그 각도를 쓰는 계산은 코어에 둔다** — 화면
    좌표는 클라이언트마다 다르지만 각도는 모두에게 같은 값이기 때문이다.
- **Esc: 월드 안에서는 일시정지 메뉴를 연다** (2026-09-07 사람 결정 — 이전에는 슬롯
  화면으로 돌아가는 키였다. 근거: 보통의 게임에서 Esc 는 나가기가 아니라 메뉴다).
  - 메뉴 내용: **계속하기 / 설정 / 메인 메뉴로 나가기**. 설정은 메뉴 화면의 설정과
    같은 것을 띄운다(해상도 등 — 「카메라 / 해상도」).
  - **월드 시뮬레이션은 멈추지 않는다.** 메뉴를 열어도 시간·동물·다른 플레이어는
    계속 움직인다. 근거: 멀티플레이에서는 한 사람이 Esc 를 눌렀다고 세계가 멈출 수
    없다 — 싱글에서만 멈추게 만들어두면 멀티를 붙이는 시점에 그 코드를 전부 다시
    쓰게 된다. 대신 **메뉴가 열린 동안 플레이어 입력만 받지 않는다**(이동/발사 입력이
    0 으로 들어간다 — 입력을 끊는 것이지 시간을 멈추는 게 아니다).
  - 메뉴 화면들에서 Esc 는 한 단계 위로 돌아간다(위 「클라이언트 화면 흐름」).
  - (2026-09-07, INBOX #17 — 실제로 만들면서 정한 구체값이다.) 일시정지 메뉴는
    `world.tscn` 의 `HUD/PauseMenu` 고, 여는 쪽은 `world.gd` 다.
    - **설정은 씬 전환이 아니라 월드 위에 겹쳐서 띄운다**(`settings.gd` 의 `as_overlay`
      를 켜고 `HUD` 에 붙인다). 씬을 바꾸면 월드가 통째로 내려가서 "월드는 멈추지
      않는다"가 애초에 성립하지 않는다 — 같은 이유로 나중에 인벤토리/맵도 이 자리에
      겹쳐서 붙인다.
    - **Esc 는 가장 안쪽 창부터 닫는다**: 설정이 열려 있으면 설정만, 아니면 일시정지
      메뉴를, 둘 다 없으면 일시정지 메뉴를 연다. 겹쳐 뜬 화면은 **자기 Esc 를 처리하지
      않는다**(띄운 쪽이 닫는다) — 양쪽이 다 처리하면 Esc 한 번에 두 창이 닫힌다.
    - **입력을 끊는 것은 `player.gd` 의 `input_enabled` 하나뿐이다.** 이동은 0 이 되고
      **조준 각도는 보던 값을 유지한다** — 0(오른쪽)으로 넣으면 메뉴를 열 때마다
      캐릭터가 홱 돈다.
    - 검증은 `game/qa/qa_world_entry.gd` — Esc 가 슬롯으로 나가지 않고 메뉴를 여는지,
      `get_tree().paused` 를 쓰지 않았는지(월드에 시계를 하나 걸어 실제로 시간이
      흐르는지 잰다), 메뉴가 열린 동안 W 를 눌러도 안 움직이고 닫으면 다시 움직이는지.
- **M: 맵 열기/닫기** — 아래 「맵 (M)」 참고. (2026-09-07 사람 결정)
- **좌클릭: 지금 손에 든 도구의 동작.** 총이면 발사, 도끼면 벌목, 곡괭이면 채광, 낫이면
  채집/수확, 낚싯대면 낚시 시작 — 모든 도구가 총과 동일하게 좌클릭 하나로 동작한다.
  별도의 "상호작용 키"(예: F)는 두지 않는다.
  - **그래서 "여는 것"도 좌클릭이다** (2026-09-07, INBOX #37). 커서가 열 수 있는
    오브젝트 위에 있고 손이 닿는 거리면 그 클릭은 **여는 클릭이고 도구로 가지 않는다** —
    안 그러면 상자 위를 겨눈 좌클릭이 도끼질이 되어 상자를 영영 못 연다. 지금 이 규칙이
    붙은 것은 「데스드롭 상자」 하나이고, 나중에 상자·제작대도 같은 자리로 붙는다.
- 우클릭: (총을 들고 있을 때) 장전된 탄종 전환 (기본탄 ↔ 마취탄)
- **R: (총을 들고 있을 때) 재장전** — 아래 「총기 스탯」의 탄창 항목.
- 숫자키 1~9: 인벤토리 맨 위 9칸(핫바)에서 아이템을 골라 손에 든다. 아래 "인벤토리 / 장비" 참고.
- E: 인벤토리 창 열기/닫기
- X: 건설 해제(철거) 모드 토글 — 설치된 건축물을 반투명 빨간색으로 표시하고 좌클릭으로
  철거해서 아이템으로 회수한다. 배치 모드와 동시에 켤 수 없다.

#### 플레이어 이동
(2026-09-06 결정 — 플레이어를 실제로 월드에 세우면서 정한 구체값이다.
`game/scripts/player_motion.gd` 가 원본이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 이동 속도 | **240 월드 단위/초** (= 타일 5칸/초) |
| 시뮬레이션 틱 | **60Hz 고정** (`TICK_DELTA` 1/60초) |
| 몸통 충돌 상자 | **18 × 10**, 원점(발밑) 기준 좌우 9 / 상하 5 |
| 이동 키 | W/A/S/D — `physical_keycode` 로 잡아 자판 배열이 달라도 같은 자리 키가 먹는다 |

- **이동 계산은 `player_motion.gd`(노드를 상속하지 않는 순수 클래스)가 한다**
  ("시뮬레이션 구조"). 노드(`player.gd`)는 **키를 읽어 -1/0/1 두 축으로 만들고 그리는
  일만** 한다 — 위 "서버 권위 / 클라이언트 신뢰"의 "클라이언트는 위치를 보내지 않는다.
  입력만 보낸다"를 나중에 붙이는 게 아니라 처음부터 지키기 위한 구조다. 그래서 코어의
  `tick()` 은 프레임 시간을 인자로 받지 않는다 — **호출 횟수가 곧 시뮬레이션 시간**이라,
  서버는 "실제 경과 시간이 허용하는 횟수"만 돌려주면 그게 그대로 속도 상한이 된다.
- **몸통 충돌 상자는 2026-09-07(INBOX #19)에 32×18 → 18×10 으로 줄었다** — 캐릭터
  캔버스가 34 → 17px 로 절반이 되면서 몸 폭도 절반이 됐기 때문이다. 어깨가 아트 7px ×
  3배 = 21 이라 상자(18)는 그보다 조금 좁다 — 해안에 비스듬히 붙었을 때 어깨가 걸리지
  않아야 한다(옛 값도 36 짜리 어깨에 32 였다).
- **한 틱 이동량(4)은 타일(48)보다 훨씬 작아야 한다.** 크면 바다 한 칸을 통째로
  건너뛴다(`docs/GOTCHAS.md`). 자체 QA 가 이 비율을 직접 검사한다.
- **대각선도 정규화한다** — 안 하면 대각으로 갈 때만 1.41배 빨라져서 대각으로만 뛰는 게
  이득이 된다.
- **충돌은 축을 따로 밀어서 판정한다.** 그래야 해안에 비스듬히 붙었을 때 붙잡히지 않고
  미끄러진다. 막히면 그 자리에서 멈추는 게 아니라 **타일 경계에 딱 붙인다** — 안 그러면
  한 틱 이동량(4px)만큼 물가에 틈이 벌어져 보인다.
- **노드의 원점은 발밑이다**(스프라이트를 `offset` 으로 올린다). 타일 점유와 나중의
  앞뒤(Y) 정렬이 자연스러우려면 몸 한가운데가 아니라 발이 기준이어야 한다.
- **카메라는 플레이어 노드의 자식이다** — 따라다니게 만드는 코드가 따로 없다. 확대/축소는
  하지 않는다(위 "카메라 / 해상도" PvP 공정성 규칙). 위치를 밖에서 볼 때는 `global_position`
  으로 본다.
- 지금 막는 것은 **바다 타일뿐이다.** 나무/벽/오브젝트 충돌은 그것들을 만드는 바퀴가
  같은 `blocked_at()` 자리에 덧붙인다. **이건 「걸을 수 있는가」만 답하는 자리다** —
  총알은 이 판정을 쓰지 않는다(물을 통과한다, 「전투」). 총알을 막는 것은 그쪽의
  `blocks_bullet` 에 따로 꽂는다.
- 검증은 `game/qa/qa_player_world.gd` — 코어(재현성/속도상한/바다충돌/미끄러짐)와
  화면(스폰 위치/발밑 정렬/WASD/카메라 추적)을 함께 본다.

### 전투
- 총기 기반 전투. 총마다 세부는 다르지만 기본은 "조준선을 대상에 놓고 쏜다".
- 탄종: 기본탄, 마취탄 (그 외 탄종은 미정)
- **포획**: 대상 동물의 체력이 10% 미만으로 떨어진 상태에서 마취탄에 맞으면 포획할 수 있다.

(아래는 2026-09-07 사람이 정한 것이다.)

- **총알은 투사체다 — 즉시판정(히트스캔)이 아니다.** 쏘면 총알이 실제로 날아가고,
  **피할 수 있다.**
  - **이 선택이 서버가 도는 코드를 정한다.** 즉시판정이면 서버는 쏜 순간 "이 각도의
    선분 위에 누가 있나"를 **한 번** 계산하고 끝나지만, 투사체면 총알을 개체로 두고
    **살아 있는 내내 매 틱** 움직이며 충돌을 본다. 「서버 권위」의 "명중 판정은 서버가
    계산한다"가 가리키는 대상이 이쪽이다.
  - **맞는 것**: 동물, 플레이어(지금은 PvE 라 서로 안 맞는다 — 아래), 그리고 **키가
    있는 것**(벽·문·나무·바위처럼 실제로 높이가 있어 총알을 가로막는 것).
    사거리(800)를 다 날아가면 사라진다.
  - **총알을 막는 것과 걸을 수 없는 곳은 다르다** (2026-09-07 사람 결정 — 그전에는
    이동 판정을 그대로 썼다).
    - **물은 총알을 통과시킨다.** 걸어서는 못 건너지만 총알은 넘어간다 — 사람이
      정한 근거: *"한 칸 물이 있다고 해서 거기에 막히면 안 되잖아."* 바다가 월드의
      45~48%(「월드 생성」)이고 섬 구조라, 물이 총알을 막으면 물가에서는 사실상
      사냥이 안 된다.
    - **판정을 둘로 나눈다**: `blocked_at`(걸을 수 있는가)과 **총알을 막는가**는
      서로 다른 물음이다. 지금 `bullets.gd` 는 이동 판정을 그대로 쓰고 있어서
      물에 막힌다 — 이걸 떼어낸다.
    - **새 오브젝트는 자기가 총알을 막는지 스스로 정한다.** 나무·벽·바위를 만드는
      바퀴가 "이건 키가 있으니 막는다"를 그 자리에 적는다. **지금은 총알을 막는
      지형이 하나도 없다** — 벽도 나무도 아직 없기 때문이다(「플레이어 이동」의
      "지금 막는 것은 바다 타일뿐"). 그러니 당분간 총알은 사거리를 다 날아가야만
      사라진다.
    - 판단이 애매한 것(목장 담장 같은 낮은 것)은 그것을 만드는 바퀴가 정하고 여기
      적는다 — 지금 미리 정하지 않는다.
  - **총알의 시뮬레이션도 순수 클래스에 둔다**(「시뮬레이션 구조」). 클라이언트는
    반응이 빠르도록 총알을 즉시 그리지만 **판정은 서버 것이 이긴다** — 어긋나면
    "맞은 것처럼 보였는데 안 맞았다"가 생기고, 투사체는 그 어긋남이 눈에 보인다.
    보정을 어떻게 할지는 범위 밖이고, 지금은 "판정은 서버"라는 방향만 지킨다.

- **조준선: 총을 들고 있을 때만 빨간 선이 조준 방향으로 뻗는다.** 그리고 **가만히 있으면
  점점 선명해지고, 움직이면 흐려진다** — 그 선명도가 곧 정조준 정도다.
  - **이 선이 탄퍼짐 표시를 겸한다.** 위 「총기 스탯」의 "멈춰서 쏘면 거의 없음, 이동하면서
    쏘면 커짐"을 숫자 대신 이 선 하나로 보여준다 — 퍼짐 수치를 따로 띄우는 HUD 를 만들지
    않는다(「인벤토리 / 장비」의 "상시로 두는 것은 핫바 하나"와 어긋나지 않게).
  - 총을 내리면 선도 사라진다. **다른 도구에는 조준선이 없다.**

(2026-09-07, INBOX #34 — 위 둘을 실제로 만들면서 정한 구체값이다. 원본은
`game/scripts/bullets.gd`(총알 코어) / `game/scripts/player_motion.gd`(정조준) /
`game/scripts/aim_line.gd`(조준선 그림)이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 총알 속도 | **900 월드 단위/초** (플레이어 240 의 3.75배 — 사거리 800 을 0.89초에 난다) |
| 총알 시뮬레이션 틱 | **60Hz 고정** (`player_motion.gd` 와 같은 값) |
| 충돌을 보며 나아가는 한 걸음 | **12** (타일 48 의 1/4 — 한 틱 이동량 15 가 두 걸음으로 쪼개진다) |
| 최대 탄퍼짐 반각 | **8도** (정조준이 완전히 풀렸을 때. 사거리 절반인 400 에서 좌우 56 = 타일 한 칸 남짓) |
| 정조준이 모이는 속도 | **초당 0.9** (서 있을 때 — 바닥에서 1.1초면 다 모인다) |
| 정조준이 흩어지는 속도 | **초당 1.8** (움직일 때 — 모이는 것의 두 배) |
| 한 발의 반동 | **정조준 -0.5** (연사 간격 0.5초에 되모으는 0.45보다 커서 계속 쏘면 점점 흐트러진다) |
| 조준선 길이 | **260** (사거리 800 을 그대로 그리면 화면 세로 810 의 절반인 405 를 훌쩍 넘겨 — 카메라가 캐릭터에 붙어 있다 — 화면을 통째로 가로지른다. **월드 단위라 논리 해상도를 넓혀도 그대로 둔다**: 조준선은 화면의 몇 %가 아니라 "몇 타일 앞"이다) |

- **총알 속도는 "피할 수 있는가"가 정한 값이다.** 더 빠르면 사실상 즉시판정이 되어 위
  결정이 무의미해지고, 더 느리면 총으로 안 읽힌다. 900 이면 사거리 절반(400)에 닿는 데
  0.44초가 걸리고 그동안 사람은 두 타일 넘게 움직인다.
- **한 걸음(12)을 속도와 따로 둔 이유**: 속도를 올려도 이 값이 그대로면 벽을 못 뚫는다
  (`player_motion.gd` 이 같은 함정을 겪었다 — `docs/GOTCHAS.md`).
- **총알은 저장하지 않는다.** 0.9초면 사라지는 것이라 월드를 나갔다 들어왔을 때 되살릴
  값이 아니다 — 바닥 아이템(「바닥 드롭」)과 갈리는 자리다.
- **총알 코어는 지형을 아예 모른다** (2026-09-07, INBOX #39 — 그전에는 이동 판정을
  그대로 불러서 물에 막혔다). `bullets.gd` 는 월드를 들고 있지도 않고 `is_land()` 를
  묻지도 않는다 — **위 「총알을 막는 것과 걸을 수 없는 곳은 다르다」를 구조로 못 박은
  것이다.** 이동 판정(`player_motion.gd` 의 `blocked_at()`)을 총알 쪽에서 다시 부르면
  그 순간 둘이 도로 붙으므로, 그 자리를 남겨두지 않고 의존 자체를 끊었다.

  | 항목 | 값 |
  |---|---|
  | 총알을 막는 것이 꽂히는 자리 | `bullets.gd` 의 **`blocks_bullet`** (`func(point: Vector2) -> bool`) |
  | 지금 꽂혀 있는 것 | **없다** — 그래서 **막는 것이 하나도 없다** |
  | 그래서 총알이 사라지는 길 | **사거리 소진뿐** (`hit_test` 도 아직 비어 있다) |

  - **벽·나무·바위를 만드는 바퀴가 여기에 한 줄 꽂는다** — 「새 오브젝트는 자기가
    총알을 막는지 스스로 정한다」가 코드에서 이 모양이다. `hit_test`(대상 명중)와
    **일부러 다른 자리**다: 맞는 것은 데미지를 받고, 막는 것은 그냥 총알을 세운다.
  - **지금 총알이 사거리 800 을 다 날아가야만 사라지는 것은 고장이 아니다.**
- **정조준(0~1) 하나가 조준선의 선명도이자 탄퍼짐이다** — 그리는 쪽과 쏘는 쪽이 같은
  함수(`player_motion.gd` 의 `spread_angle()`)를 부르므로 **보이는 흐릿함과 실제로
  빗나가는 정도가 어긋날 자리가 없다.** 화면 상태가 아니라 **플레이어 상태**다(총알이
  어디로 갈지를 정하므로 나중에 서버가 알아야 한다 — 「서버 권위」).
- **조준선은 심지 한 줄 + 퍼짐 부채꼴 두 겹으로 그린다.** 심지의 진하기가 정조준에
  비례하고, 부채꼴의 폭이 탄퍼짐이다. 부챗살을 여러 줄 긋는 방식은 **살이 낱낱이 보여서
  "흐린 선"이 아니라 "빨간 선 일곱 개"로 읽혀** 버렸다.
- **연사 간격은 따로 만들지 않았다** — 도구 쓰기 자체의 `USE_TICKS`(30틱 = 0.5초,
  「캐릭터 애니메이션」)가 그대로 「총기 스탯」의 초당 2발이 된다.
- **맞을 대상이 아직 없다**(동물이 없고 이 서버는 PvE 다). 그래서 지금의 명중은
  사거리뿐이고, 대상 판정은 `bullets.gd` 의 `hit_test` **자리만** 비어 있다 — 동물을
  만드는 바퀴가 거기에 함수를 꽂으면 된다.
- 검증은 `game/qa/qa_bullets.gd` — 코어(즉시판정이 아닌지 / 사거리 / **물은 안 막고
  사람은 못 들어가는지**(같은 지도 같은 칸으로 나란히 묻는다) / **꽂아준 벽은 막되
  한 칸짜리를 건너뛰지 않는지** / 스냅되지 않은 원본 각도 / 정조준·반동 / 탄퍼짐
  재현성 / 명중 판정 자리)와 화면(총을 들었을 때만 선이 뜨는지, 정조준에 따라 번지는지,
  총알이 실제로 날아가는지, **물가에서 물 건너로 쏜 총알이 바다를 지나가고 같은 자리에서
  사람은 물에 못 들어가는지**).

- **PvP 는 지금 범위가 아니다 — 이 세션 서버는 무조건 PvE 다** (2026-09-07 사람 결정).
  플레이어끼리는 서로 쏠 수 없고 총알이 사람을 통과한다. **PvP 는 나중에 별도의
  "PvP 섬"을 만들 때 다시 설계한다.**
  - **그렇다고 PvP 공정성 규칙을 지금 버리지 않는다.** 「카메라 / 해상도」의 해상도
    공정성, 「시야 / 전장의 안개」의 "서버가 보내지 않은 것은 그릴 수 없다", 「장비 /
    제작 결과물 설계 원칙」의 포인트 예산제는 전부 그대로 지킨다 — 이것들은 지금
    지키는 비용이 거의 0 이고 나중에 고치는 비용이 매우 크다는 이유로 들어온 규칙이라,
    PvP 가 뒤로 밀렸다고 근거가 사라지지 않는다.
  - 「무엇을」의 한 줄 피치에 있는 PvP 는 **장기 방향**이고, 지금 만드는 서버의 범위가
    아니라는 뜻이다.

- **목장에 풀어놓은 가축은 데미지를 받지 않는다** (2026-09-07 사람 결정 — "어렵게 잡았는데
  한 번에 죽으면 슬프잖아"). 야생 동물은 그대로 맞는다 — **포획되어 목장에 들어간
  순간부터** 무적이다. 근거: 포획은 사냥 → 체력 10% 아래로 깎기 → 마취탄이라는 긴
  과정을 거치는데, 그 결과물이 오발 한 방에 사라지면 그 과정을 다시 하고 싶지 않게 된다.

#### 총기 스탯 (1차 초안 — 임의로 정함, 밸런스는 나중에 조정)
지금은 총 하나만 있다. **기본 소총**:
- 사거리: 800 (게임 단위 — 화면에서 대략 절반 정도를 커버하는 거리감)
- 데미지(기본탄/마취탄 공통): 25
- 연사 속도: 초당 2발 (사격 간격 0.5초)
- 탄퍼짐: 멈춰서 쏘면 거의 없음, 이동하면서 쏘면 커짐
- 반동: 연속 사격 시 **조준이 흐트러지고**(조준선이 흔들리고 흐려진다), 사격을 멈추면
  빠르게 원래대로. (**2026-09-07 정정** — 전에는 "조준선이 위로 살짝 튀고"였다. 그건
  화면 위쪽이 곧 먼 곳인 FPS 기준이고, **탑다운에서는 "위"가 총구 방향이 아니다.**
  같은 날 정한 조준선(가만히 있으면 선명해지는 빨간 선)과 같은 축에서 다룬다 — 쏘면
  정조준이 풀리고, 멈추면 다시 모인다.)
- 아래 사슴 최대 체력(100)과 맞춰서 정한 숫자다 — 4방이면 처치, 마취탄으로는 체력 10(=10%) 아래일 때 포획 가능.
- **탄창**: 8발. 다 쏘면 좌클릭해도 발사되지 않고, **R키로 재장전**한다(약간의 시간 소요).
  예비 탄약(재장전 횟수 제한)은 아직 없다 — R을 누르면 항상 가득 재장전된다(범위 밖: 예비
  탄약을 아이템으로 관리하는 것). 탄창에 남은 발수는 총을 들고 있을 때만 화면에 표시한다.
- **기본탄과 마취탄은 서로 다른 탄창(잔여 발수)을 따로 관리한다.** 우클릭으로 탄종을
  바꾸는 것은 "다른 탄창으로 갈아 끼우는 것"과 같다 — 기본탄을 다 쓰고 마취탄으로
  바꿔도 마취탄 탄창은 그대로 가득 차 있어야 한다(하나의 탄약 수를 공유하면 안 됨).

(2026-09-07, INBOX #35 — 위 탄창 항목을 실제로 만들면서 정한 구체값이다. 원본은
`game/scripts/gun_ammo.gd`(탄창 코어) / `game/scripts/gun_hud.gd`(총 UI) 이고,
여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 탄창 | **탄종마다 8발** (`gun_ammo.gd` 의 `MAG_SIZE`) |
| 재장전 시간 | **90틱 = 1.5초** (`RELOAD_TICKS` — 연사 간격 0.5초의 세 배) |
| 탄종 전환 | **즉시**. 진행 중이던 재장전은 취소된다 |
| 총 UI 자리 | 하단 핫바 **바로 위**, 화면 가운데 (`world.tscn` 의 `HUD/Layout/GunHud`) |

- **재장전 1.5초를 고른 근거**: 「총기 스탯」이 "약간의 시간 소요"라고만 정해둔 값이다.
  연사 간격(0.5초)의 세 배로 잡았다 — 그보다 짧으면 쏘는 리듬이 안 끊겨 탄창이 있으나
  마나가 되고, 훨씬 길면 8발을 4초에 비우고 재장전을 더 오래 기다리게 되어 사냥이
  기다림이 된다.
- **탄종을 바꾸면 진행 중이던 재장전이 취소된다.** 전환이 곧 "다른 탄창으로 갈아
  끼우는 것"이라(위), 손에서 내려놓은 탄창이 저 혼자 채워지고 있으면 안 된다.
- **탄창 상태는 저장하지 않는다.** 나갔다 들어오면 두 탄창 다 가득이다 — 예비 탄약이
  없어 R 한 번이면 되돌아가는 값이라 저장 포맷을 늘려서 지킬 이유가 없다(날아가는
  총알을 저장하지 않는 것과 같은 판단이다, 위 「전투」).
- **남은 발수와 든 탄종은 플레이어 코어(`player_motion.gd` 의 `gun`)가 들고 있다** —
  화면 상태가 아니라 **플레이어 상태**다(「서버 권위」: 서버가 "이 발사가 유효한가"를
  판정하려면 알아야 한다). 총을 잠깐 내려놔도 코어에 그대로 남는다 — 없어지면 도로
  채워지는 꼴이 된다.
- **탄종 id 는 아이템 id 그대로다**(`ammo_basic` / `ammo_tranq`). 총 UI 가 이름과 색을
  `item_types.gd` 에서 그대로 읽으므로 두 곳이 어긋날 자리가 없고, 나중에 탄약
  아이템을 실제로 소비하게 될 때(「새 테크 라인」 — 지금은 범위 밖) 이어붙일 자리가
  이미 맞아 있다. **지금 총은 탄약 아이템을 쓰지 않는다.**
- **R / 우클릭은 좌클릭과 같은 길로 간다** — 키는 `world.gd` 의 `_unhandled_input`
  한 곳에서 받아 **입력**(`player_input.gd` 의 `reload` / `switch_ammo`)에 실리고,
  코어는 눌린 틱 하나만 표시하며, **총을 들었는지는 인벤토리를 가진 쪽이 본다**
  (「서버 권위」의 "클라이언트가 '나는 총을 들었다'고 주장할 자리가 없다").
- **탄창이 비었거나 재장전 중이어도 사용 모션은 그대로 나온다** — 모션은 손에 든 것을
  모르는 코어가 틀고(「생활 스킬 — 채집 계열」의 "대상이 없어도 사용 모션은 나온다"),
  총알을 만드는 것은 인벤토리를 아는 쪽이다. 못 쐈다는 것은 총 UI 의 빈 탄창이 말한다.
- **총 UI 는 조준선과 같은 한 줄에서 켜고 꺼진다**(`world.gd` 의 `_process`) — 총을
  들었을 때만 뜨고, 다른 도구·빈손·창이 열린 동안에는 사라진다. 따로 갈리면 총을
  내렸을 때 한쪽만 남는다. 「인벤토리 / 장비」의 "상시로 두는 것은 핫바 하나다"는
  그대로다(이건 상시가 아니다).
- **재장전은 탄창 칸이 왼쪽부터 하나씩 차오르는 것으로 보인다** — 진행 막대를 따로
  두지 않는다. 탄창이 채워지는 그림 자체가 진행도이고, 다 차오른 순간이 곧 다시 쏠
  수 있게 되는 순간이라 둘이 어긋날 자리가 없다.
- 검증은 `game/qa/qa_gun_ammo.gd` — 코어(8발 / 9발째가 안 나가는지 / 재장전이 즉시가
  아닌지 / **탄종마다 탄창이 따로인지** / 전환이 재장전을 취소하는지 / 눌린 틱 하나에만
  먹는지 / 연사 초당 2발)와 화면(총을 들었을 때만 UI 가 뜨는지, 쏘면 칸이 줄어드는지,
  빈 탄창으로 좌클릭해도 총알이 안 나가는지, R 로 되살아나는지, 탄종을 바꾸면 색이
  바뀌는지 — 전부 캡처의 픽셀로 본다).

### 체력 / 죽음 / 리스폰
(2026-09-07 사람이 정했다. 그전까지는 플레이어 체력이라는 개념 자체가 없었다.)

- **플레이어 최대 체력 100.** 사슴과 같은 값이다 — 밸런스는 나중에 조정하는 임시값이다.
- **리스폰 지점은 월드를 처음 만들 때의 스폰 좌표다.** **침대를 설치하면 그 침대가
  리스폰 지점이 된다**(침대는 「건축 / 방 시스템」에 새로 들어가는 설치물이다).
- **죽으면 그 자리에 「데스드롭 상자」가 생기고, 인벤토리 내용물이 전부 그 안으로 들어간다.**

(2026-09-07, INBOX #36 — 체력과 리스폰을 실제로 만들면서 정한 구체값이다. 원본은
`game/scripts/player_health.gd`(체력 코어) / `game/scripts/player_motion.gd`(죽음 판정과
리스폰) 이고, 여기 표는 그 요약이다. **데스드롭 상자는 2026-09-07 INBOX #37 에 붙었다** —
아래 「데스드롭 상자」.)

| 항목 | 값 |
|---|---|
| 최대 체력 | **100** (`player_health.gd` 의 `MAX_HEALTH` — 사슴과 같은 값) |
| 리스폰 지점 | `player_motion.gd` 의 **`respawn_position` 값 하나** (월드에 들어올 때 스폰 좌표가 들어간다) |
| 죽음 판정과 리스폰이 도는 자리 | 코어의 **고정 틱** — 죽은 다음 틱에 되살아난다 |
| 저장 | **안 한다** — 월드에 들어오면 언제나 가득이다 |

- **리스폰 지점을 값 하나로 둔 이유**: 침대를 설치하면 그 침대가 리스폰 지점이 된다는
  위 규칙이, **그 값 하나를 덮어쓰는 것**(`set_respawn()`)으로 끝나야 하기 때문이다.
  침대는 「건축 / 방 시스템」과 같이 오므로 지금 범위가 아니다.
  **텔레포트(`place_at`)로는 따라가지 않는다** — 옮겨졌다고 리스폰 지점까지 같이
  움직이면 침대의 의미가 없어진다.
- **되살리는 것은 데미지를 주는 쪽이 아니라 코어의 틱이다.** 리스폰은 위치를 옮기는
  시뮬레이션 한 단계라 서버가 도는 틱 위에 있어야 한다 — 밖에서 부르는 함수가 그
  자리에서 위치를 옮기면 그 틱의 이동 계산과 순서가 어긋난다. `take_damage()` 는
  체력을 깎기만 하고, 다음 틱에 코어가 스스로 되살아난다.
- **체력을 저장하지 않는 근거**: **회복 수단이 아직 하나도 없다**(음식의 포만감/버프는
  「아이템 카테고리」에 카테고리만 있고 효과는 범위 밖이다). 저장하면 체력 3 으로
  나간 캐릭터가 영영 체력 3 으로 돌아오고, 되돌리는 유일한 방법이 **일부러 죽는 것**이
  된다 — 그건 「왜」의 힐링 정체성과 정면으로 부딪힌다. **회복 수단이 생기는 바퀴가
  이 결정을 다시 볼 것**(그때는 저장하는 쪽이 맞을 수 있다). 탄창을 저장하지 않는 것
  (「총기 스탯」)과 결론은 같지만 근거는 다르다 — 그건 R 한 번이면 되돌아가는 값이다.
- **체력을 화면에 상시로 표시하지 않는다.** 「인벤토리 / 장비」가 "체력·허기 같은
  것을 여기에 덧붙이지 말 것 — 지금 화면에 상시로 두는 것은 핫바 하나다"라고 이미
  정해뒀다. 체력 표시를 실제로 넣는다면 그 절을 먼저 고쳐야 한다.
- **아직 게임 안에서 죽을 방법이 없다** — 동물이 없고 이 서버는 PvE 라 사람도 서로
  안 맞는다(「전투」). 그래서 자체 QA 가 코드로 데미지를 준다. 실제로 죽는 일은 동물이
  생긴 뒤부터이고, 그때 `take_damage()` 를 부르는 것은 **서버**다(「서버 권위」의
  "명중 판정은 전부 서버가 계산한다") — `bullets.gd` 의 `hit_test` 자리가 그 입구다.
- **인벤토리를 옮기는 것은 코어가 아니다** (2026-09-07, INBOX #37). 코어는 애초에
  인벤토리를 모르므로, 죽은 틱에 `respawned`(그 틱 하나에만 참)와 `death_position`
  (되살아나기 **전에** 적어둔 죽은 자리)만 남긴다 — 그걸 보고 상자를 만들어 채우는
  것은 **인벤토리를 아는 쪽**(`world.gd`)이다. 총알·재장전과 같은 자리·같은 이유다
  (「서버 권위」).
- 검증은 `game/qa/qa_health.gd` — 코어(최대 체력 / 데미지 계산 / **죽은 다음 틱에**
  리스폰 / 리스폰 지점이 값 하나인지 / 살아 있으면 리스폰하지 않는지 / 죽으면 쓰던
  모션이 끊기는지)와 화면(들어올 때 가득인지, 리스폰 지점이 스폰 칸인지, **다쳐도
  새 HUD 가 안 생기는지**, 멀리 떠났다 죽으면 스폰 화면으로 돌아오는지 — 캡처의
  픽셀을 견줘서 본다).

#### 데스드롭 상자
- **타이머가 다 되면 상자와 내용물이 함께 사라진다.** 기본 **30분**.
- **이 시간은 월드 설정에서 바꿀 수 있다**(아래 「월드 설정」). 근거: 죽으면 짐을 잃는
  방식 자체를 불쾌해하는 사람이 많다 — 이 게임의 1차 정체성은 힐링이라(「왜」), 그
  강도를 사람마다 정할 수 있어야 한다.
- **상자를 열면 타이머가 멈춘다.** 근거: 29분 55초에 열어서 아이템을 옮기는 중에 상자가
  사라지면, 규칙이 공정하든 말든 그냥 불쾌하다. **정해진 시간은 "찾아올 시간"이지
  "꺼낼 시간"이 아니다.**
  - 창을 닫으면 타이머가 다시 흐른다. (**열어둔 채 멀리 떠나면** 상자가 영영 안 사라지므로,
    플레이어가 상자에서 충분히 멀어지면 닫힌 것으로 친다 — 2026-09-07 세션 판단이고,
    사람이 다르게 원하면 바꾼다.)
- **일반 상자와 다른 오브젝트다** — 「건축」의 설치형 상자는 타이머가 없다.
- **다른 플레이어도 남의 데스드롭 상자를 열 수 있다** (2026-09-07, INBOX #37 —
  그전까지 미정이었다). 근거 셋:
  1. **이 서버는 PvE 이고**(「전투」) **방 코드를 아는 사람만 들어오는 협동 방이다**
     (「멀티플레이 — 세션 서버 아키텍처」, 동시 접속 4~8명). 낯선 사람의 약탈을 막는
     일은 방 코드가 이미 하고 있다 — 그걸 상자에서 한 번 더 막을 이유가 없다.
  2. **막으면 「인벤토리 안전」과 부딪힌다.** 죽은 사람이 그대로 접속을 끊으면 파티원이
     짐을 지켜줄 방법이 아예 없어져서, 타이머가 그대로 **아이템 소멸**이 된다.
  3. 파티원이 대신 주워다 주는 것은 PvE 협동에서 자연스러운 행동이다.
  - **구현은 "상자에 주인을 적어두지 않는 것"이다** — 여는 쪽을 막는 코드가 아예 없다.
  - **PvP 섬을 만드는 시점에는 이 결정을 다시 본다**(「전투」의 PvP 항목) — 거기서는
    남의 데스드롭이 곧 전리품인지가 별도 설계다.

(2026-09-07, INBOX #37 — 실제로 만들면서 정한 구체값이다. 원본은
`game/scripts/death_boxes.gd`(상자 코어) / `game/scripts/world_settings.gd`(타이머 설정)
이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 상자 칸 수 | **27칸** (인벤토리 일반 18 + 장비 9 — 통째로 들어와야 한다) |
| 상자 크기 | **48 × 42** (아트 16 × 14px × 씬 스케일 3배 — 타일 한 칸 폭, 플레이어 51px 의 8할) |
| 여는 거리 | **72** (타일 1.5칸, `OPEN_RADIUS`) |
| 열어둔 상자가 「닫힌 것」이 되는 거리 | **480** (타일 10칸, `CLOSE_DISTANCE`) |
| 고를 수 있는 타이머 | **5 / 10 / 30 / 60 / 180분 / 사라지지 않음** (기본 30분) |

- **좌클릭으로 연다.** 「조작」이 *"별도의 「상호작용 키」(예: F)는 두지 않는다"* 고
  정해뒀으므로, 좌클릭 하나가 그 자리도 겸한다 — **커서가 상자 위에 있고 `OPEN_RADIUS`
  안에 서 있으면** 그 클릭은 상자를 여는 클릭이고 도구로 가지 않는다. 우선순위를 상자에
  준 이유는 간단하다: 상자 위를 겨눈 좌클릭이 도끼질이 되면 **상자를 영영 못 연다.**
- **꺼내기만 되는 상자다 — 넣는 길은 두지 않는다.** 30분 뒤 내용물과 함께 사라지는
  상자에 물건을 넣게 만들면 「인벤토리 안전」의 *"아이템이 조용히 사라지면 안 된다"* 와
  정면으로 부딪힌다. (「건축」의 설치형 상자는 타이머가 없으니 그쪽은 양방향이다.)
- **꺼내는 경로는 `inventory.take_in(뭉치)` 하나뿐이다** — 인벤토리가 꽉 차 있으면
  들어가는 만큼만 들어가고 **못 들어간 몫은 상자에 그대로 남는다**(「인벤토리 안전」).
  「모두 가져가기」도 칸마다 같은 함수를 부르는 것뿐이다.
- **빈 상자는 월드에 남지 않는다** — 빈손으로 죽으면 아예 안 생기고, 다 꺼내면 그
  자리에서 사라진다. 찾아올 것이 없는 상자는 그냥 쓰레기다.
- **타이머는 벽시계로 잰다** — 남은 시간과 마지막으로 적은 시각을 저장하고, 불러올 때
  그 차이만큼 한 번에 깎는다. 「시뮬레이션 구조」의 *"서버를 다시 켰을 때 `지금 시각 -
  마지막 저장 시각`만큼 큐를 앞으로 감으면 끝난다"* 그대로라, **빨리 감는 코드가 따로
  없다.** 나가 있는 동안 시간이 다 된 상자는 아예 안 살아난다.
- **불러온 상자는 전부 닫힌 채로 시작한다.** 열린 채로 살아나면 타이머가 멈춘 상자가
  되어 영영 안 사라진다(위 "멀리 떠나면 닫힌 것으로 친다"와 같은 이유).
- **상자를 만드는 시점의 타이머 값을 새겨둔다** — 나중에 월드 설정을 바꿔도 **이미 놓인
  상자의 남은 시간은 안 흔들린다**(아래 「월드 설정」).
- **그림은 구워진 도트 스프라이트다** (2026-09-07, INBOX #40 — 그전에는 `draw_rect`
  로 그린 자리표시였다). 만드는 코드는 `game/tools/gen_box.py` 이고, 아래 표가 그 요약이다.

  | 항목 | 값 |
  |---|---|
  | 시트 | `death_box.png` — **아트 16 × 14px 칸 × 2프레임**(닫힘 · 열림) |
  | 배율 | **3배** — 캐릭터·타일·바닥 아이템과 같다(`death_box_node.gd` 의 `DOT`) |
  | 재질 | **나무(`helve`) · 쇠(`blade`) 둘뿐** — 도구 자루·날과 **같은 램프**다 |
  | 코드로 남는 것 | 납작한 그림자 + **남은 시간 막대**(값에 따라 길이가 변하는 UI 다) |

  - **크기는 그림 쪽에서 고른 값이 아니다.** 48 × 42 는 위 표가 이미 정해뒀고, 씬
    스케일이 3배라 아트가 16 × 14px 로 떨어진다 — **도구 아이콘(17px)·바닥 그림(12px)과
    달리 칸이 정사각형이 아닌 첫 자산**이다(`qa_sprite_check.py` 에 `cell_h` 가 그래서
    생겼다). 짚는 자리(`box_rect`)는 그대로라 클릭 판정은 한 줄도 안 바뀌었다.
  - **재질을 새로 늘리지 않고 도구의 나무·쇠 램프를 쓴다.** 그 둘은 「캐릭터
    애니메이션」의 *"도구 색은 커스터마이징을 따라가지 않는다"* 로 이미 고정색이고,
    상자도 같아야 한다 — 옷색을 바꿨다고 남의 데스드롭 상자 색이 달라지면 안 된다.
    덤으로 바닥에 떨어진 도끼와 **정확히 같은 나무색**이 되어 나란히 놓여도 안 어긋난다.
  - **열려 있는 것은 뚜껑이 젖혀진 프레임으로 보인다**(INBOX #40 (3) 이 판단에 맡긴
    것이다). 전에는 테두리에 밝은 띠를 둘러서 알렸는데, 그건 그림이 아니라 월드 위에
    뜬 UI 로 보였다 — **타이머가 멈춰 있다는 것을 그림 자체가 말해야 한다.**
  - **「건축」의 설치형 상자와 실루엣을 나눠 쓰지 않는다**(INBOX #40 (4) 의 미정을
    여기서 정한다). 근거: 둘은 규칙이 정반대다 — 데스드롭은 **타이머가 있고 꺼내기만**
    되고, 설치형은 타이머가 없고 양방향이다. 같은 그림이면 플레이어가 *"여기 넣어도
    되나"* 를 그림으로 구별할 수 없다. **남은 시간 막대로는 갈리지 않는다** — 「사라지지
    않음」을 고르면 그 막대가 아예 안 그려지기 때문이다(위 `_draw_bar`). 설치형 상자를
    만드는 바퀴가 **다른 실루엣**을 만든다(이쪽은 뚜껑이 둥근 궤짝이니 평평한 뚜껑 등).
- 검증은 `game/qa/qa_death_box.gd` — 코어(만료 / **열면 멈춤** / **멀어지면 닫힘** /
  「인벤토리 안전」 / 「사라지지 않음」 / 저장 왕복과 오프라인 / 짚는 자리)와
  화면(죽은 자리에 생기는지, 인벤토리가 통째로 들어가는지, 좌클릭으로 열리는지,
  열어둔 동안 실제로 시간이 멈추는지, 모두 가져가면 되돌아오고 상자가 사라지는지).

### 월드 설정
(2026-09-07 사람 결정. 지금 「설정」 화면에는 해상도 같은 **그래픽 옵션만** 있는데,
그것과 성격이 다른 "이 월드가 어떻게 굴러가는가"를 두는 자리다.)

- 그래픽 설정이 `user://settings.json` 에 기계 단위로 저장되는 것과 달리, **월드 설정은
  그 월드(슬롯)에 저장된다** — 같은 사람이 월드를 여러 개 만들면 서로 다를 수 있다.
- 지금 있는 항목: **데스드롭 상자 타이머**(기본 30분).
- **멀티플레이 대비**: 월드 설정은 그 월드를 여는 호스트의 것이고, 접속한 사람이 각자
  바꿀 수 있는 값이 아니다 — 「서버 권위」에 따라 서버가 들고 판정에 쓴다.

(2026-09-07, INBOX #37 — 실제로 만들면서 정한 구체값이다. 원본은
`game/scripts/world_settings.gd`(값) / `game/scripts/world_settings_screen.gd`(화면)
이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 여는 곳 | **일시정지 메뉴**의 「월드 설정」 (「설정」 바로 아래) |
| 저장 자리 | 슬롯의 `world_settings` (인벤토리 `inventory` 옆) |
| 데스드롭 상자 타이머 | 5 / 10 / 30 / 60 / 180분 / **사라지지 않음** (기본 30분) |

- **「사라지지 않음」을 고를 수 있다.** 이 설정을 둔 근거가 *"죽으면 짐을 잃는 방식
  자체를 불쾌해하는 사람이 많다 … 그 강도를 사람마다 정할 수 있어야 한다"* 이므로,
  강도의 한쪽 끝이 없으면 그 문장이 반만 지켜진다. 반대쪽 끝(5분)도 같은 이유로 둔다.
- **바꾼 값은 다음에 생기는 상자부터 적용된다.** 이미 놓인 상자의 남은 시간을 설정이
  거슬러 올라가 흔들지 않는다 — 「무제한 → 30분」처럼 남은 시간을 새로 지어내야 하는
  경우가 생기고, 그러면 "내 상자가 몇 분 남았는가"를 플레이어가 예측할 수 없게 된다.
- **화면은 「설정」(해상도)과 같은 방식으로 월드 위에 겹쳐 띄운다**(씬 전환이 아니다 —
  씬을 바꾸면 월드가 내려간다). Esc 는 **가장 안쪽 창부터** 규칙 그대로 이 화면만
  닫고 일시정지 메뉴를 남긴다.
- 검증은 `game/qa/qa_death_box.gd` 가 함께 본다(저장 왕복 + 일시정지 메뉴에서 열려서
  바꾼 값이 슬롯에 적히는지).

### 생활 스킬 — 채집 계열
- **사냥**: 총으로 동물을 공격해서 잡는다.
- **벌목**: 도끼로 나무(logging_point)를 벤다. 목재를 드롭한다.
- **채광**: **곡괭이**로 채광 포인트를 캔다.
- **채집 + 수확**: **낫**으로 채집 포인트와 밭의 다 자란 작물을 벤다.
  (**2026-09-07 사람이 곡괭이낫을 곡괭이와 낫으로 쪼갰다.** 이전 결정은 "채광 포인트를
  나중에 광산으로 확장하면서 그 안에 추가 채집물도 놓을 계획이라 처음부터 합쳐둔다"
  였다. 쪼갠 대가는 그대로 남는다 — **광산 안에서 곡괭이와 낫을 번갈아 들어야 한다.**
  그게 번거로워지면 그때 다시 판단한다.)
  - **손으로 줍는 채집(장갑 같은 별도 도구)은 두지 않는다** — 채집은 낫 하나다.
    맨손 채집을 나중에 따로 넣고 싶으면 그때 도구를 추가한다(아래 「새 도구를 추가하는
    절차」).
- **낚시**: 낚싯대. **물에 던져야 한다.**
  (**2026-09-07 사람이 다시 정했다** — 이전에는 "바다의 「낚시 스팟」에서만"이었다.
  **「낚시 스팟」이라는 별도 오브젝트는 두지 않는다.** 물 자체가 낚시터다.)
  - **좌클릭으로 던진다. 던지는 거리는 마우스로 조절한다** — 커서가 멀면 멀리, 가까우면
    가깝게. 커서까지의 거리를 아래 최소~최대로 잘라서 쓴다.

    | 항목 | 값 |
    |---|---|
    | 최소 거리 | **바로 앞**(플레이어가 선 칸의 바로 다음 칸) |
    | 최대 거리 | 구현할 때 정하고 여기 적는다. **화면 안이어야 한다** — 안 보이는 곳에 던지면 찌가 어디 떨어졌는지 알 수 없다 |

  - **찌가 떨어진 칸이 물이어야 낚시가 시작된다.** 땅에 떨어지면 아무 일도 일어나지
    않는다(줄만 회수한다). **"물 위인가"는 찌가 떨어진 칸으로 판정하지, 플레이어가
    선 자리로 판정하지 않는다** — 물가에 서 있어도 땅으로 던지면 안 되고, 반대로
    멀리 있는 물에 닿으면 된다.
  - **그래서 낚싯대는 "대상이 없어도 사용 모션이 나오는" 규칙의 예외다**(위 도구 표).
    다른 도구는 나중에 근접무기로 확장할 계획이라 허공에 휘둘러도 모션이 나와야 하지만,
    낚싯대는 그 계획이 없고 **"물에서만 되는 행동"이라는 게 낚시의 정체성**이라 아무
    데서나 던지는 모션이 있으면 오히려 혼란스럽다.
  - **서식지에 따라 잡히는 물고기가 달라진다** (2026-09-07 사람 결정 — 방향만 정해둔다).
    지금은 지형이 땅/바다 2종뿐이라 **구분할 서식지가 없다.** 물의 종류나 지역이
    늘어나는 시점(민물/깊은 바다/동굴 물 등)에 그 구분을 붙이고, 어떤 물에서 무엇이
    잡히는지를 여기 표로 적는다. **지금 물고기 종류를 지어내지 말 것** — 서식지가
    생기기 전에 종류부터 만들면 나눌 기준이 없어 전부 같은 물에서 나온다.
- **도구는 전부 인벤토리에 들어가는 아이템이고, 핫바에서 선택해서 손에 들어야 쓸 수
  있다.** 현재 목록은 아래 표다 (2026-09-07 사람이 4종 → 7종으로 늘렸다).

| 도구 | 쓰임 | 대상 없이 휘두르기 |
|---|---|---|
| **총** | 사냥 / PvP | O |
| **도끼** | 벌목 | O |
| **곡괭이** | 채광 | O |
| **낫** | 채집 + 수확 | O |
| **괭이** | 밭 일구기 (아래 「생산 계열」) | O |
| **물뿌리개** | 밭에 물 주기 | O |
| **낚싯대** | 낚시 | **X — 찌가 물에 떨어져야만** |

- **낚싯대를 뺀 나머지는 대상이 없어도(허공에 대고) 좌클릭하면 항상 사용 모션이 나와야
  한다.** 근거: 나중에 이 도구들에 공격력을 부여해서 근접무기로도 쓸 계획이라, "유효한
  대상이 있을 때만 모션이 나가는" 방식이면 근접 공격 자체가 성립하지 않는다.
  낚싯대만 예외인 이유는 위 「낚시」 항목에 있다.
- **이 목록은 계속 늘어난다.** 새 도구를 붙이는 절차는 아래 「새 도구를 추가하는 절차」
  에 있다 — 도구를 추가하는 바퀴는 그 절을 그대로 따른다.

#### 예시 자원
- 작물(채집으로 얻는 씨앗 대상): **벼** (곡물)
- 동물(사냥/포획 대상): **사슴** (최대 체력 100 — 위 총기 스탯과 맞춰 정함). 마취탄으로
  체력 10% 미만일 때 맞으면 생포(목장행), 기본탄으로 죽이면 **고기**(육류)가 드롭된다.
- 나무(벌목 대상): 목재 드롭.
- 광물(채광 대상, 필드에서 확률적으로 섞여 나옴 — **돌이 가장 흔하고, 철광석, 유황광석
  순으로 희귀해짐**): **돌**, **철광석**, **유황광석**. 정확한 확률은 구현 재량.
- 채소는 아직 구체적인 작물이 정해지지 않았다(카테고리만 확정 — 아래 "아이템 카테고리"
  참고). 나중에 별도 지시로 추가.
- 채집/채광 포인트는 다 캐면 회색으로 표시하는 대신 **완전히 사라졌다가**, 쿨다운이 끝나면
  다시 나타난다.

### 아이템 획득 방식 — 바닥 드롭
- 사냥/벌목/채집/채광으로 얻는 아이템은 곧바로 인벤토리에 들어가지 않는다. 대상 위치에
  아이템이 **바닥에 드롭**되고, 플레이어가 가까이 다가가면 자동으로 주워서 인벤토리에
  들어간다 (따로 습득 버튼은 없음 — 좌클릭은 도구 동작에 쓰므로 습득은 접촉으로 처리).
- **인벤토리 창 밖으로 끌어다 버린 것도 같은 바닥 아이템이 된다**(「인벤토리 / 장비」의
  버리기). 버린 자리에 놓이고, 다시 다가가면 주워진다.
- **바닥 아이템은 저장된다** (2026-09-07 추가 — 그전까지 정한 바가 없었고, 실제로 월드를
  나가면 사라졌다). 월드를 나갔다 들어와도 버린 자리에 그대로 있어야 한다. 근거:
  「인벤토리 안전」이 "아이템이 조용히 사라지면 안 된다"고 못박은 것과 같은 이유다 —
  버리기는 **버리는 행동이지 삭제가 아니다.**
- **바닥 아이템은 놓인 지 24시간이 지나면 사라진다** (2026-09-07 INBOX #33 에서 정함 —
  그전까지 미정이었다). **오프라인을 포함한 실제 시간**이고, 그래서 「시뮬레이션 구조」의
  "서버를 다시 켰을 때 `지금 시각 - 마지막 저장 시각`만큼 큐를 앞으로 감으면 끝난다"가
  저절로 성립한다(불러올 때 빨리 감는 코드가 따로 없다 — 놓인 시각만 저장한다).
  값을 24시간으로 고른 이유:
  - **한 세션보다 훨씬 길어야 한다.** "여기 잠깐 내려놓고 나중에 가져가야지"가 벌이 되면
    위의 "버리기는 버리는 행동이지 삭제가 아니다"와 부딪힌다.
  - **하루를 넘기면 안 된다.** 다음 날 다시 들어왔을 때 어제 흘린 것이 그대로 남아 있으면
    월드가 지저분해진다 — 그게 이 타이머가 있는 이유다.
  - **「데스드롭 상자」의 30분과 다른 값인 이유**: 그건 죽음 페널티의 강도(= 찾아올 시간)이고
    이건 청소 주기다. 같은 값을 쓸 이유가 없다.
- **줍히는 거리는 36**(타일 48의 0.75칸). 아이템 위를 지나가면 주워지되 옆 칸을 스쳐
  지나갈 때 딸려오지는 않는 정도다.
- **버린 사람이 그 자리를 벗어나야 다시 주워진다** (2026-09-07 INBOX #33). 시간 유예가
  아니라 **거리**로 잠그는 이유는, 시간으로 하면 버려놓고 그 자리에 서 있을 때 몇 초 뒤에
  도로 주워져서 **버릴 수가 없는** 상태가 되기 때문이다. 거리로 하면 그런 경우가 아예
  생기지 않는다. 월드를 다시 불러온 직후에도 잠긴 채로 시작한다 — 나갈 때 아이템 위에
  서 있었다고 들어오는 순간 도로 주워지면 그것도 버리기를 되돌리는 것이다.
  - **여기서 「그 자리」는 놓인 자리가 아니라 버린 사람이 서 있던 자리다**
    (2026-09-07, INBOX #42 — 아래에서 버린 것이 앞 한 칸에 놓이면서 둘이 갈렸다).
    발밑에 놓이던 동안에는 같은 점이라 구별할 필요가 없었고, 위 문장이 원래 말하는
    쪽이 이쪽이다. 원본은 `ground_items.gd` 의 `lock_origin` 이다.
    **저장하지는 않는다** — 불러온 것의 기준점은 놓인 자리다(위 "불러온 직후에도
    잠긴 채로 시작한다"가 그대로 성립한다).

- **버린 것은 발밑이 아니라 바라보는 방향 앞 한 칸에 놓인다** (2026-09-07, INBOX #42 —
  그전에는 플레이어 좌표 그대로였다). 위 *"플레이어 근처 바닥에 드롭"* 안에서 그 「근처」를
  구체화한 것이다.
  - **근거는 「보이는가」다.** 발밑에 놓으면 바닥 그림(30~36px)이 캐릭터(51px) 뒤에 통째로
    숨어서, "버려지긴 했나"를 확인할 방법이 그 자리를 벗어나는 것뿐이다. 실제로 자체 QA
    (`qa_ground_items.gd`)가 **"버린 자리에 목재가 안 보인다"로 먼저 잡았고**, 그림을 보는
    단계마다 아이템을 옆으로 밀어놓고 봐야 했다 — 검사가 그런 우회를 해야 한다는 것 자체가
    플레이어도 못 본다는 뜻이다.

  | 항목 | 값 |
  |---|---|
  | 놓이는 자리 | 바라보는 방향 **48**(타일 한 칸) 앞 — `player_motion.gd` 의 `DROP_DISTANCE` |
  | 방향 | **스냅된 4방향**(그림이 보는 쪽). 조준 각도 그대로가 아니다 |
  | 물 위 | **안 놓는다** — 한 걸음(12)씩 나아가며 **땅인 마지막 자리**에 놓는다 |
  | 잠금 기준 | 놓인 자리가 아니라 **버린 사람이 서 있던 자리**(위) |

  - **방향을 스냅된 4방향으로 고른 이유**: 플레이어가 화면에서 보는 것은 스냅된 그림이다
    (「조작」의 4방향 시트). 원본 각도로 놓으면 캐릭터는 아래를 보는데 물건은 비스듬히
    떨어져서, **어디로 버려지는지를 그림이 말해주지 못한다.** 총알·시야 콘이 원본 각도를
    쓰는 것(「전투」)과 갈리는 자리다 — 그쪽은 정확도가 값의 의미이고, 이쪽은 "보는 쪽에
    놓인다"가 값의 의미다.
  - **48 을 고른 이유**: 줍히는 거리(36)보다 커야 **서 있는 자리와 놓인 자리가 확실히
    갈린다.** 타일 한 칸이라 나중의 격자 건축과도 어긋나지 않고, 캐릭터(51px) 바로 앞이라
    한 걸음이면 닿는다.
  - **물 위에 안 놓는 이유**: 바다는 걸어 들어갈 수 없으므로(「플레이어 이동」) 물 위의
    아이템은 **영영 못 줍는다** — 그건 「인벤토리 안전」의 *"아이템이 조용히 사라지면
    안 된다"* 와 같은 말이다. **물인지는 몸통 상자가 아니라 떨어진 칸으로 판정한다**
    (놓이는 것은 걸어다니는 몸이 아니라 점 하나다 — 「낚시」의 *"물 위인가는 찌가 떨어진
    칸으로 판정한다"* 와 같은 자리다). 한 걸음을 타일(48)의 1/4 로 둔 것은 총알과 같은
    이유다 — 크면 물 한 칸을 통째로 건너뛴다.
  - **물가에서 물을 보고 버리면 발밑에 놓인다**(땅인 마지막 자리가 제자리다). 그때만
    예전처럼 가려지는데, 여기서 다른 방향을 찾아 돌려놓지 않는 이유는 **어디에 놓일지를
    플레이어가 예측할 수 있어야** 하기 때문이다.
  - **자리를 고르는 것은 코어다**(`player_motion.gd` 의 `drop_position()`) — 나중에
    서버가 "이 버리기가 유효한가"를 화면 없이 판정해야 한다(「서버 권위」).
- **바닥 아이템은 인벤토리 아이콘이 아니라 「바닥용 그림」으로 그린다**
  (2026-09-07, INBOX #38 — 그전에는 아이콘(아트 17px)을 2배로 그려서 화면 34px 이었다).
  놓인 자리에 납작한 그림자가 깔리고, 플레이어와 **같은 Y정렬 묶음** 안에 있어서 위에
  놓인 것은 뒤에, 아래에 놓인 것은 앞에 그려진다.

  | 항목 | 값 |
  |---|---|
  | 배율 | **3배** — 캐릭터·타일과 같다(`ground_item_node.gd` 의 `ZOOM`) |
  | 그림 한 칸 | **아트 12px** → 화면 36px (`gen_character.py` 의 `GROUND_N`) |
  | 그림이 없는 아이템 | 코드로 그린 **자리표시 더미** 아트 10 × 8px → 화면 30 × 24px |

  - **바꾼 이유는 크기가 아니라 도트 결이다.** 캐릭터(아트 17px)도 타일(아트 16px)도
    씬 스케일이 3배라 아트 픽셀 하나가 화면에서 3px 인데, 바닥 아이템만 2배라 **그
    하나만 2px** 이었다(`docs/STYLE_GUIDE.md` 1번의 정수 배율 규칙은 지켰지만 배율이
    이웃과 달랐다). 화면에서 차지하는 크기는 34px → 36px 로 거의 그대로다.
  - **아이콘을 그대로 3배로 그리는 길은 막혀 있다** — 17px × 3 = 51px 이라 도끼가
    플레이어와 같은 키가 된다. 그래서 **같은 도형을 더 작은 칸(12px)에 다시 굽는다**
    (`ground_icon()` — 형태를 새로 정의하지 않고 길이 값만 줄인다. 손에 쥔 도끼 ·
    칸 안의 도끼 · 바닥의 도끼가 **같은 도구**여야 하기 때문이다).
  - **아이콘이 없는 아이템의 자리표시는 인벤토리 것과 다르다.** 인벤토리는 색 사각형 +
    두 글자인데, 그걸 풀밭에 놓으면 띠와 테두리를 두른 정사각형이라 **떠 있는 궤짝**으로
    보이고 글자는 뜬 UI 로 보인다. 바닥에서는 **작은 더미 하나**로 그린다 — 아이템을
    가르는 것은 여전히 **색 하나**라 창 안팎에서 같은 아이템이 같은 색이다. 원재료
    아이콘이 생기는 바퀴가 그 아이템에 바닥용 그림을 붙이면 저절로 안 쓰인다.

### 인벤토리 / 장비
- **일반 슬롯 18칸.** 그중 **맨 위 9칸이 핫바**를 겸한다 — 숫자키 1~9로 그 칸의
  아이템을 손에 든다(코어키퍼 방식: 핫바가 인벤토리와 분리된 별도 UI가 아니라 인벤토리의
  일부).
- **장비 슬롯 9칸** (일반 18칸과는 별개): 모자 1, 상의 1, 하의 1, 신발 1, 목걸이 2, 반지 2,
  가방 1. 장비 슬롯에 넣은 아이템이 실제로 어떤 효과를 주는지는 아직 안 정해짐 (범위 밖).
- E 키로 인벤토리 창을 열고 닫는다.
- **화면 아래에 핫바가 항상 보인다** (2026-09-07 사람 결정 — 마인크래프트/코어키퍼 방식.
  이전 결정은 "상시 HUD는 없다, 확인은 E키 창으로만"이었다. 바꾼 이유: **지금 무엇을
  들고 있는지 안 보이면 도구 기반 조작이 성립하지 않는다** — 좌클릭 하나로 총·도끼·
  곡괭이가 전부 갈라지는데(「조작」), 든 것을 확인하려고 매번 E 를 눌러야 하면 그
  설계가 무너진다.)
  - **핫바는 인벤토리 맨 위 9칸을 그대로 비추는 것이지 별도의 보관함이 아니다** —
    위의 "핫바가 인벤토리와 분리된 별도 UI가 아니라 인벤토리의 일부"는 그대로다.
    E 창에서 1번 칸을 바꾸면 하단 핫바도 같이 바뀐다.
  - 고른 칸은 눈에 띄게 표시하고, 빈 칸도 자리를 지킨다(칸이 사라지면 번호와 자리가
    어긋난다). **그 외의 상시 HUD는 여전히 없다** — 체력·허기 같은 것을 여기에
    덧붙이지 말 것. 지금 화면에 상시로 두는 것은 핫바 하나다.
  - (2026-09-07, INBOX #25 — 실제로 만들면서 정한 구체값이다.) 하단 핫바는
    **`inventory_panel.gd` 의 `hotbar_only` 모드**다(`world.tscn` 의 `HUD/Layout/Hotbar`).
    E 창과 **같은 스크립트·같은 코어**라 칸 그림·아이콘·번호·고른 칸 표시가 두 곳에서
    어긋날 수가 없고, 창에서 칸을 옮기면 하단 핫바가 그대로 따라온다.
    - **창(인벤토리/지도/일시정지/설정)이 열리면 핫바를 감춘다.** 인벤토리 창은 같은
      9칸을 자기 맨 윗줄에 이미 그려서 두 벌이 겹치고, 나머지 창은 화면을 어둡게
      덮는데 핫바만 밝게 남으면 "지금 조작이 끊겨 있다"는 신호와 어긋난다.
    - 칸 56px × 9 + 사이 8px, 화면 아래 여백 16px.
- 인벤토리 창이 열려 있을 때, 마우스로 아이템을 **드래그**해서 슬롯 사이(일반↔일반,
  일반↔장비, 핫바 포함)로 옮길 수 있다. 인벤토리 창 **바깥으로 드래그해서 놓으면**
  그 아이템은 인벤토리에서 사라지고 플레이어 근처 바닥에 드롭된다 (버리기).

(2026-09-07, INBOX #23 — 실제로 만들면서 정한 구체값이다. `game/scripts/inventory.gd`
와 `game/scripts/item_types.gd` 가 원본이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 일반 칸 | **18칸** — 맨 위 **9칸이 곧 핫바**(별도 UI 가 아니다) |
| 장비 칸 | **9칸** — 모자1 / 상의1 / 하의1 / 신발1 / 목걸이2 / 반지2 / 가방1 |
| 한 칸 최대 수량 | **도구·장비 1개, 나머지 99개** (`item_types.gd` 의 `MAX_STACK`) |
| 저장 자리 | 슬롯의 `inventory` (탐험 기록 `explored` 옆 — **캐릭터마다 따로**) |
| 아이템 정의 | `game/scripts/item_types.gd` — 이 문서가 이름을 적어둔 것만 있다 |

- **인벤토리 코어는 노드가 아닌 순수 클래스다**(`inventory.gd` — 「시뮬레이션 구조」).
  나중에 「서버 권위」의 "재료 소모 / 제작 완료는 전부 서버가 계산한다"가 이 클래스를
  그대로 부른다. 화면(`inventory_screen.gd` / `inventory_panel.gd`)은 **그리기와 좌표 →
  칸 변환만** 하고, 무엇이 어디로 가는지는 전부 코어의 `move()` / `take_in()` 이 정한다.
- **「인벤토리 안전」을 반환값의 모양으로 지킨다.** 넣는 경로는 `take_in(뭉치)` 하나이고,
  **넣은 만큼만 그 뭉치에서 덜어낸다** — 못 넣은 몫은 호출한 쪽이 들고 있던 뭉치(바닥/
  출력 버퍼/상자에 있는 바로 그 물건)에 그대로 남는다. 호출한 쪽이 반환값을 무시해도
  아이템이 조용히 사라질 수가 없다. 편의용 `add(id, 개수)` 는 **남은 수량**을 돌려준다.
- **드래그는 아이템을 인벤토리에서 빼지 않는다.** 집은 칸을 흐리게 그리고 마우스 자리에
  같은 그림을 하나 더 그릴 뿐이다 — 끌던 중에 창이 닫혀도 사라질 물건이 없다.
- **창 밖에 놓으면(버리기) `ground_items.gd` 로 간다.** 이번 바퀴는 "월드의 이 자리에
  놓였다"는 **데이터까지만**이다 — 바닥 아이템의 그림과 줍기는 「바닥 드롭」을 실제로
  만드는 바퀴가 그 클래스에 붙인다.
- **아이템 아이콘은 생긴 것부터 하나씩 그려진다**(2026-09-07, INBOX #24 — 도끼가
  첫 번째다). `item_types.gd` 의 그 아이템에 `icon` 한 줄이 있으면 칸에 그 그림이
  **정수 배율로** 그려지고, 없으면 예전대로 아이템 색 자리표시 + 두 글자 이름이
  그려진다 — 둘이 섞여 있어도 된다. 아이콘은 「새 도구를 추가하는 절차」 6번대로
  그 도구의 [DESIGN] 바퀴가 만든다.
- **키는 `world.gd` 의 `_unhandled_input` 한 곳에서 정한다**(「조작」의 Esc 규칙과 같은
  자리). E 로 열고 닫고, 숫자키 1~9 는 창이 닫혀 있어도 먹는다(일시정지/설정이 열려
  있을 때만 안 먹는다 — 그때는 조작 자체가 끊긴 상태다). **Esc 는 가장 안쪽 창부터**:
  설정 → 인벤토리 → 지도 → 일시정지 메뉴. 인벤토리가 열려 있는 동안 M 은 아무 일도
  하지 않는다(창을 겹겹이 쌓지 않는다).
- 처음 들어오는 캐릭터에게 넣어주는 것은 `world.gd` 의 `STARTER_ITEMS` 다(도구 7종 +
  테스트용 아이템). **"저장된 적이 없는 캐릭터"에게만** 준다 — 다 버려서 비어 있는
  인벤토리와 구별해야 하기 때문이다.
- 검증은 `game/qa/qa_inventory.gd` — 코어(칸 구성 / 겹치기 / **꽉 찬 인벤토리에서 넘치는
  몫이 안 사라지는지** / 장비 종류 / 저장 왕복)와 화면(E·Esc·M, 숫자키, **실제 마우스로**
  끌어다 놓기, 창 밖에 버리기, 창이 열린 동안 안 움직이는지, 저장했다 불러오기).

### 생활 스킬 — 생산 계열
- **농사** (2026-09-07 사람이 스타듀밸리식으로 구체화 — 이전에는 "씨앗을 들고 좌클릭하면
  심긴다"가 전부였고 밭도 물도 없었다):
  1. **괭이**로 땅을 좌클릭해 **밭으로 일군다.** 일군 타일은 눈에 띄게 달라 보여야 한다.
  2. **씨앗 아이템을 손에 든 상태에서 일군 밭에 좌클릭**하면 심긴다(도구가 아니라
     아이템으로 심는다 — 목장의 "풀어놓기"와 같은 패턴이라 규칙이 갈라지지 않는다).
     **일구지 않은 땅에는 심기지 않는다.**
  3. **물뿌리개**로 밭을 좌클릭하면 물이 준 상태가 된다.
  4. **작물은 물 준 날에만 한 단계 자란다.** 물을 안 주면 그날은 자라지 않을 뿐 **죽지는
     않는다** — 하루 20분짜리 게임에서 접속을 못 하면 밭이 전멸하는 건 「왜」의
     "생활만 즐기려는 플레이어를 버리지 않는다"에 정면으로 어긋난다.
  5. **비가 오면 그날 밭 전체가 자동으로 물 준 상태가 된다**(위 「시간 / 계절 / 날씨」의
     강수). 여름 장마(30%)가 농사에 유리해지는 계절 색이 여기서 나온다.
  6. 물 준 상태는 **날이 바뀌면 마른다.** 다 자란 작물은 **낫**으로 수확한다.
  - **범위 밖(지금)**: 작물별 성장 일수·계절 제한·품질 등급. 실제로 작물을 만드는
    바퀴가 정하고 여기 적는다.
- **목장**: 사냥 후 마취탄으로 포획한 동물을 사육한다. 목장 구역 경계는 실제 충돌체(담장
  등)로 막혀 있어서 동물이 안팎으로 통과할 수 없다 (위치를 강제로 되돌리는 방식이 아니라
  물리적으로 막는다). **풀어놓기**: 포획한 동물 아이템(예: captured_deer)을 핫바에서
  손에 든 상태로 목장 구역에 좌클릭하면 풀어놓아진다 (빈손 좌클릭이 아니라, 씨앗 심기와
  같은 "든 것으로 좌클릭" 패턴).

### 새 도구를 추가하는 절차 (도구가 늘어날 때마다 그대로 따른다)
(2026-09-07 — 사람이 "도구의 종류는 계속해서 추가될 거니까 잘 기록해줘". 도구는 앞으로도
계속 늘어나므로, 매번 다시 판단하지 않도록 순서를 고정한다.)

**도구는 두 단계로 붙인다. 1단계는 도구마다, 2단계는 그 도구가 캘 대상이 생긴 뒤다.**
(**2026-09-07 사람이 쪼갰다** — 이전에는 "도구 하나 = [DESIGN] 한 바퀴 + [BUILD] 한
바퀴"로 도구마다 그림과 동작을 한 쌍으로 묶어뒀다.)

| 단계 | 내용 | 언제 |
|---|---|---|
| **1. 그림과 모션** | 그 도구를 든 캐릭터 + 좌클릭하면 사용 모션이 나오는 것까지 | **지금** — 도구마다 [DESIGN] 한 바퀴 |
| **2. 실제 효과** | 벌목·채광·사냥·낚시·밭일구기가 실제로 무언가를 바꾸는 것 | **대상 오브젝트가 생긴 뒤** — 그 오브젝트를 만드는 바퀴가 같이 붙인다 |

- **왜 쪼갰나**: 도끼의 사용 모션은 지금 만들 수 있지만 **벌목은 월드에 나무가 없으면
  만들 수가 없다.** 도구마다 "그림 → 동작"으로 묶으면 도구 6종이 전부 대상 오브젝트를
  기다리며 멈춘다. 모션까지는 대상과 무관하게 끝낼 수 있으므로 거기까지 먼저 간다.
- **1단계가 끝난 도구는 "허공에 휘두르면 모션이 나오는" 상태다.** 그건 미완성이 아니라
  의도된 중간 상태다 — 위 「채집 계열」이 "대상이 없어도 사용 모션은 항상 나와야 한다"
  고 이미 정해뒀기 때문에, 2단계는 그 위에 판정과 결과를 얹기만 하면 된다.
- **2단계는 도구가 아니라 대상을 기준으로 묶인다.** 나무를 만드는 바퀴가 벌목까지,
  광맥을 만드는 바퀴가 채광까지 붙인다 — 도구 쪽에서 다시 열지 않는다.
- 그림과 동작을 한 항목에 넣지 않는 원칙 자체는 그대로다
  (「그래픽 파이프라인」의 "한 항목에 방향 × 모션 × 색을 다 넣지 않는다").

**1단계 — 도구를 든 캐릭터 그림과 모션 ([DESIGN] 한 바퀴)**
1. 모션은 **3종**이다: `hold_<도구>`(들고 있기) / `use_<도구>`(사용) / `walk_<도구>`(든 채
   걷기). 「캐릭터 애니메이션」의 "도구별 애니메이션에는 반드시 든 채 걷기도 있어야 한다".
   - **사용 모션이 없는 도구는 2종이다**(2026-09-07, INBOX #31 — 지금은 낚싯대뿐이다).
     `gen_character.py` 의 `has_use()` 와 `player_frames.gd` 의 `NO_USE_TOOLS` 가
     짝이고, 시트도 검사도 그 한 줄을 보고 갈린다 — 아래 3·4번의 "3모션"이 그만큼
     줄어든다. **`hold`/`walk` 는 어떤 도구도 건너뛰지 않는다.**
2. **도구를 캐릭터 옆에 아이콘으로 띄우지 않는다** — 손에 쥔 모양을 프레임 자체에 그려
   넣는다(같은 절의 규칙). 그리는 코드는 `gen_character.py` 안에서 **기존 `character()` 에
   오프셋만 넣는 방식**을 유지한다 — 형태를 다시 정의하면 프레임 사이에서 캐릭터 크기가
   어긋난다.
3. **`player_frames.gd` 의 `MOTIONS` 에 세 줄을 늘리는 것 말고 노드는 고치지 않는다.**
   시트가 같이 실리고 색 바꿔치기도 따라온다. 시트 이름은 `player_<모션>_<머리모양>.png`
   규칙 그대로다(머리모양 4종 × 3모션 = **도구 하나당 시트 12장**).
4. **`qa_sprite_check.py` 의 `SPECS` 와 `qa_player_walk.gd` 를 그 세 모션까지 보게 넓힌다.**
   안 넓히면 새 모션은 아무도 검사하지 않는다.
5. **도구를 든 손이 방향 4개에서 다 보여야 한다** — 뒷모습(up)에서 도구가 몸에 완전히
   가려지면 무엇을 들었는지 알 수 없다.
6. 아이템 아이콘(인벤토리 칸에 보일 그림)도 같은 바퀴에 만든다 — `gen_character.py` 의
   `tool_icon()` 이 **손에 쥔 것과 같은 램프·같은 광원·같은 원시 도형으로** 굽고,
   `item_types.gd` 의 그 아이템에 `icon` 한 줄을 늘리면 인벤토리 칸에 나온다.
   **바닥에 놓였을 때 쓸 그림도 같이 나온다**(2026-09-07, INBOX #38) — `ICONS` 에 한
   줄을 넣으면 `ground_icon()` 이 **같은 도형을 12px 칸에 축소해서** 함께 굽는다.
   `item_types.gd` 에 `ground` 한 줄을 더 늘릴 것(위 「바닥 드롭」).
   「아이템/오브젝트 크기 표준」의 하한은 **이 아이콘이 지킨다**(2026-09-07, INBOX #24 —
   손에 쥔 도구는 17px 캐릭터 그림의 일부라 그 하한의 대상이 아니다. 자세한 것은
   `docs/STYLE_GUIDE.md` 「도구」).

7. 위 「채집 계열」의 도구 표에 한 줄 추가하고, 도구 등급(돌 → 철)에서 어디에 놓이는지
   「도구 등급」에 적는다.
8. **모션이 실제로 나오는지까지 확인하고 끝낸다.** `player_frames.gd` 의 `TOOLS` 에
   이름을 넣으면 시트가 실리고, 핫바에서 그 도구를 골라 좌클릭하면 사용 모션이
   나와야 한다(그 배선은 INBOX #25 가 도끼로 이미 깔아뒀다 — 도구 이름만 늘어난다).
   **여기까지가 1단계다.** 실제 효과는 만들지 않는다.

**2단계 — 그 도구가 캘 대상을 만드는 바퀴가 같이 붙인다**
9. 좌클릭이 실제로 무언가를 바꾼다(나무가 넘어가고 목재가 드롭되는 등).
10. **대상 판정과 결과(무엇이 드롭되는가)는 순수 클래스에 둔다** — 「서버 권위」의
    "명중 판정 / 자원 획득은 전부 서버가 계산한다"가 나중에 같은 코드를 부른다.
11. **새 아이템이 생기면 「테스트용 저장 상자 자동 보급」 규칙대로 같은 바퀴에 상자를
    채운다**(무료 지급 도구는 제외).

### 동물 AI
- 평소에는 배회(랜덤 이동)한다.
- 플레이어가 가까이 다가가거나 총에 맞으면 도망간다.
- **도주는 플레이어 반대 방향으로 일직선이 아니라 예측하기 어렵게 움직여야 한다**
  (지그재그/방향 변화 등). 일직선 도주는 맞추기 너무 쉬워서 사냥의 긴장감이 없다.

### 시간 / 계절 / 날씨
- **하루 길이: 20분 (낮 10분 + 밤 10분)**. 계절이 바뀌어도 이 길이는 그대로다.
- 날짜: 1·2·3월 = 봄, 4·5·6월 = 여름, 7·8·9월 = 가을, 10·11·12월 = 겨울.
- 비는 낮에도 밤에도 올 수 있고, 각각 독립적으로 랜덤이다 (낮에만/밤에만/둘 다/둘 다 아님 전부 가능).
- **강수 확률 공식**: `그날의 강수확률 = 그 계절의 기본값 + (마지막으로 비 온 뒤 지난 날 수) × 1%p`.
  비가 온 날(낮이든 밤이든 하나라도 비가 왔으면)은 다음 날부터 다시 기본값에서 시작한다
  (스트릭이 리셋됨). 이 확률로 낮/밤을 각각 독립적으로 굴린다.
- **계절별 기본값**: 봄·가을·겨울 = 1%, **여름(4·5·6월) = 30%**(장마).

### 멀티플레이 — 세션 서버 아키텍처
- **호스트 겸용 리슨 서버**: 방을 만든 플레이어의 클라이언트가 그 세션의 서버 역할도 함께
  한다. 별도의 전용 게임 서버 프로세스는 두지 않는다.
- **방 코드**: 호스트가 "방 만들기"를 하면 짧은 방 코드(영숫자, 자릿수는 구현 시 정함)가
  발급된다. 다른 플레이어는 그 코드를 입력해서 방에 들어온다.
- 방 코드는 실제 접속 정보(호스트에게 어떻게 연결하는지)를 가리키는 키다. 서로 다른
  네트워크에 있는 플레이어끼리 포트포워딩 없이 코드만으로 연결되려면, 코드↔접속정보를
  중개해줄 작은 매치메이킹/릴레이 서버가 하나 필요하다 — 게임 클라이언트와는 별개의
  작은 인프라이며, 실제 만들 때 별도로 다룬다 (지금은 "이런 게 필요하다"만 기록).
- 게임 내부 상태 동기화는 Godot 내장 고수준 멀티플레이 API(ENetMultiplayerPeer)를 쓴다.
- **스팀 출시 예정**이므로, 나중에 Steam Networking Sockets(스팀 자체 P2P 릴레이 — NAT
  문제를 스팀이 대신 처리)로 연결 계층만 바꿔 낄 수 있도록, 지금부터 "연결 방식"과
  "게임 로직"을 분리해서 만든다. 스팀 SDK 연동 자체는 지금 범위에 없다 (범위 밖 참고).
- 동시 접속 인원 상한은 우선 작게 잡고(예: 4~8명) 나중에 필요하면 늘린다.

### 서버 권위 / 클라이언트 신뢰 (지금부터 지킬 것)

호스트 겸용 리슨 서버라도 **클라이언트가 보낸 값을 그대로 믿지 않는다.** 지금은 친구끼리
하는 협동이라 당장 문제가 안 되지만, 아래를 어긴 코드는 PvP를 붙이는 시점에 전부 다시
써야 한다 — 그때 고치는 비용이 지금 지키는 비용보다 훨씬 크다.

- **클라이언트는 위치를 보내지 않는다. 입력(이동 방향 / 조준 각도 / 버튼)만 보낸다.**
  위치는 서버가 시뮬레이션하고 속도 상한으로 자른다. 클라이언트는 예측해서 부드럽게
  그리고, 서버 값과 어긋나면 서버 쪽으로 보정된다. 근거: "나 여기 있다"를 받는 순간
  이동속도 핵과 순간이동을 구조적으로 막을 방법이 없어진다.
- **입력은 실제 경과 시간을 기준으로 처리량을 제한한다.** 입력에 시퀀스 번호를 붙이고,
  틱 레이트를 넘겨 들어오는 입력은 버린다. 근거: 입력만 받더라도 클라이언트가 입력을
  실시간보다 빠르게 밀어넣으면 서버가 그만큼 더 시뮬레이션해줘서 결국 속도 핵이 된다.
  이 제한이 없으면 위 규칙을 지켜도 소용이 없다.
- **명중 판정 / 자원 획득 / 재료 소모 / 제작 완료는 전부 서버가 계산한다.** 클라이언트는
  결과를 통보받아 그릴 뿐이다.
- **범위 밖(지금)**: 예측·보정(client-side prediction / reconciliation)의 정교한 구현.
  지금은 "위치를 받지 않는다"는 방향만 지키면 되고, 부드러움 보정은 나중에 다듬는다.

### 건축 / 방(Room) 시스템 (Oxygen Not Included 참고)
- **어디든 자유롭게 지을 수 있다.** 정해진 "건축 가능 구역" 같은 건 없다 — 월드 어느
  자리에나 건축 오브젝트를 놓을 수 있다.
- **격자(타일) 기반으로 짓는다.** 참고 게임인 코어키퍼도 실제로는 설치할 때 정해진 격자
  칸 안에 스냅되는 방식이다 — 자유 배치처럼 보여도 내부적으로는 격자다.
- **격자 칸과 벽/문/창문 스프라이트는 반드시 1:1로 딱 맞아야 한다** — 충돌 크기와 화면
  표시 크기가 어긋나면(한쪽만 키우면) 인접(비겹침) 배치가 오히려 겹쳐 보이는 착시가
  생기거나, 반대로 실제로는 빈틈이 생겨 방이 밀폐되지 않는 문제가 난다. 격자 크기 자체를
  "충돌 크기 = 스프라이트 표시 크기"가 동시에 자연스러운 값으로 잡을 것 — 스프라이트만
  따로 확대/축소하는 방식은 쓰지 않는다.
- **침대**(2026-09-07 추가): 설치하면 **리스폰 지점이 그 침대로 바뀐다**
  (「체력 / 죽음 / 리스폰」). 격자 위에 놓는 설치물이고 철거하면 환급된다 —
  여러 개 설치했을 때 어느 것이 리스폰 지점인지는 실제로 만들 때 정하고 여기 적을 것.
- **재료를 가공해서 건축 오브젝트를 만든다.** 나무벽/나무문/석제벽/강철벽/강철문/창문은
  가공대에서 아이템으로 제작 가능하다.
- **철거**: 설치된 벽/문/창문은 X키(건설 해제 모드) + 좌클릭으로 철거해서 아이템으로
  100% 환급받을 수 있다(위 "조작" 참고). 상자/제작대류/밭/목장 같은 다른 설치물도 같은
  모드로 철거 가능하되, "안에 뭔가 든 상태"면 막는다 — 상자는 비어있을 때만, 제작대류는
  제작 배치가 진행 중이 아닐 때만, 목장은 동물이 없을 때만 철거 가능. 밭은 언제든
  철거 가능하고 심어진 작물은 함께 소실된다(의도된 단순화).
- **배치 겹침 검사**: 벽/문뿐 아니라 그 칸에 이미 있는 다른 오브젝트(밭/채집·채광
  포인트/목장/상자/제작대류)나 플레이어가 서 있는 칸에도 설치할 수 없다.
- **방(Room) 인식**: 벽 등으로 막힌(둘러싸인) 공간이 생기면 자동으로 "방"으로 인식된다.
  그 방 안에 있는 "핵심 오브젝트"들의 **카테고리**를 모아서 판정한다 — 정확히 한
  카테고리만 있고 그 종류의 요구 수량을 채우면 그 이름의 방, **카테고리가 0개(핵심
  오브젝트 없음)이거나 2개 이상 섞여 있으면 잡실**이다. 저장 상자는 어느 카테고리에도
  속하지 않는 "중립" 오브젝트라 방 종류 판정에 영향을 주지 않는다.
  | 방 이름 | 핵심 오브젝트(카테고리) | 요구 수량 |
  |---|---|---|
  | 주방 | 조리대, 조리용 화로 | 각 1개 이상 (**둘 다** 있어야 함) |
  | 제작소 | 가공대 | 1개 이상 |
  | 대장간 | 제련로 | 1개 이상 |
  | 연구소 | 연구대 | 1개 이상 |
  | 농장 | 밭(farm_plot) | 1개 이상 |
  | 목장 | 목장 구역(ranch_zone) | 1개 이상 |
  근거: 제작소(가공대)와 대장간(제련로)은 성격이 달라서(목공/조립 vs 제련/금속가공)
  따로 분리해두는 게 나중에 방별 자동화(예: 대장간 전용 NPC)를 붙이기도 쉽다.
- **"둘러싸임"과 "방 혜택"은 별개다 — 조건을 못 채우면 잡실.** 벽으로 둘러싸인 공간이
  있어도, 그 방 종류가 요구하는 오브젝트를 갖추지 못했거나 다른 방 종류의 오브젝트가
  섞이면 **"잡실"**로 취급하고 **방 혜택을 전혀 주지 않는다 — 둘러싸이지 않은 야외와
  완전히 동일하게 취급**한다. 근거: 방 혜택이 "아무 공간이나 벽으로 두르기만 하면 됨"일
  정도로 헐거우면, 서로 다른 방 종류를 굳이 나눌 이유가 없어져서 전부 하나로 뭉쳐버릴
  것이다. 방 종류별 요구 오브젝트를 깐깐하게 정하면 여러 용도를 한 방에 합칠수록
  오히려 잡실이 되기 쉬워져 자연스럽게 용도별로 방을 나누게 유도된다.
- **방-상자 자동 연동**: 방 안에 저장 상자가 있으면, 같은 방 안의 제작대류(가공대/
  제련로/조리대/조리용 화로 등)가 그 상자의 내용물을 자동으로 재료로 인식해서 쓸 수
  있다(매번 손으로 옮길 필요 없음). 판정 기준은 **거리가 아니라 방 ID 일치 여부**다 —
  제작대와 상자가 "같은, 잡실이 아닌 정상 인식된 방"에 속해 있을 때만 켜진다. **문은
  열려있든 닫혀있든 "설치돼 있다"는 사실만으로 벽과 동일하게 방 경계 역할을 한다**(열림/
  닫힘에 따라 방 인식이 깜빡이지 않게). **방이 없는 상태에서도 상자/제작대는 정상
  작동한다 — 자동 연동 혜택만 없을 뿐**, 손으로 재료를 옮겨서 쓰는 기존 방식은 항상
  가능하다.
- **방 오버레이 UI**: 화면 오른쪽 위 토글 버튼으로 켜고 끌 수 있는 "방 오버레이" 모드 —
  켜면 화면에 보이는 격자 칸 위에 반투명 색을 겹쳐 그린다(산소미포함 참고). 정상 방은
  종류별로 다른 색, 잡실/야외는 회색. 색은 완전 불투명이 아니라 알파값을 낮춰 뒤가
  비쳐 보이게 한다. 아래 "시야/전장의 안개"의 상시 렌더링 레이어와는 별개다.
- 이 시스템은 나중에 추가할 **생산 자동화**(농사/목장 자동화 기계 등)와 **플레이어가
  직접 집을 짓는 것**을 지원하기 위한 기반이다 — 자동화 자체는 아직 범위에 없다.
- **"광산" 구상**: 지금의 단순 채광 포인트(필드에 그냥 놓인 광물)는 장기적으로 "광산"
  개념으로 대체될 예정이다 — 광산 구조물을 설치하고 그 주위에 벽을 지으면 위의 방
  인식 매커니즘으로 "광산" 방이 만들어지고, 나중에 그 방에 NPC를 배치해서 자동으로
  채광 작업을 시키는 게 생산 자동화의 핵심이 된다. 아직 설계/구현 범위는 아니고
  방향만 기록.

### 시야 / 전장의 안개 (Fog of War, 코어키퍼·덕코프 참고)
- **왜**: 시야 밖을 안 보이게 만들어 건축의 의미를 살리고, 총격전에서 "안 보이면
  못 맞힌다"는 감각을 준다. 다만 덕코프처럼 답답하지 않아야 한다.
- **시야 방향**: 플레이어가 바라보는 방향 = 마우스 조준 방향(총 조준과 같은 계산).
- **시야 모양**: 플레이어 위치를 꼭짓점으로 하는 부채꼴(콘) — 조준 방향 중심 좌우 대칭.
  각도/반경은 구현 시 임의로 정하고(예: 전체 100~120도) 실제로 플레이해보며 조정한다.
- **가림(Occlusion)**: 콘/반경 안이어도 플레이어와 그 지점 사이를 벽이나 닫힌 문이
  가로막으면 안 보인다(직선 가로막힘 판정). 열린 문은 막지 않는다.
- **표현**: 안 보이는 영역은 **거의 티가 안 날 정도로만 살짝 어둡게**(낮은 알파의
  어두운 색을 얇게 겹치는 정도) 칠한다 — "여기는 지금 내 시야가 아니다"만 은은하게
  인지시키는 목적이지, 실제로 가려서 안 보이게 하는 용도가 아니다(바닥/지형/벽 등
  정적인 것은 시야 밖이어도 흐리게라도 다 보인다). **완전한 불투명 검은색은 쓰지
  않는다** — 덕코프 대비 너무 답답해지는 걸 피하기 위함이다.
- **벽/문/창문 등 건축물은 이 시야 안개의 적용을 받지 않는다** — 시야 콘/가림 판정과
  무관하게 항상 정상적으로 보인다. 근거: 벽은 지형/건축 정보라서 안 보이면 자기 집
  구조 파악이 힘들어 답답해진다.
- **동물처럼 움직이는 대상은 시야/가림 판정에 따라 완전히 안 보이게 숨긴다**(어둡게
  처리가 아니라 아예 렌더링 안 함) — 대신 화면에 "소리가 난다"는 표시(방향 힌트
  아이콘)를 띄운다. 정확한 위치까지 알려줄 필요는 없고 "이 방향에 뭔가 있다"는 힌트
  수준이면 충분하다. 실제 위치 기반 사운드는 나중 작업(범위 밖).
- **범위**: 렌더링(화면에 뭘 그릴지)에만 적용한다. 이동 충돌/방 감지/총알 물리 같은
  게임 로직 자체는 바뀌지 않는다.
- **멀티플레이**: 각 클라이언트가 자기 자신의 플레이어 위치/조준 방향 기준으로 자기
  화면의 안개만 계산한다(다른 플레이어 화면에 영향 없음).
- **지금의 안개는 연출이고, PvP 공정성에는 기여하지 않는다 — 이 둘을 혼동하지 말 것.**
  각 클라이언트가 자기 화면의 안개만 계산한다는 것은, **클라이언트가 이미 모든 대상의
  위치를 알고 있고 그리지 않기로 선택할 뿐**이라는 뜻이다. 메모리를 읽는 도구 앞에서는
  그대로 무력화된다. 위 "카메라 / 해상도" 절에서 해상도로 시야 이득을 보는 것까지 막아둔
  것과 나란히 놓고 보면, 이쪽 구멍이 비교가 안 되게 크다.
- **PvP를 실제로 붙이는 시점에는 "서버가 보내지 않은 것은 클라이언트가 그릴 수 없다"로
  바꾼다** — 서버가 각 플레이어의 시야 콘/가림을 계산해서 **그 플레이어에게 보이는 대상의
  좌표만** 전송한다. 근거: 탈출 슈터에서 게임을 실제로 죽이는 핵은 에임핵이 아니라 남의
  위치가 다 보이는 레이더다. 2D 타일 기반이라 이 가시성 계산이 3D보다 훨씬 싸서 실제로
  구현 가능하다 — 2D를 고른 이점을 여기서 회수한다.
- **그래서 시야 계산은 "무엇을 어둡게 칠할까"가 아니라 "이 지점이 이 플레이어에게
  보이는가"를 답하는 순수 판정 함수로 분리해둔다.** 지금은 클라이언트 렌더링이 그 함수를
  호출하고, 나중에는 서버의 전송 필터가 같은 함수를 호출한다. 이 분리는 지금 해둔다
  (비용이 거의 없다).
- **범위 밖(지금)**: 서버측 가시성 필터링 자체. 지금은 위 함수 분리까지만 한다.
- **범위 밖**: 화면 구석에 항상 떠 있는 미니맵. 지금은 M 키로 여는 전체 맵만 만든다
  (아래 「맵 (M)」). 월드 렌더링 쪽 안개는 여전히 **순수 실시간 시야만** 다룬다 —
  "한 번 본 곳은 흐리게 계속 보이는" 탐험 기록은 **맵에만** 있고 월드 화면에는 없다.
  (2026-09-07 사람 결정으로 미니맵/탐험 기록을 통째로 범위 밖에 두던 것에서 바꿨다.)

### 맵 (M)
(2026-09-07 사람 결정. "m키를 눌러 맵을 볼 수 있었으면 좋겠고 플레이어의 위치도
표시 됐으면 좋겠어.")

- **M 키로 전체 월드 맵을 화면 위에 겹쳐서 연다.** 다시 M 또는 Esc 로 닫는다(맵이
  열려 있을 때의 Esc 는 일시정지 메뉴가 아니라 맵 닫기다 — 가장 안쪽 창부터 닫힌다).
- **자기 위치를 표시한다.** 위치 점은 **조준 방향을 함께 나타낸다**(화살표 등) —
  방향 정보가 이미 있는데 점만 찍으면 맵에서 방향을 잃는다.
- **가본 곳만 보인다.** 처음에는 맵이 비어 있고, 돌아다닌 자리부터 채워진다.
  - **기록 단위는 타일이다.** 플레이어 주변 일정 반경의 타일을 "방문함"으로 표시한다.
    **시야 콘이 아니라 반경이다** — 콘으로 기록하면 맵이 부채꼴 자국으로 얼룩덜룩해지고,
    마우스를 돌리는 것만으로 맵이 채워진다.
  - **기록은 슬롯 저장 데이터에 들어간다**(캐릭터마다 따로). 월드가 아니라 캐릭터에
    붙는 정보다 — 같은 월드라도 다른 캐릭터는 자기가 가본 곳만 안다.
  - 저장 크기: 타일당 1비트면 충분하다. 맵 전체를 타일마다 한 칸씩 쓰는 배열로 두지
    말고 **압축 가능한 형태**(비트 배열 등)로 넣을 것 — 「월드 생성」의 맵 크기를
    보고 실제 바이트 수를 계산해서 정한다.

(2026-09-07, INBOX #18 — 실제로 만들면서 정한 구체값이다. `game/scripts/explored_map.gd`
와 `game/scripts/map_canvas.gd` 가 원본이고, 여기 표는 그 요약이다.)

| 항목 | 값 |
|---|---|
| 방문 기록 반경 | **6타일** (`world.gd` 의 `EXPLORE_RADIUS_TILES`) |
| 기록 시점 | 플레이어가 **선 칸이 바뀔 때** 그 주변 반경을 한 번에 적는다 |
| 저장 형식 | 타일당 1비트 = **8,192바이트** → deflate → base64 한 줄 (슬롯의 `explored`) |
| 지도 그림 | 타일 하나 = 픽셀 하나(256×256)를 **정수 배율**로 확대. 기본 2배 = 화면 512×512 |
| 줌 | **마우스 휠로 1 / 2 / 4 / 8배** (2026-09-07 사람 결정 — 아래) |
| 저장 주기 | 3초마다(내용이 바뀐 경우에만) + 월드를 나갈 때 한 번 |

- **맵 줌 (마우스 휠)** — 2026-09-07 사람 결정.
  - **정수 배율만 쓴다: 1 / 2 / 4 / 8배.** 소수 배율을 끼우면 지도 픽셀 하나가 화면에서
    1px, 2px 로 들쭉날쭉해져 도트가 뭉개진다(`STYLE_GUIDE.md` 1번과 같은 이유다 — 지도도
    타일 하나가 픽셀 하나인 도트 그림이다). 휠 한 칸에 한 단계씩 움직인다.
  - **1배는 월드 전체(256×256)가 한눈에 들어오는 배율**이고, 2배가 기본이다. 그보다
    확대하면 화면에 다 안 들어가므로 **플레이어를 중심에 두고 잘라서 보여준다** —
    지도 가장자리에서는 지도 밖이 나오지 않게 중심을 안쪽으로 물린다.
  - **줌 단계는 지도를 닫았다 열어도 유지되지만 저장하지는 않는다** — 게임을 다시
    켜면 기본 2배로 돌아온다. 슬롯 저장 데이터(「맵 (M)」의 `explored`)에 넣지 말 것:
    캐릭터의 진행 상황이 아니라 지금 보고 있는 방식일 뿐이다.
  - **멀티플레이와 무관하다** — 순수하게 내 화면을 어떻게 그리는지의 문제라 서버가
    알 필요도, 검증할 필요도 없다(위 「서버 권위」의 검증 대상에 넣지 말 것).
  - (2026-09-07, INBOX #22 — 실제로 만들면서 정한 구체값. 원본은 `game/scripts/map_canvas.gd`
    의 `map_rect()` 이고 여기 적힌 것은 그 요약이다.)
    - **지도 창은 배율과 상관없이 512×512 로 고정**이고, 배율은 그 안에 텍스처를 붙이는
      사각형만 바꾼다. 그림(256×256 이미지)은 다시 굽지 않는다.
    - 창보다 큰 배율(4·8배)에서는 잘라서 보여줘야 하므로 지도 창이 **자기 밖을 잘라낸다**
      (`clip_contents`). 안 그러면 확대한 지도가 창틀과 월드 화면까지 통째로 덮는다.
    - 1배에서는 지도(256px)가 창(512px)보다 작아 가운데에 놓이고 남는 자리는 여백색이다.
      여백과 안 가본 칸이 둘 다 어두워 세계의 끝이 안 보이므로, **지도 바깥에 창틀과 같은
      금색 한 줄**을 그어 어디까지가 세계인지 보이게 한다.
    - **줌 값은 스크립트에 붙은 static 변수 하나로 산다**(`map_canvas.gd` 의 `zoom_index`).
      지도 노드는 닫을 때마다 사라지니 노드에 두면 못 남고, 슬롯에 두면 저장돼 버린다 —
      "닫았다 열어도 유지, 다시 켜면 기본 2배"가 이 자리에서 그대로 나온다.

- **반경 6타일을 고른 근거**: 화면 세로 절반이 7.5타일(360 ÷ 48)이다. 그보다 조금
  작게 잡아야 "화면에 실제로 보인 만큼"만 기록에 남는다 — 크게 잡으면 화면 밖까지
  가본 것으로 적히고, 훨씬 작게 잡으면 지나온 길이 실 한 줄로만 남아 지도를 못 읽는다.
- **저장 크기 계산**: 256 × 256 = 65,536타일 ÷ 8 = **8,192바이트**. 타일마다 JSON
  배열 칸 하나씩(`0,`) 쓰면 그것만으로 131KB 라 16배 크다. deflate + base64 로 적은
  실측값은 처음 96자, 10% 탐험 688자, 절반 탐험 1,816자, 전부 탐험 44자다(전부 1이면
  도로 잘 줄어든다) — 어느 쪽도 슬롯 JSON 한 줄로 충분하다.
- **읽지 못하는 기록은 빈 지도로 떨어진다**(형식이 바뀌거나 파일이 깨진 경우). 탐험
  기록은 지형과 달리 잃어도 다시 걸어서 채울 수 있어서, 이것 때문에 캐릭터를 못 쓰게
  만들 이유가 없다.
- **지도는 설정 화면과 같은 자리에 겹쳐 붙는다**(`world.tscn` 의 `HUD`) — 씬을 바꾸면
  월드가 내려간다. **겹쳐 뜬 지도는 자기 M/Esc 를 처리하지 않는다**(닫는 것은 `world.gd`
  다 — 「조작」 Esc 항목의 "가장 안쪽 창부터" 규칙과 같은 이유). 지도가 열려 있는 동안
  **월드는 계속 돌고 플레이어 입력만 끊긴다**(일시정지 메뉴와 같은 `input_enabled`).
  일시정지 메뉴나 설정이 열려 있을 때 M 은 아무 일도 하지 않는다 — 창을 겹겹이 쌓지 않는다.
- 검증은 `game/qa/qa_map.gd`(기록 코어 + 화면 — 안 가본 곳이 안 보이는지, 보이는 지형이
  월드와 맞는지, 돌아다니면 채워지는지, 저장했다 불러와도 남는지를 캡처의 픽셀로 본다).
- **지형은 시드로 다시 계산해서 그린다** — 방문 기록은 "어디를 봤는가"만 갖고, 그
  자리의 지형이 무엇인지는 `world_gen` 에 다시 물어본다. 지형을 기록에 같이 저장하면
  같은 정보를 두 벌 갖게 되고 둘이 어긋날 수 있다.
- **멀티플레이 대비 — 맵에 다른 플레이어를 표시하지 않는다.** 지금은 자기 위치만이다.
  근거: 위 「시야 / 전장의 안개」의 "탈출 슈터에서 게임을 실제로 죽이는 핵은 에임핵이
  아니라 남의 위치가 다 보이는 레이더다" 그대로다 — 맵은 그 레이더를 **게임이 직접
  제공하는** 자리라, 여기에 남의 위치를 얹으면 PvP 설계가 무너진다. 나중에 파티/팀원
  표시를 넣는다면 **서버가 그 팀원의 좌표를 보내주기로 판단한 경우에만** 그린다
  (클라이언트가 알고 있는 좌표를 그리는 방식으로 만들지 말 것).
- **방문 기록은 클라이언트가 자기 것만 갖는다** — 서버가 검증할 값이 아니다(맵을
  조작해봐야 자기 맵만 넓어질 뿐 남에게 영향이 없다). 위 「서버 권위」의 검증 대상
  목록에 이걸 넣지 말 것.

## 아이템 카테고리 / 제작(크래프팅) 테크

### 카테고리
모든 아이템은 다음 중 하나의 카테고리에 속한다 (세부 하위분류는 계속 늘어날 수 있음):
- **원재료**: 목재, 돌, 철광석, 유황광석 (나중에 다른 지역에서 더 상위 등급 광물 추가 예정)
- **가공물**: 판자(목재 가공), 석재(돌 가공), 철(철광석 제련), 숯(목재를 태워서 만듦),
  화약(유황광석+숯)
- **완성품**: 나무벽/나무문, 석제벽, 도구 업그레이드(돌곡괭이→철곡괭이 등), 탄약
- **식재료**: 네 하위분류로 나눈다 — **곡물**(벼 등), **육류**(고기 등), **어류**(낚시로
  얻는 것 — 2026-09-07 추가. 종류는 위 「낚시」의 서식지가 생길 때 정한다), **채소**(아직
  구체적 작물 미정)
- **가공식품**: 원재료 식재료를 조리한 것(밥, 익힌고기, 익힌채소 등) — 날것보다 포만감이 큼
- **요리**: 가공식품을 조합한 완성 음식/음료(예: 밥+익힌고기+익힌채소 → 스테이크) —
  포만감이 가장 크고 버프도 준다(버프 세부는 나중에 정함). 나중에 **음료/음식**으로
  다시 나뉠 예정.

### 생산 라인 — 작업대 2계통
제작(가공/조리)은 지금 단계에서는 등급을 나누지 않고 **각 계통마다 "최종 테크" 하나씩만
만든다** (아래 "개발 순서" 참고). 계통은 둘:
- **가공 라인**: **가공대**(목재→판자, 돌→석재, 화약 조합, 벽/문/도구 조립 등 열이 필요
  없는 가공) + **제련로**(철광석→철, 목재→숯처럼 열이 필요한 가공). 둘은 별개 오브젝트다
  (화로를 가공대와 겸용으로 쓰지 않는다).
- **조리 라인**: **조리대**(레시피 조합) + **조리용 화로**(가열). 이것도 가공 라인의
  화로와는 별개의 오브젝트다(용도별로 화로를 나눔).
- 두 라인 모두 플레이어가 근처로 다가가 상호작용하면 레시피 목록에서 골라 재료를 소모하고
  결과물을 얻는 제작 UI를 여는 방식.
- 레시피는 데이터(예: `{inputs: {아이템: 개수, ...}, output: 아이템, amount: 개수}` 목록)로
  관리해서 나중에 항목을 추가하기 쉽게 한다.
- **레시피 재료 종류는 최대 1~2개로 얕게 유지한다.** 완성품 하나를 만들려고 부품을 여러
  단계 거쳐 조합하게 만들지 않는다. 근거: 부품을 너무 많이 만들게 하면 플레이어에게
  스트레스를 준다 — 재가공품 1~2개 정도로 조합하는 정도가 적당하다.
- **제작은 즉시 완성되지 않는다 — 수량 지정 + 타이머 + 수동 수령 방식.** 플레이어가
  만들 수량을 지정 → 그 수량 전체분의 재료를 시작 시점에 한 번에 소모 → 타이머가 돌면서
  (1개당 5초, 명시적 임시값) 결과물이 하나씩 그 제작대/화로 자체의 **내부 출력 버퍼**에
  쌓임 → 플레이어가 직접 "수령"해야 인벤토리로 들어간다. 인벤토리 공간이 부족하면 수령
  가능한 만큼만 옮기고 나머지는 출력 버퍼에 그대로 남는다(사라지지 않음). 근거: 나중에
  NPC/자동화가 이 출력 버퍼에서 상자로 자동으로 옮기게 만들 계획이라, 그 구조에 맞춰
  처음부터 "제작 공간에 쌓였다가 수거"하는 모델로 만들어둔다.

### 인벤토리 안전
**인벤토리에 공간이 없다고 아이템이 사라지면 안 된다** — 바닥 드롭 줍기, 제작 수령,
상자 이전 등 인벤토리에 아이템을 넣는 모든 경로는 넣기 전에 공간을 확인하고, 공간이
없거나 모자라면 넣을 수 있는 만큼만 넣고 나머지는 원래 위치(바닥/제작대 출력 버퍼/상자)에
그대로 남겨야 한다.

### 테스트용 저장 상자 자동 보급 (상시 규칙)
스폰 지점 근처에 테스트용 저장 상자를 **용량 무제한**으로 배치한다(일반 게임용 상자는
이 옵션을 꺼서 고정 슬롯 수로 쓴다). **새 아이템 종류가 하나라도 추가될 때마다(무료
지급 도구/`captured_deer` 제외), 그 아이템을 만든 바퀴가 같은 바퀴 안에서 이 상자에도
넉넉한 수량으로 채워 넣는다.** 근거: 매번 "테스트 상자에도 넣어달라"고 다시 요청하지
않아도 새 콘텐츠가 항상 바로 테스트 가능한 상태를 유지해야 한다.
(2026-09-07, INBOX #23 — **저장 상자 자체가 아직 없다.** 상자를 만드는 바퀴가 오기
전까지는 `world.gd` 의 `STARTER_ITEMS`(처음 들어오는 캐릭터에게 주는 목록)가 이 자리를
대신한다 — 새 아이템을 추가하는 바퀴는 거기에 넣고, 상자가 생기면 그쪽으로 옮긴다.)

### 도구 등급
돌 → 철 → (다음 등급은 미정, 나중에 정함) 순으로 도구가 업그레이드된다.
**날붙이 도구(도끼 / 곡괭이 / 낫 / 괭이)는 전부 이 사다리 위에 있다** — 돌괭이 →
철괭이처럼 재료가 등급을 정한다(2026-09-07, INBOX #29 에 괭이를 붙이며 확인).
**물뿌리개도 이 사다리 밖이다**(2026-09-07, INBOX #30) — **날이 없어서 "돌 물뿌리개
→ 철 물뿌리개"가 성립하지 않는다.** 물뿌리개가 등급을 갖는다면 재료가 아니라
**한 번에 담기는 물의 양**으로 갈릴 텐데, **물 보충 자체가 2단계라 아직 정해진 바가
없다** — 그때 정해서 여기 적는다.
**낚싯대도 이 사다리 밖이다**(2026-09-07, INBOX #31) — 물뿌리개와 같은 이유로 날이
없다. 낚싯대가 등급을 갖는다면 재료가 아니라 **던질 수 있는 거리**나 **잡히는 물고기**
로 갈릴 텐데, 「낚시」의 최대 거리도 서식지별 어종도 아직 정해진 바가 없다(둘 다
2단계다) — 그때 정해서 여기 적는다.
**총은 이 사다리 밖이다**(2026-09-07, INBOX #28) — 지금 총은 「총기 스탯」의
**기본 소총 하나뿐**이고 총기 추가 종류는 「범위 밖」이다. 총이 여러 종류가 되는
시점의 등급은 재료가 아니라 「장비 / 제작 결과물 설계 원칙」의 **포인트 예산제**로
갈린다(같은 등급이면 총합이 같고 배분만 다르다). **지금 게임
시작 시 공짜로 지급하는 기본 도구(위 「채집 계열」의 도구 표 전부)는 테스트 서버로서 지금
단계에서는 그대로 유지한다** — 정식 출시 시점에는 이 무료 지급을 없애고 튜토리얼을
통해 처음부터 직접 제작하게 만들 계획이다(지금 당장 할 일 아님).

### 새 테크 라인
- **화약/탄약**: 유황광석 + 숯(목재 가공) → 화약(가공대) → 탄약(총알/마취탄, 가공대).
  지금 총은 R키로 무한 재장전인데, 탄약 아이템을 실제로 소비하도록 연동하는 건 이번
  범위가 아니다(아이템 자체만 만들고, 총 재장전과의 연동은 별도 지시로 나중에 처리).
- **전기/기계**: 방향만 잡아둠(구리 등 새 광물, 발전기 등) — 세부는 나중에 다시 논의하기로
  하고 지금은 착수하지 않는다. 배터리/조명/발전기는 아이템으로만 존재할 수 있고, 실제
  전력/조명 효과 같은 기능 연동은 범위 밖이다.
- **가죽/직물**(가방 포함): 사슴을 사살하면 고기와 함께 가죽도 드롭되는 것까지는 계획에
  있으나, **동물이 사슴 하나뿐이라 다양성이 부족해서 전체 보류** — 동물 종류가 늘어난
  뒤에 다시 착수한다. 가방(장비 슬롯에 이미 자리는 있음)은 상위 등급 가죽으로 만드는
  완성품이 될 예정.

### 개발 순서 철학 ("탑다운 레벨")
지금은 게임 내에서 등급을 나눈 여러 단계의 작업대를 만들지 않는다. 대신 **가장 좋은
버전(최종 테크) 작업대 하나로 전체 파이프라인(원재료→가공→완성품, 식재료→가공식품→
요리)이 실제로 끝까지 동작하는 것부터 검증**한다. 그 다음에 위에서 아래로(top-down)
등급을 쪼개서(예: 돌 시대 작업대 → 철 시대 작업대) 진행도를 나누고, **그 등급 구조
안에서 어느 시점에 자동화(NPC)를 넣을지 나중에 정한다**. 근거: 게임 초반부터 자동화가
있으면 성취감이 없다 — 등급을 나눠서 단계별로 발전하는 느낌 자체가 중요한 재미 요소다.

## 시뮬레이션 구조 (자동화가 오기 전에 잡아둘 것)

자동화는 아직 범위 밖이지만, 아래는 자동화가 들어올 자리를 미리 비워두는 구조라 지금
만드는 시스템(작물 성장, 제작 타이머, 가축)에도 그대로 적용한다.

- **게임 로직은 프레임이 아니라 고정 틱 위에서 돈다.** 프레임 레이트가 달라도 결과가
  같아야 한다.
- **"시간이 지나면 완성되는 것"은 매 틱 전부 훑지 않는다 — 완성 예정 틱을 우선순위 큐에
  넣고, 만료된 것만 꺼내서 처리한다.** 근거: 자동화 게임이 무너지는 지점은 개수다. 밭 한
  칸, 제작대 하나, 가축 한 마리마다 매 프레임 갱신을 돌리면 수백 개에서 프레임이 무너진다.
  큐 방식이면 설치물이 1만 개여도 실제 처리량은 "지금 완성되는 것"뿐이다.
- **오프라인 진행은 이 큐를 빨리 감는 것으로 처리한다.** 세션 서버는 호스트가 항상 켜져
  있지 않다 — 서버를 다시 켰을 때 `지금 시각 - 마지막 저장 시각`만큼 큐를 앞으로 감으면
  끝난다. "오프라인 보상 계산" 같은 별도 로직을 따로 만들지 않는다.
- **시뮬레이션 코어는 Godot 노드를 상속하지 않는 순수 클래스로 만든다**(`RefCounted` 등).
  씬 트리에 올리지 않아도 단독으로 돌아가야 한다. 근거: 노드에 묶이면 헤드리스 자체 QA에서
  검증하기가 어려워지고, 화면 없이 로직만 테스트하는 게 불가능해진다.

## 아이템 소유권 상태 (저장 포맷에 자리만 미리 잡아둔다)

- 아이템 인스턴스에 **소유 상태** 필드를 지금부터 넣어둔다. **지금은 값이 항상 `로컬`
  하나뿐이고 아무 동작도 하지 않는다.**
- 나중에 쓸 값: `로컬`(이 세션 안에만 존재) / `반출중`(다른 지역·서버로 나가서 잠김) /
  `정산중`(결과 확정 대기).
- 근거: 세션 서버 사이의 이동·거래·의뢰를 붙이는 시점에 이 상태가 반드시 필요해지는데,
  그때 가서 저장 포맷을 바꾸면 기존 세이브가 전부 깨진다. 지금 필드 하나 두는 비용은
  거의 0이다.
- **범위 밖**: 상태 전이 로직, 서버 간 이동, 중앙 원장 — 전부 `docs/VISION.md` 참고.

## 장비 / 제작 결과물 설계 원칙 (수치를 실제로 정할 때 따를 규칙)

장비 효과도 인챈트도 아직 범위 밖이지만, 정하는 시점에 아래 두 규칙을 따른다. 지금
적어두는 이유는 이 두 규칙이 나중에 시스템 전체의 형태를 결정하기 때문이다.

- **총합은 고정하고 배분만 플레이어에게 준다(포인트 예산제).** 같은 등급의 장비는 스탯
  총합이 같고, 그 예산을 어디에 몰아줄지만 달라진다(예: 같은 등급 소총이라도 반동을 줄인
  근거리형 / 사거리를 늘린 대신 무거운 저격형). 근거 1: 어떤 조합도 절대적으로 강하지
  않아서 PvP 밸런스의 천장이 무너지지 않고, 그러면서도 "누가 만들었는가"는 여전히 의미가
  남는다. 근거 2: 총합이 등급의 함수이므로, 나중에 서버가 "이 장비가 정상적으로 만들어진
  것인가"를 산술 한 줄로 검증할 수 있다 — 밸런싱을 위한 규칙이 그대로 치팅 검증 수단이
  된다.
- **제작/강화는 성공·실패가 아니라 품질 분포로 만든다.** 제작은 항상 성공하고, 결과물이
  품질 범위 안 어딘가에 떨어진다. 숙련도와 재료 등급이 그 범위의 하한과 중앙값을 올린다
  (예: 초보 40~70 → 숙련자 78~95). **실패로 재료가 사라지거나 장비가 파괴되는 방식은
  쓰지 않는다.** 근거: 이 게임의 1차 정체성은 힐링이다. 파괴형 강화 도박은 그 정체성과
  정면으로 충돌해서, 생활 위주로 즐기려는 플레이어 — 즉 이 게임이 가장 붙잡아야 할
  관객 — 를 가장 먼저 떠나게 만든다. 품질 분포로 하면 "좋은 걸 뽑으려면 여러 번 만들어야
  한다"는 반복은 그대로 남으면서 손실 공포만 사라지고, 하급 결과물도 버려지지 않고
  초보용 장비로 흘러가 경제의 아래층을 만든다.

## 범위 밖 / 아직 미정

- (2026-09-06 해제: 캐릭터 커스터마이징 항목은 정해졌다 — 위 "캐릭터 커스터마이징 항목" 참고)
- 장비 슬롯(모자/상의/하의/신발/목걸이/반지/가방)에 넣은 아이템의 실제 효과(스탯 보너스,
  가방의 인벤토리 용량 증가 여부 등)
- 총기 추가 종류 (지금은 "기본 소총" 하나뿐)
- **PvP 전체** — 2026-09-07 에 뒤로 미뤘다. 이 세션 서버는 무조건 PvE 이고, PvP 는
  나중에 별도의 "PvP 섬"을 만들 때 다시 설계한다(위 「전투」 참고). **PvP 공정성
  규칙들은 미루지 않고 지금 그대로 지킨다** — 이유는 같은 절에 있다.
- 채집물/광물/작물/동물 추가 종류 (지금은 벼/철/사슴 하나씩뿐)
- 방 코드 매치메이킹/릴레이 서버의 실제 구현(호스팅 위치, 코드 자릿수·형식 등)
- Steam SDK 연동 자체 (지금은 "나중에 붙이기 쉽게 구조만 분리"까지만)
- 방(Room) 안 이름을 화면에 표시하는 UI (방 감지 자체는 됨)
- 연구소/연구대 + 표본(연구 재료) 시스템
- 생산 자동화(농사/목장 자동화 기계 등) — "나중에" 넣을 예정이라고만 언급됨

이 목록에 없는 것을 구현해야 할 상황이 오면, 임의로 정하지 말고 STATUS.md의
"막힌 것 / 보류"에 적고 다음 지시를 기다린다.

### 장기 비전
지금 구현 범위가 아닌 얼리억세스 이후 로드맵은 `docs/VISION.md`에 별도로 있다 — 매 바퀴
읽는 파일이 아니고, 장기 로드맵을 실제로 설계하기 시작할 때만 참고한다.

## 캐릭터 애니메이션 (중요 규칙 — 절대 되돌리지 말 것)

- **캐릭터는 정지 이미지 한 장이 아니라, 방향별로 여러 프레임(그림 여러 장)을 빠르게
  갈아끼우는 방식(2D 게임의 표준적인 "스프라이트 시트" 애니메이션 — 뼈대/스켈레톤
  애니메이션이 아니다)으로 움직인다.** Godot의 `AnimatedSprite2D`(`SpriteFrames`)를 쓴다.
  최소한 방향별 idle(1프레임 이상)과 walk(4~6프레임) 애니메이션이 있어야 한다.
- **서로 다른 생성 호출로 만든 프레임끼리 캐릭터가 차지하는 크기(픽셀 비율)가 달라지면
  안 된다** — 프레임을 만들고 나면 반드시 같은 세트 안에서 캐릭터 크기를 직접 비교해서
  맞출 것.
- **애니메이션 프레임 사이의 움직임이 자연스럽게 이어져야 한다.** 걷기 프레임을 순서대로
  넘겨봤을 때 다리/팔이 매끄럽게 움직이는 것처럼 보여야지, 중간에 자세나 크기가 뚝뚝
  끊겨 보이면 안 된다. idle에서 사용(발사/채광 등) 모션으로 바뀔 때도 자세가 갑자기
  다른 캐릭터처럼 변하지 말고 자연스럽게 이어지는 느낌이어야 한다 — 앞으로 만드는 모든
  도구/오브젝트 애니메이션에 항상 적용한다.
- **도구는 캐릭터 옆에 별도 아이콘을 띄우는 방식을 쓰지 않는다.** 대신 도구를 든
  캐릭터의 "들고 있는 모션"과 "실제로 쓰는 모션" 프레임 자체에 도구를 쥔 손 모양을
  그려 넣는다 — 도구별로 캐릭터 전체가 다시 그려진 애니메이션 프레임 세트(방향별)가
  생긴다 — 어떤 도구가 있고 각각 무엇을 하는지는 「생활 스킬 — 채집 계열」의 도구 표가
  원본이고, **새 도구를 붙이는 순서는 「새 도구를 추가하는 절차」에 있다.**
  도구별 애니메이션에는
  "들고 있기"/"사용하기" 말고도 반드시 "그 도구를 든 채 걷기" 세트도 있어야 한다.
  근거: 아이콘을 캐릭터 옆에 띄우고 위치만 옮기는 방식은 캐릭터 자체가 정적이고 도구가
  손과 무관하게 붙어 있어서 생기는 구조적 문제였다 — 코드 조정(오프셋/z_index)으로는
  한계가 있고, 캐릭터+도구를 하나의 그림으로 통합해서 다시 그려야 한다.

(2026-09-07, INBOX #15 — 걷기를 실제로 만들면서 정한 구체값이다.)

| 항목 | 값 |
|---|---|
| 걷기 프레임 | **한 바퀴 6프레임** (`gen_character.py` 의 `WALK_FRAMES`) |
| 재생 속도 | **14fps** (`player_frames.gd` 의 `WALK_FPS`) — 0.43초에 두 걸음 |
| 시트 | `player_walk_<머리모양>.png`, 행 = 방향 / 열 = 프레임 (idle 과 같은 규칙, 아트 **17px** 칸) |

(2026-09-07, INBOX #24 — 첫 도구(도끼)를 실제로 만들면서 정한 구체값이다.
**남은 도구 6종도 이 값에서 출발한다.**)

| 항목 | 값 |
|---|---|
| 도구 하나가 붙이는 모션 | **3종** — `hold_<도구>`(1프레임) / `use_<도구>`(6프레임) / `walk_<도구>`(6프레임) |
| 사용 모션 재생 속도 | **12fps** (`player_frames.gd` 의 `USE_FPS`) — 0.5초에 한 번 내려친다 |
| 시트 | `player_<모션>_<머리모양>.png` — 머리모양 4종 × 3모션 = **도구 하나당 12장** |
| 아이템 아이콘 | `item_<도구>.png` (17px 한 칸) |
| 바닥에 놓인 그림 | `ground_<도구>.png` (**12px 한 칸** — 위 「바닥 드롭」) |
| 도구 목록이 적힌 곳 | `gen_character.py` 의 `TOOLS` / `player_frames.gd` 의 `TOOLS` / `qa_sprite_check.py` 의 `TOOL_NAMES` **세 곳뿐이다** |

- **사용 모션이 걷기와 같은 6프레임인 것은 그림 사정이다** — 4장으로 줄이면 도끼가
  한 프레임에 도는 각도가 커져 「이어짐」 상한을 넘고, 무엇보다 올라갈 때와 내려올
  때가 **같은 그림**이 되는 자리가 생긴다(걷기에서 위상을 반 칸 밀었을 때와 같은 실패).
- **재생만 12fps 로 걷기(14)보다 느리다** — 도끼질이 걸음보다 무겁다. 더 빠르면
  도끼가 순간이동하는 것처럼 보인다.
- (2026-09-07, INBOX #26 — **두 번째 도구(곡괭이)를 붙이면서 알게 된 것**이다.
  남은 5종도 여기서 출발한다.)
  - **도구가 늘어도 고치는 곳은 「도구 목록이 적힌 곳」 세 곳뿐이었다.** 곡괭이는
    거기에 이름을 넣고 `gen_character.py` 에 머리 모양 하나를 더한 게 전부다 —
    `player.gd`·`world.gd`·씬은 한 줄도 안 고쳤다(INBOX #25 가 깔아둔 배선 그대로).
  - **도구끼리 실루엣이 겹치지 않는지는 그림 쪽 문제다** — 도끼와 곡괭이는 둘 다
    자루 끝에 머리가 달렸지만, **도끼는 한쪽으로 넓은 쐐기 / 곡괭이는 좌우 대칭
    갈고리**로 갈랐다. 자세한 것은 `docs/STYLE_GUIDE.md` 「손에 쥔 도구」.
  - **사용 모션은 도구마다 다르다.** 곡괭이는 도끼보다 덜 크게 휘두르고 앞으로
    찍는다(바위벽을 향해 치는 동작이라 머리 위로 넘기지 않는다) — 값은
    `gen_character.py` 의 그 도구 설정에만 있고, 재생 속도(`USE_FPS` 12)와
    길이(`USE_TICKS` 30틱)는 도구 공통이다.
  - **자체 QA 에 도구 이름을 박아두지 않는다.** `qa_hotbar.gd` 가 "시트가 없는
    도구"의 예로 곡괭이를 박아뒀다가 이번 바퀴에 거짓 실패를 냈다 — 지금은
    아이템 표에서 그때 남아 있는 것 하나를 골라 쓴다.
- (2026-09-07, INBOX #27 — **세 번째 도구(낫)를 붙이면서 알게 된 것**이다.)
  - **휜 날은 `capsule` 토막을 이어 붙여 그리면 안 된다.** 곡괭이 갈래는 그렇게
    그렸지만 그건 굵기가 일정해도 되는 갈고리였다 — 날은 **가운데가 두껍고 양끝이
    뾰족해야** 날로 읽힌다. 낫은 **원에서 어긋난 원을 도려낸 초승달**(`crescent`)
    이다. 자세한 것은 `docs/STYLE_GUIDE.md` 「손에 쥔 도구」.
  - **도구가 길면 사용 모션이 칸에 안 들어간다.** 자루 + 머리가 6px 을 넘으면
    자루가 수평에 가까워지는 순간 날이 칸 밖으로 나간다(「잘림」). 낫은 도끼보다
    팔꿈치를 훨씬 깊게 접어(`swing_tuck` 4.0 vs 2.2) 해결했다 — 자루를 짧게
    하는 것만으로는 모자랐다.
  - **가로베기와 내리찍기는 각도 진폭이 아니라 손이 가는 방향으로 갈린다.**
    도끼는 자루가 82도 ↔ -60도를 돌지만, 낫은 44도 ↔ -24도만 돌고 대신 **손이
    몸 앞을 가로지른다**(`swing_fwd` 가 음수 — 날이 내려갈수록 손이 앞으로).
- (2026-09-07, INBOX #28 — **네 번째 도구(총)를 붙이면서 알게 된 것**이다.
  **양손으로 잡는 첫 도구**라 여기서 틀이 한 번 넓어졌다.)
  - **양손 도구는 `tool_pose()` 가 두 손을 같이 움직인다**(`support`). 지금까지는
    한 손만 도구를 잡는다는 전제였다 — 받치는 손은 **자기 어깨에서 조금만**
    움직이고(크게 옮기면 팔이 몸을 가로지르는 막대가 되어 「각짐」에 걸린다),
    되튐에도 두 손이 같이 밀린다(한 손만 밀리면 총이 손에서 빠져 보인다).
  - **쥐는 손이 도구마다 다를 수 있다**(`hand`). 총구가 화면 오른쪽을 보므로
    방아쇠 손은 **뒤쪽(화면 왼쪽)** 손이다 — 다른 셋은 오른손 하나로 든다.
  - **옆모습에만 다른 값을 줄 수 있다**(`reach_side` / `drop_side`). 앞/뒷모습은
    어깨가 7px 라 손을 몸 밖으로 내보내야 자루가 안 묻히지만, **가로로 긴 도구**는
    옆모습에서 그만큼 더 내밀면 총구가 칸 밖으로 나가고(「잘림」) 허리띠 줄을
    통째로 덮는다(「비율」이 몸통과 다리를 가르는 줄을 잃는다).
  - **되튐은 한쪽으로만 가는 모션이다**(`swing_bias`). 도끼·곡괭이·낫은 자루가
    가운데를 두고 위아래로 왕복하지만, 총은 **`hold` 자세에서 뒤로 밀렸다 돌아올
    뿐**이다 — 앞으로도 나가게 두면 총이 노를 젓는 것처럼 보인다. 위상도 밀어서
    **0번과 5번 프레임이 쉬는 자세**가 되게 한다(`swing_phase`). 그래야
    `hold` → `use` → `hold` 로 넘어가는 순간 총이 튀지 않는다.
  - **되튐은 캐릭터를 밀지 않는다** — 팔과 총만 움직인다(「자리」 검사가 몸이
    칸 안에서 미끄러지는 것을 잡는다). 탑다운에서 "위로 튀는" 반동은 없다
    (위 「총기 스탯」의 2026-09-07 정정과 같은 이유다).
- (2026-09-07, INBOX #29 — **다섯 번째 도구(괭이)를 붙이면서 알게 된 것**이다.
  **도끼와 같은 원시 도형(`wedge`)을 쓰는 첫 도구**라, 겹치지 않게 만드는 값이
  전부 여기서 나왔다.)
  - **도끼와 갈리는 것은 날의 방향이다.** 도끼는 쐐기의 축이 자루를 따라 내려가
    잘린 날 변이 **자루와 나란히 선다(세로)**. 괭이는 축이 자루에 **수직**이라
    그 변이 **가로로 눕는다** — 밭을 미는 날이다.
  - **날만 눕혀서는 안 됐다.** 쐐기를 눕혀 자루 끝에 바로 붙이면 17px 에서 그냥
    **가로로 뻗은 쇠막대**(자/칼)로 읽힌다. 괭이를 괭이로 만드는 것은 날이 아니라
    **꺾임**이다 — 자루가 곧게 서고, 그 끝에서 쇠가 한 칸 바깥으로 나갔다가
    직각으로 내려온다. 그래서 괭이머리는 **목(`capsule`) + 날(`wedge`)** 두
    토막이고, 둘 사이에 자루가 보이는 **빈 칸이 한 줄** 남는다(곡괭이 갈래 끝의
    "한 칸 빈틈"·낫의 오목한 안쪽과 같은 장치다).
  - **날을 걸어 내리는 길이가 도끼·곡괭이와 갈라놓는 나머지 절반이다.** 한 줄만
    내리면 갈고리라 곡괭이 갈래 한 짝처럼 보이고, 두 줄을 내려 **아랫변이 곧은
    널판**이 되어야 괭이로 읽힌다.
  - **사용 모션은 도끼보다 덜 들고 더 깊이 내린다**(0도 ± 62도 대 도끼 11도 ±
    71도). 괭이는 머리 위로 넘기는 도구가 아니라 가슴께에서 들어 **발 앞의 땅**
    을 찍는 도구다 — 마지막 프레임에서 날이 발 앞에 닿는다.
- (2026-09-07, INBOX #30 — **여섯 번째 도구(물뿌리개)를 붙이면서 알게 된 것**이다.
  **날이 없는 첫 도구**라, 다섯 도구가 공유하던 "막대 + 그 끝의 얇은 쇳덩이"라는
  틀이 여기서 처음 깨졌다.)
  - **머리를 손 위에 올리지 않는다 — 손 아래에 매단다**(`hold` 각도가 음수인 첫
    도구다). 다섯 도구는 전부 자루를 세워 머리가 어깨~머리 높이에 오는데, 통을
    거기 올리면 **얼굴 옆의 쇳덩어리**라 무엇을 들었는지 안 읽힌다. 통은 들고
    다니는 것이라 허리 아래에 매달려야 물통으로 읽힌다.
  - **그 자리가 17px 칸에서 유일하게 넓은 곳이기도 하다.** 어깨 옆은 세 칸
    남짓이라 통(3px)과 주둥이(2px)를 같이 넣을 수 없고, 다리 옆은 다섯 칸이라
    둘 다 들어간다. 어깨 옆에 욱여넣은 후보는 전부 **주둥이가 통에 먹혀** 그냥
    쇳덩이가 됐다.
  - **가는 관과 그 끝의 살수구를 따로 그리면 실루엣이 조각난다.** `capsule`(관)
    + `wedge`(살수구)로 나눈 후보는 축소 뒤에 살수구가 **대각선으로만 붙은 한 칸**
    이 됐다(실루엣 검사는 4-이웃으로 센다). 뿌리에서 끝으로 벌어지는 `wedge`
    **하나**로 그리면 관과 살수구가 한 붓에 나온다.
  - **사용 모션은 총과 같은 한쪽짜리 모션이다**(`swing_bias`) — 내리찍기가 아니라
    `hold` 자세에서 기울었다 돌아오는 붓기다. 다만 **통이 손 아래 3px 에 매달려
    있어서 각도만 바꾸면 통이 몸 안으로 들어온다** — 기우는 동안 손을 그만큼
    바깥으로 밀어야 한다(`swing_fwd` 2.8, 통과 구간은 위로 「잘림」·아래로
    "휘두르는 것처럼 보임"에 막혀 있다).
  - **물줄기는 그리지 않았다** (INBOX #30 이 판단에 맡긴 것이다). 17px 에서 물은
    1px 짜리 줄이라 **재질 하나를 새로 늘려야 하는데**(「그래픽 파이프라인」의
    재질별 4단계 램프), 그 대가로 얻는 게 프레임 6장 중 2~3장의 한두 칸이다.
    게다가 **물이 떨어지는 자리는 방향마다 다르다** — 앞모습에서 "앞"은 화면
    아래(=자기 발밑)라 물을 자기 발에 붓는 그림이 된다. 실제 물주기 효과가
    2단계라 그때 밭 타일과 같이 정하는 쪽이 맞다.
- **낚싯대**(2026-09-07, INBOX #31 — 「그래픽 파이프라인」의 절차대로 붙인 일곱 번째
  도구이자 마지막 도구다):
  - **사용 모션(`use_fishing_rod`)이 없는 유일한 도구다**(사람 결정). 낚시의 피드백은
    캐릭터 자세가 아니라 **찌가 날아가 물에 떨어지는 것**이고 그건 그림이 아니라
    던지기 로직이라 2단계다. 위 도구 표에서 낚싯대만 "대상 없이 휘두르기 X" 인 것과
    같은 자리다 — **그래서 이 도구만 시트가 8장(모션 2종 × 머리모양 4종)이다.**
    `hold`/`walk` 는 만든다: 그게 없으면 낚싯대를 든 게 안 보여서 위 "도구를 캐릭터
    옆에 아이콘으로 띄우지 않는다"가 깨진다.
  - **사용 모션이 없는 도구는 좌클릭해도 자세가 안 바뀐다.** 없는 `use_` 로 갔다가
    빈손 모션으로 물러나면 0.5초 동안 손에 든 낚싯대가 사라지는데, 그건 바로 위
    규칙과 정면으로 어긋난다. 목록은 `player_frames.gd` 의 `NO_USE_TOOLS` 이고
    생성기(`gen_character.py` 의 `has_use()`)와 짝이다.
  - **머리가 없는 유일한 도구다.** 앞의 여섯은 전부 "자루 + 그 끝의 쇳덩이(또는 통)"
    인데 낚싯대는 **자루가 그대로 도구 전체**다 — 갈리는 자리가 머리 모양이 아니라
    **길이와 굵기의 변화**다.
  - **릴 두 칸이 낚싯대를 낚싯대로 만든다**(총의 탄창 한 칸과 같은 성격이다).
    쇠가 자루 끝이 아니라 **손 옆**에 있는 유일한 도구이기도 하다 — 없으면 실루엣이
    통째로 휜 나뭇가지·활이 된다.
  - **줄과 찌는 그리지 않았다.** 줄은 17px 에서 1px 짜리 선이라 재질을 새로 늘려야
    하는데, 무엇보다 **던지지 않은 낚싯대의 줄은 대에 붙어 있어서** 대와 겹친 한 줄이
    되어 보이지 않는다. 찌가 날아가는 것은 2단계이므로 그때 같이 정한다(물뿌리개의
    물줄기와 같은 판단이다).
- **도구 색은 커스터마이징을 따라가지 않는다.** 자루(나무)·날(쇠) 램프는 고정색이라
  어떤 외형을 골라도 같은 도끼다 — 팔레트 교체가 기준색 램프에 있는 색만 갈아끼우기
  때문에 저절로 그렇게 된다(`qa_player_walk.gd` 가 픽셀로 확인한다).

- **손에 든 도구가 캐릭터에 그대로 보인다** (2026-09-07 사람 결정). 핫바에서 도끼를
  고르면 캐릭터가 `hold_axe` 로 서고, 걸으면 `walk_axe`, 좌클릭하면 `use_axe` 가
  한 번 재생된 뒤 다시 `hold_axe` 로 돌아온다. 빈손이면 예전대로 `idle`/`walk` 다.
  - **사용 모션은 대상이 없어도 나온다**(낚싯대 제외 — 「생활 스킬 — 채집 계열」).
  - **멀티플레이 대비**: "몇 번 칸을 들었는가"와 "좌클릭했는가"는 **입력**이다
    (「서버 권위」의 "입력만 보낸다"). 서버가 좌클릭의 결과(무엇을 캤는가)를
    계산하려면 그 시점에 무엇을 들고 있었는지를 알아야 하므로, 든 칸은 화면 상태가
    아니라 플레이어 상태로 다룬다.
  - (2026-09-07, INBOX #25 — 실제로 만들면서 정한 구체값이다.)
    | 항목 | 값 |
    |---|---|
    | 입력에 실리는 것 | `player_input.gd` 의 `hotbar`(든 **칸 번호**) + `use`(좌클릭) |
    | 든 칸이 사는 곳 | `player_motion.gd` 의 `held_slot` — 코어가 들고 있는 플레이어 상태다 |
    | 한 번 쓰는 시간 | **30틱 = 0.5초** (`player_motion.gd` 의 `USE_TICKS`) |
    - **입력에 담는 것은 아이템 id 가 아니라 칸 번호다.** 서버는 그 칸에 무엇이 들어
      있는지 자기 인벤토리에서 직접 보므로, 클라이언트가 "나는 도끼를 들었다"고
      주장할 자리가 없다.
    - **어느 모션을 그릴지는 노드가 정한다**(그리기다). 우선순위는 **쓰는 중 → 걷는 중
      → 서 있기**이고, **시트가 없는 도구를 들면 조용히 빈손 모션으로 물러난다** —
      도구 7종 중 그림이 있는 것만 `player_frames.gd` 의 `TOOLS` 에 있고, 나머지를
      들어도 에러로 죽지 않는다.
    - **`USE_TICKS` 는 그림 길이(`use_<도구>` 프레임 수 ÷ `USE_FPS`)와 같아야 한다** —
      어긋나면 짧은 쪽이 끝난 뒤 도끼가 멈춘 그림으로 얼어 있거나 모션이 잘린다.
      `qa_hotbar.gd` 가 둘을 직접 견준다.
    - **사용 모션만 도는(loop) 애니메이션이 아니다**(`player_frames.gd` 의 `plays_once`).
      돌게 두면 `hold_` 로 넘어가기 전에 두 번째 스윙이 시작되어 그림이 튄다.
    - **좌클릭 키는 `world.gd` 의 `_unhandled_input` 한 곳**이고 **창이 하나라도 열려
      있으면 안 먹는다** — 인벤토리에서 아이템을 끄는 좌클릭과 부딪힌다.
    - 검증은 `game/qa/qa_hotbar.gd`.
- **모션이 늘어나도 노드는 안 고친다.** 어떤 모션이 있고 몇 fps 인지는
  `player_frames.gd` 의 `MOTIONS` 한 곳에만 있다 — 거기에 한 줄 늘리면 시트가 같이
  실려서 `<모션>_<방향>` 애니메이션이 생기고, 색 바꿔치기(`character_sprite.gd` 의
  `sprite_frames()`)도 자동으로 그 모션까지 칠한다. 도구별 모션은 이 자리로 붙인다.
- **걷기 시트도 반드시 같이 칠해야 한다.** idle 만 칠하면 걷기 시작하는 순간 캐릭터가
  기준색으로 되돌아간다 — `qa_player_walk.gd` 가 네 방향 × 모든 프레임을 픽셀로 견준다.
- **한 걸음은 타일 1칸쯤이다.** 이동 속도 240(타일 5칸/초)에 14fps × 6프레임이면
  초당 약 4.7걸음이다. 더 느리게 재생하면 발이 땅에서 미끄러지는 것처럼 보인다.
  - (2026-09-07, INBOX #20) **재생 속도를 10 → 14fps 로 올린 것은 캔버스가 34 → 17px
    로 절반이 됐기 때문이다.** 그릴 수 있는 보폭도 절반이 됐는데(다리가 세 줄뿐이다)
    이동 속도 240 은 그대로라, 10fps 로는 한 걸음이 **캐릭터 키의 1.4배**(타일 1.5칸)가
    되어 걷는 게 아니라 미끄러지는 것으로 보인다. 걸음 빈도는 다리 길이의 제곱근에
    반비례하므로(길이가 절반이면 √2 배) 10 × 1.41 ≈ 14 이고, 그러면 한 걸음이 캐릭터
    키만큼으로 줄어든다. **이동 속도 240 은 건드리지 않았다** — 그건 전투/이동 감각
    쪽 값이라 그림 사정으로 바꿀 값이 아니다.
- **17px 걷기에는 몸이 가라앉는 바운스가 없다**(2026-09-07, INBOX #20 — `CFG` 의
  `bob=0`). 도트에서 몸을 내릴 수 있는 최소 단위가 1px 인데, 캔버스가 절반이 되면서
  그 1px 이 몸 높이의 1/14 가 됐다 — 목이 사라지고 머리가 어깨에 얹혀서 **튀는 게
  아니라 주저앉는** 것으로 보인다. 위아래 움직임은 **발이 뜨는 것**(`lift`)이 맡는다.
- 프레임을 만드는 자세 계산은 `gen_character.py` 의 `walk_pose()` 한 곳에 있고,
  **형태 정의는 프레임마다 다시 쓰지 않는다** — 같은 `character()` 에 오프셋만 넣는다.
  그래서 프레임 사이에서 캐릭터가 차지하는 크기가 어긋날 수가 없다(위 두 번째 규칙).
- 검증은 `game/qa/qa_sprite_check.py` 의 「이어짐」(그림 자체 — 연속 프레임 사이의
  변화량이 너무 크거나 너무 작지 않은지, idle 에서 걷기로 넘어갈 때 뚝 끊기지 않는지)과
  `game/qa/qa_player_walk.gd`(엔진 쪽 — 애니메이션이 실렸는지, 칠해졌는지, 키에 맞춰
  도는지). 새 모션을 만드는 바퀴는 둘 다 넓혀서 그 모션도 보게 할 것.

## 아이템/오브젝트 크기 표준 (앞으로 나올 모든 아이템에 항상 적용)

- **캐릭터는 타일 두 칸 높이다. 이 비율이 이 절에서 가장 중요한 값이다.**
  (2026-09-07 사람 결정 — 아래 「한 번 깨뜨린 적이 있다」 참고.)

  | | 아트 | 씬 스케일 | 화면 |
  |---|---|---|---|
  | 캐릭터 | **32px** | 3배 | **96px** |
  | 타일 | 16px | 3배 | 48px |
  | | | | **= 2.00 칸** |

  - **왜 2칸인가**: 스타듀밸리(타일 16px / 캐릭터 16×32px)와 Fields of Mistria(실측 —
    타일 54 화면px / 캐릭터 약 108px) 둘 다 정확히 2칸이다. **이 비율이 건축의
    세밀함을 정한다** — 벽 한 칸이 캐릭터 절반 높이라야 무언가를 지었을 때 캐릭터가
    안 가려진다.
  - **한 번 깨뜨린 적이 있다.** 2026-09-07 아침에 "캐릭터를 절반으로"라는 요구를 받고
    캐릭터 캔버스만 34 → 17px 로 줄였는데, 그 결과 비율이 **2.12칸 → 1.06칸**이 되어
    **정반대**가 됐다(타일 한 칸이 캐릭터 키만 해져서 벽 하나가 캐릭터를 통째로 가린다).
    사람이 줄이고 싶었던 것은 **캐릭터가 아니라 타일**이었다 — 정확히는 **둘의 비**였다.
    **"캐릭터가 크다/작다"는 요구가 오면 먼저 이 비율을 확인할 것.**
  - **비율은 씬 스케일과 무관하다** — 아트 픽셀 수의 비(32 : 16)만으로 정해진다.
    스케일을 바꾸면 둘 다 같이 커지거나 작아질 뿐 비율은 안 바뀐다.

- **가로 폭은 강제하지 않는다** (2026-09-08 사람 결정 — *"1:2 를 안 지켜도 돼"*).
  스타듀가 16×32(1:2)라서 한때 그걸 목표로 잡았지만, 사람이 고른 참고 자료
  (`docs/design_reference/ref_farmer_ai.png`)가 그렇지 않아서 제약을 풀었다.
  **높이(=타일 두 칸)만 지키고 폭은 그림이 정한다** — 칸이 32×32 이므로 폭은 최대 32 다.
  (**2026-09-08, INBOX #49 정정**: 여기 한때 참고 자료가 **24×28** 이라고 적혀 있었는데,
  24 는 `ref_farmer_32.png` **파일의 폭**이고 그림은 그 안에 15칸만 쓴다 — 실측은
  **15 × 28 = 1 : 1.87** 이다. 지금 idle 은 그 비에 맞춘 **18 × 32 = 1 : 1.78** 이다.)
- **기준 크기**: 플레이어가 화면에서 차지하는 높이는 약 **96px**(캔버스 **32px** × 씬
  스케일 3배)다. 이걸 "보통 크기" 기준점으로 삼는다.
  (**2026-09-07 사람이 절반으로 낮췄다** — 이전 값은 102px(캔버스 34px × 3배)였다.
  "캐릭터를 작게 만들어줘 … 지금의 반으로 줄여도 될것같아". **줄이는 방법으로 씬
  스케일이 아니라 캔버스를 골랐다**: 스케일을 1.5배로 낮추면 캐릭터의 아트 픽셀만
  타일(아트 16px × 3배)의 절반 크기가 되어, 위 「월드 생성」의 "타일 크기 48은
  캐릭터와 같은 도트 크기 단위에서 나왔다"와 STYLE_GUIDE 1번(정수 배율)을 동시에
  깬다. 캔버스를 줄이면 도트 하나의 화면 크기는 그대로 3px 이라 어울림이 유지된다.
  대가는 그릴 수 있는 도트 수가 1/4 로 준다는 것이고, 그래서 눈·옷 포인트 같은
  디테일 기준은 STYLE_GUIDE 에서 함께 다시 잡는다.)
- **최소 크기**: 도구/드롭 아이템/작은 채집물처럼 "특별히 커야 할 이유가 없는" 오브젝트는
  화면에서 **최소 32px**(가로/세로 중 짧은 쪽 기준) 이상으로 보이게 만든다.
  (2026-09-07 — 캐릭터가 96px 로 돌아오면서 하한도 같은 비율로 16 → 32px 이 됐다.
  이 값은 원래 도구 아이콘이 32×32 인 데서 나온 값이라 되돌아온 것이다.)
  **이보다 작게 만들지 않는다.** (2026-09-07 — 기준 크기를 절반으로 낮추면서 같은
  비율로 32px 에서 내렸다. 하한이 그대로 32px 이면 도구가 플레이어(51px)의 2/3 만
  해져서 "주먹만 한 도끼가 플레이어보다 커지는" 쪽으로 넘어간다. 인벤토리 UI 의 도구
  아이콘이 32×32 인 것은 그대로다 — 그건 화면 UI 크기이지 월드에 놓이는 크기가 아니다.)
- **큰 오브젝트는 커도 된다**: 나무, 큰 가구, 벽/건축물, 목장 같은 구역, 덩치 큰 동물처럼
  원래 커야 하는 것은 이 최소 크기보다 훨씬 커도 상관없다 — 이 규칙은 하한선이지 상한선이
  아니다. 다만 그 카테고리 안에서는(예: 나무들끼리) 서로 크기가 들쭉날쭉하지 않게 통일할 것.
  단, **격자 기반 건축물(벽/문/창문)만은 예외** — 이건 격자 칸과 반드시 1:1로 맞아야
  하므로(위 "건축/방 시스템" 참고) 이 크기 표준보다 격자 정합이 우선한다.
- 새 아이템을 만들 때마다 "이게 특별히 커야 하는 이유가 있는가?"를 먼저 판단하고,
  없으면 위 최소 크기 기준을 지키고, 있으면 플레이어 크기(96px)와 비교해서 비율이
  터무니없지 않은지(예: 주먹만 한 도끼가 플레이어보다 커지는 등) 확인한다.

## 그래픽 파이프라인

- **모든 그림/애니메이션(캐릭터·동물 같은 유기체 포함)은 파이썬 Pillow로 절차적
  생성한다.** 외부 API는 쓰지 않는다.
- **AI 로 만든 그림은 「참고 자료」로만 쓴다 — 자산으로는 쓰지 않는다**
  (2026-09-08 결정. 로컬 ComfyUI + SDXL 을 이 용도로 붙였다).
  - **왜 자산으로 못 쓰나** — 실제로 해보고 확인한 것이다. ① 1024px 그림을 32px 로
    줄이면 얼굴이 뭉개진다. ② 색이 347개라 **커스터마이징의 팔레트 교체가 성립하지
    않는다**(무엇이 피부고 무엇이 옷인지 구분이 안 된다). ③ 필요한 것은 한 장이 아니라
    **4방향 × (idle·걷기·도구 7종×3모션) × 머리모양 4종 = 92장**인데, AI 는 같은
    캐릭터를 92번 일관되게 못 그린다 — 방향만 바꿔도 다른 사람이 된다. 이게 「캐릭터
    애니메이션」이 절차 생성을 고른 이유 그대로다.
  - **참고 자료로는 값을 한다** — 비율, 그리고 **어디를 갈라야 사람으로 보이는가**
    (팔이 몸통 옆에 보이는 것 · 두 다리가 갈리는 것 · 신발이 바지와 구분되는 것).
    글로 "팔을 넣어라"라고 지시하는 것보다 그림 한 장이 훨씬 정확하다.
  - 참고 자료는 `docs/design_reference/` 에 두고 **어떻게 만들었는지 함께 적는다.**
- **합격 기준은 예술적 완성도가 아니라 "이질감이 없는가"다 — 이게 최우선이다.**
  1) **어울림**: 새로 만드는 아이템/캐릭터가 이미 있는 것들과 같은 팔레트·같은 도트
     크기 단위·같은 외곽선 스타일을 쓰는가 — 기존 자산 몇 개를 실제로 나란히 놓고
     비교해서 확인한다. 나머지와 동떨어져 보이면 불합격.
  2) **자연스러움**: 움직이는 것(애니메이션)은 프레임 사이 전환이 매끄럽고 실제
     동작처럼 보이는가 — idle과 동작이 다르게 구별되고, 방향 4개의 자세가 서로 크게
     어긋나지 않는가.
  3) **퀄리티** (2026-09-06 상향 — 이전에는 "엉성한 도트여도 괜찮다"였다):
     **누가 봐도 "퀄리티 좋다"는 말이 나올 때가 멈추는 시점이다.** 이질감이 없는
     것만으로는 부족하다 — 목표는 코어키퍼를 보는 느낌이다. 바꾼 이유: 낮은 기준을
     문서에 적어두니 매 바퀴가 그 선에서 멈춰서, 결과물이 "네모를 쌓은 것" 수준에
     머물렀다. 기준선이 곧 결과물의 천장이 된다.
     - **한 번 뽑고 끝내지 않는다.** 후보를 여러 개 만들어 나란히 놓고 보고, 가장
       나은 것을 골라 다시 다듬는 것을 만족할 때까지 반복한다(Pillow 는 무료다).
     - **미달이면 커밋하지 않는다.** 적당한 결과로 항목을 닫지 말고, 미완료로 두고
       `docs/STATUS.md` 에 어디가 부족한지 적는다 — 다음 바퀴가 이어받는다.
     - 구체적인 그림체 기준·파이프라인·체크리스트는 `docs/STYLE_GUIDE.md` 에 있다.
- 여러 결과를 만든 뒤 그중 괜찮은 것을 골라서 채택하는 방식(생성 → 검수 → 채택)도
  좋다 — Pillow는 무료라 여러 후보를 만드는 데 제약이 없다.
- **여러 색상 × 여러 방향 × 여러 모션을 한 번에 다 만드는 항목을 만들지 않는다.**
  먼저 기준 색상 1개(보통 green)로 필요한 방향·모션 전체를 완성해서 품질 검증까지
  끝내는 항목 하나, 그다음 그 방식을 나머지 색상(blue/red 등)에 그대로 적용해서
  확장하는 항목 하나 — 이렇게 최소 두 항목으로 쪼갠다. 근거: 한 번에 다 만들려고
  하면 한 바퀴의 예산/시간 안에 못 끝내고 커밋 전에 죽을 위험이 크다 — 쪼개두면
  중간에 죽어도 손실이 색상 하나 분량으로 줄어든다.

## 완성의 기준

**공통 합격 기준**: 다른 사람이 봤을 때도 퀄리티가 떨어지지 않는가가 최우선 기준이다.
이 기준을 통과하지 못하면 커밋하지 않는다.

**그림/애니메이션([DESIGN] 항목)의 구체적 기준은 위 "그래픽 파이프라인"의 "이질감이
없는가"(어울림 + 자연스러움)를 따른다.** 그 외 다음 세 가지도 항상 함께 확인한다:
- **상태가 빠지면 안 된다** — 예: 도구를 든 채 이동하면 걷기 프레임이 있어야 한다
  (idle/사용 포즈로 얼어붙어 있으면 안 됨).
- **서로 다른 상태(idle vs 사용/발사)는 시각적으로 구별돼야 한다.**
- **방향 4개가 서로 크게 어긋나면 안 된다** — 방향마다 기본 자세 자체가 달라 보이면
  안 된다.

**멈추는 시점은 "누가 봐도 퀄리티가 좋다"는 말이 나올 때다.**
(2026-09-06 상향 — 이전에는 "정교함을 더 다듬는 건 지금 단계 목표가 아니다"였다.
바꾼 이유는 위 「그래픽 파이프라인」 3) 참고: 낮게 적어둔 기준선이 그대로 결과물의
천장이 됐다.) 기능(BUILD) 진행 속도를 이유로 그림 퀄리티를 깎지 않는다 — 그림이
아직 기준에 못 미치면 그 항목을 미완료로 두고 다음 바퀴가 이어받게 하지, 미달인
결과를 커밋해서 항목을 닫지 않는다.

## 캐릭터 스타일 목표
캐릭터(사람/동물) 그림의 구체적 스타일 기준(비율/음영/외곽선/팔레트, 참고 이미지
경로)은 `docs/STYLE_GUIDE.md`에 있다 — `[DESIGN]` 태그 항목일 때만 읽는다. `[BUILD]`
항목은 그림을 만들지 않으니 필요 없다.
