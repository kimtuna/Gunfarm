# STATUS — 이번 바퀴 작업 메모

> 매 바퀴 완전히 덮어쓴다. 영구 기록이 아니다 — 지난 이력은 `git log`를 본다.

## 마지막 갱신

2026-09-07 — **만든 것 없음.** 큐가 비어서 만들 항목이 없었다. 대신 **전체 자체 QA 를
현재 HEAD 에서 한 번 다 돌려 초록인 것을 확인**했다.

## 시작할 때 이미 있던 것

없음(작업 트리가 깨끗했다).

## 이번 바퀴에 일어난 일

**바퀴가 열릴 때 미완료 항목은 `#41` 하나뿐이었고, 그건 조건이 안 찬 항목이었다**
(*"대상이 늘어나면"* — 열 수 있는 것은 아직 데스드롭 상자 하나다. `world.gd` 의
`_unhandled_input` 을 실제로 열어 확인했다: 먼저 물어보는 것은 `_try_open_death_box`
한 줄뿐이다).

**그래서 지난 두 바퀴와 같은 자리에 섰다** — 그 항목은 열 때마다 같은 결론(조건 미달)에
도달하는데 번호가 가장 작아서 큐 앞을 막고, 매 바퀴 비용만 태운다. 이번 바퀴는 그것을
사람에게 넘길 `[ASK]` 를 쓰던 중이었다.

**그 사이에 사람이 같은 문제를 먼저 고쳤다** (`c4d3f0b`, 이 바퀴가 도는 동안 들어왔다).
`#41` 을 `docs/feedback/LATER.md` 로 내리고, `PROMPT.md` 에 **「조건이 차면 할 것」은
INBOX 가 아니라 LATER.md 에** 라는 규칙을 넣었다. 이번 바퀴가 쓰려던 `[ASK]` 는 그래서
쓰지 않았다 — 물어보려던 것이 이미 답으로 들어와 있다.

**결과: 지금 INBOX 의 미완료 항목은 0개다.** 루프는 다음 바퀴에 세션을 열지 않고
「할 일이 다 떨어졌습니다」로 끝난다.

## 만든 것

**없다.** 만들 항목이 없었다. 코드는 한 줄도 안 건드렸다.

## 자체 QA — 현재 HEAD 전수 검사

만들 것이 없는 바퀴에 할 수 있는 가장 쓸모 있는 일이라 전부 돌렸다(INBOX #43 이
이렇게 발견된 전례가 있다 — 그 바퀴도 조건 미달로 비어 있었다).

- **엔진 자체 QA 22벌 전부 PASS** (종료 코드 0). 창을 띄워서 돌렸다(`--headless` 를
  붙이면 캡처가 통째로 헛돈다 — `docs/GOTCHAS.md`).
- **`qa_sprite_check.py` PASS** (`.venv/bin/python`). 그림은 이번 바퀴에 안 건드렸지만
  전수 검사라 같이 돌렸다.
- **로그의 `ERROR:` 세 건은 전부 일부러 낸 것이다** — 확인했고 고장이 아니다:
  - `qa_map` — 깨진 base64 를 넣어 「읽지 못하는 기록은 빈 지도로 떨어진다」를 본다.
  - `qa_slot_store` — 깨진 JSON 을 넣어 같은 성격의 검사를 한다.
  - `qa_player_sprite` — `player_use_fishing_rod_short.png` 를 **찾지 못하는 것이
    정답이다**(낚싯대는 사용 모션이 없는 유일한 도구다 — `NO_USE_TOOLS`).
  - `docs/GOTCHAS.md` 가 *"종료 코드로 실패를 알리고 로그의 SCRIPT ERROR 유무도 같이
    볼 것"* 이라고 해서 셋 다 열어봤다. **새로 볼 것은 없다.**
- **찾은 고장 없음.** 이번 바퀴에 새로 넣을 INBOX 항목도 없다.

## 스스로 판단해서 고친 부분

- **`[ASK]` 를 쓰다 말았다.** 쓰던 도중에 사람이 같은 문제를 더 나은 방식으로 고쳐서
  (항목을 되묻는 대신 **큐에서 내리는 자리**를 아예 만들었다) 그대로 접었다. 지금
  올리면 이미 답이 나온 물음으로 루프를 한 번 더 멈추게 할 뿐이다.
- **`LATER.md` 를 고치지 않았다** (아래 「사람에게」에 적기만 했다). 사람이 지금 이
  저장소에서 같이 일하고 있고, 큐/문서는 사람 몫이다.

## 어려움 / 에러와 해결

- **바퀴 도중에 파일이 바뀌었다.** INBOX 를 처음 읽었을 때 `#41` 은 `- [ ]` 였는데,
  같은 줄을 다시 읽으니 `- [x] … LATER.md 로 옮김` 이었다. `PROMPT.md` 가 경고한
  *"같은 저장소에 다른 세션이 겹쳐서 커밋할 수 있다"* 그대로다 — 지시대로 **당황하지
  않고 커밋 메시지로 출처부터 확인**했고(`c4d3f0b`, 사람), **파일이 겹치지 않는다**
  (그쪽은 `PROMPT.md`/`INBOX.md`/`LATER.md`/`index.html`, 이쪽은 `STATUS.md` 하나다).
  손실 위험 없음.

## 사람에게 — 다음에 무엇을 넣을지

**큐가 비었다.** `LATER.md` 를 읽어보니 다음 한 칸을 정하는 갈림길이 이미 그 안에
적혀 있다 — *"다음에 필요한 것은 도구 항목이 아니라 **자원 오브젝트를 월드에 놓는
항목**이다."* 지금 바로 열 수 있는 후보 셋과 각각이 여는 문:

- **나무(logging_point)** — `bullets.gd` 의 **`blocks_bullet` 에 처음으로 무언가가
  꽂힌다**(지금 총알은 사거리 800 을 다 날아가야만 사라진다 — 고장이 아니라 막는 게
  없어서다). 덤으로 도끼 2단계(벌목)와 **목재**가 열린다.
- **사슴 + 동물 AI** — `bullets.gd` 의 **`hit_test` 자리가 채워진다.** 총이 처음으로
  무언가를 맞히고, 「체력 / 죽음 / 리스폰」이 *자체 QA 가 코드로 데미지를 주는* 상태를
  벗어난다. 마취탄·포획도 여기서 열린다. **가장 큰 항목이라** [DESIGN](그림) 과
  [BUILD](AI·판정) 로 쪼개야 한다.
- **설치형 저장 상자** — 「테스트용 저장 상자 자동 보급」이 아직 대역(`world.gd` 의
  `STARTER_ITEMS`)으로 돌고 있는 것이 제자리를 찾고, **`LATER.md` 로 내려간 「여는
  좌클릭 모으기」의 조건이 그 바퀴에 바로 찬다**(열 수 있는 것이 둘이 된다). 대신
  「건축 / 방 시스템」의 격자·배치·철거가 아직 없어서 **상자를 어떻게 놓을 것인가**를
  그 바퀴가 정해야 한다.

### `LATER.md` 에 이미 끝난 것 두 줄이 남아 있다

옮겨 적을 때 딸려온 듯하다. 사람 파일이라 손대지 않았다:

- **「플레이어 체력 + 죽음 + 리스폰 + 데스드롭 상자」** — *"순서는 총알 → 체력·죽음 →
  데스드롭 상자다"* 인데 **셋 다 이미 끝났다**(INBOX #34 / #36 / #37, 그림은 #40).
- **「월드 설정 화면」** — *"데스드롭 상자를 만드는 바퀴가 같이 만드는 게 자연스럽다"*
  인데 **그 바퀴(#37)가 실제로 같이 만들었다**(`world_settings_screen.gd`).

## 막힌 것 / 보류

- **없다.** 막힌 것이 아니라 **할 일이 없는 것**이다 — 큐가 비었을 뿐 코드는 전부 초록이다.
- `docs/GOTCHAS.md` 는 **123줄 그대로**다(이번 바퀴에 새로 일반화할 엔진 함정이 없었다).
  150줄 상한까지 여유가 줄고 있다는 지난 바퀴들의 메모는 계속 유효하다.
