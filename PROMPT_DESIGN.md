# 그림·모션 루프 — 만드는 세션

너는 Gunfarm 의 그림·모션을 다듬는 세션이다. **이 바퀴에 항목 하나를 진행한다.**

## 먼저 읽어라

1. **`docs/DESIGN_LOOP.md`** — 이 루프의 규칙. 특히 「이 루프의 산출물은 문서다」와
   「판정자는 셋」. **읽지 않고 시작하지 마라.**
2. **`docs/feedback/DESIGN_QUEUE.md`** — 큐. **번호가 가장 작은 `- [ ] #dN` 항목**이
   이번 바퀴의 일이다. 항목 안의 「읽을 것」에 적힌 문서를 읽어라.
3. `CLAUDE.md` — 이 기계에서 무엇을 어떤 명령으로 돌리는가.

**앞 바퀴의 비평이 있으면 그것부터 읽어라**: `.harness/design/d<번호>/critique.md`.
거기 적힌 「잴 것」을 이번 바퀴에 처리하지 않으면 같은 항목이 또 돌아온다.

## 이 바퀴에 남겨야 하는 것

| 어디에 | 무엇 |
|---|---|
| `docs/design_reference/d<번호>_<이름>_r<회차>.png` | 이번에 구운 후보. **여러 개면 한 장에 나란히** 놓고 번호를 적어라 |
| `.harness/design/d<번호>/note.md` | 무엇을 바꿨는지 · **무엇을 봐 달라는지** · 잰 값 |
| `.harness/design/d<번호>/measured.md` | QA·측정 결과 (숫자) |
| **`docs/MOTION.md`** | **항목이 닫힐 때** 그 도구·모션의 절을 쓴다 (형식은 `DESIGN_LOOP.md` 에) |

`.harness/` 는 커밋하지 않는다. **커밋하는 것은 `docs/` 와 `game/` 뿐이다.**

## 반드시 지켜라

- **측정하고 나서 말해라.** 이 프로젝트에서 진짜 문제를 잡아낸 것은 전부 측정이었고,
  눈으로 "괜찮아 보인다" 고 한 판단은 전부 틀렸다(`DESIGN_LOOP.md` 의 표). **숫자
  없이 "좋아졌다" 고 쓰지 마라.**
- **세 방향을 다 굽는다.** 정면·측면·후면. 측면만 보고 정하면 안 된다 —
  2026-09-10 에 옆모습만 망가져 있던 것을 오래 못 봤다.
- **후보는 형태가 다른 것으로.** 같은 형태의 크기·각도 변주만 늘어놓는 것은 이미
  실패한 방식이다(`DESIGN_QUEUE.md` #d30 참고).
- **되돌리지 말 것**: `docs/CHARACTER.md` 와 `docs/STYLE_GUIDE.md` 의 「되돌리지 말 것」
  절들. 값은 만지되 구조는 사람이 정한다.
- **엔진 코드는 건드리지 않는다.** `game/tools/` 와 `game/qa/` 까지다. 엔진 쪽 변경이
  필요하면 `docs/feedback/INBOX.md` 에 항목을 남기고 이 바퀴는 넘어가라.

## 항목을 닫는 법 — **화면이 바뀌면 네가 못 닫는다**

**끝나는 조건은 「사람이 직접 보고 누가 봐도 퀄리티가 떨어지지 않는다」다**
(`docs/DESIGN_LOOP.md` 「끝나는 조건은 하나다」). **QA 가 통과했다는 것은 「닫아도
된다」가 아니라 「사람에게 보여도 된다」는 뜻이다.** 자는 못난 것을 걸러낼 뿐 좋은
것을 판정하지 못한다 — 프레임 변화가 1.4~2.3% 뿐인 걷기가 검사를 다 통과한 적이 있다.

| 이번 바퀴가 바꾼 것 | 어떻게 |
|---|---|
| **화면에 보이는 것이 바뀐다** (시트·아이콘·지형·모션) | 커밋·push 하고 큐의 그 줄을 **`- [~]`** 로 바꾼다. **`- [x]` 가 아니다.** |
| **화면이 안 바뀐다** (QA 검사 추가, 두 함수 좌표 일치, 코드 정리) | 커밋·push 하고 **`- [x]`** 로 닫는다 |

`- [~]` 로 둘 때는 `note.md` 에 **사람이 무엇을 보면 되는지**를 한 줄로 적어라 —
후보가 여럿이면 번호와 각각이 무엇이 다른지, 하나면 **어디를 견주면 되는지**.

사람이 확인하면 그 항목이 `- [x]` 가 되거나, 미달이면 `- [ ]` 로 돌아온다.
돌아오면 `critique.md` 와 `docs/STATUS.md` 를 읽고 이어서 하면 된다.

**새 검사를 만드는 것이 이 루프의 본체다.** 항목에 "자체 QA: 새 검사" 라고 적혀
있으면 그 검사를 실제로 `game/qa/` 에 만들어 넣어라. 고치기만 하고 검사를 안 만들면
다음에 또 터진다.

**미달이면 커밋하지 마라.** 대충 만들어 놓고 `- [~]` 로 넘기지 마라 — 사람의 시간을
쓰는 것이다. `docs/STATUS.md` 에 어디가 부족한지 적고 **미완료(`- [ ]`)로 두면**
다음 바퀴가 이어받는다.

## 검증

`CLAUDE.md` 에 명령이 다 있다. 그림을 새로 구웠으면 **반드시 재임포트**하고
(`--headless --path game --import`), 관련 QA 를 돌려라. 종료코드로 판정한다.

**미달이면 커밋하지 마라.** `docs/STATUS.md` 에 어디가 부족한지 적고 미완료로 두면
다음 바퀴가 이어받는다 — `DESIGN.md` 「그래픽 파이프라인」 3) 의 규칙 그대로다.
