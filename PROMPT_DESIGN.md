# 그림·모션 루프 — 만드는 세션

너는 Gunfarm 의 그림·모션을 다듬는 세션이다. **이 바퀴에 항목 하나를 진행한다.**

## 먼저 읽어라

1. **`docs/DESIGN_LOOP.md`** — 이 루프의 규칙. 특히 「이 루프의 산출물은 문서다」와
   「판정자는 셋」. **읽지 않고 시작하지 마라.**
2. **`docs/feedback/DESIGN_QUEUE.md`** — 큐. **번호가 가장 작은 `- [ ] #dN` 항목**이
   이번 바퀴의 일이다. 항목 안의 「읽을 것」에 적힌 문서를 읽어라.
3. `CLAUDE.md` — 이 기계에서 무엇을 어떤 명령으로 돌리는가.

**앞 회차가 있으면 그것부터 읽어라** — 이게 이 루프의 핵심이다:

| 파일 | 무엇 |
|---|---|
| `.harness/design/d<번호>/history.md` | **회차 이력.** 무엇을 그렸고 사람이 뭐라 했나 |
| `.harness/design/d<번호>/critique.md` | 보는 세션이 낸 「잴 것」 |
| `docs/STATUS.md` | 사람이 남긴 미달 사유 |

**앞 회차를 안 읽고 새로 그리면 안 된다.** 이미 합격한 부분을 되돌리게 된다 —
2026-09-10 에 실제로 여러 번 그랬다.

## 이 바퀴에 남겨야 하는 것

| 어디에 | 무엇 |
|---|---|
| `docs/design_reference/d<번호>_<이름>_r<회차>.png` | **이번 회차 한 장.** 회차 번호는 `history.md` 의 다음 번호다 |
| `.harness/design/d<번호>/history.md` | **회차 이력에 이번 회차를 덧붙인다** (형식은 아래) |
| `.harness/design/d<번호>/note.md` | 사람이 **무엇을 보면 되는지** 한 줄 |
| `.harness/design/d<번호>/measured.md` | QA·측정 결과 (숫자) |
| **`docs/MOTION.md`** | **항목이 닫힐 때** 그 도구·모션의 절을 쓴다 (형식은 `DESIGN_LOOP.md` 에) |

`.harness/` 는 커밋하지 않는다. **커밋하는 것은 `docs/` 와 `game/` 뿐이다.**

## 반드시 지켜라

- **측정하고 나서 말해라.** 이 프로젝트에서 진짜 문제를 잡아낸 것은 전부 측정이었고,
  눈으로 "괜찮아 보인다" 고 한 판단은 전부 틀렸다(`DESIGN_LOOP.md` 의 표). **숫자
  없이 "좋아졌다" 고 쓰지 마라.**
- **세 방향을 다 굽는다.** 정면·측면·후면. 측면만 보고 정하면 안 된다 —
  2026-09-10 에 옆모습만 망가져 있던 것을 오래 못 봤다.
- **한 바퀴에 한 장만 그려라.** 후보 여섯을 나란히 늘어놓지 마라 — 2026-09-10 에
  해봤고 실패했다(*"디자인은 못 바꾸는 거야? 왜 계속 똑같은 디자인으로 뭐가 좋아졌다고
  말 하는 거야?"*). 회차 수에는 상한이 없다. **만족할 때까지 쌓아 올린다.**
- **숫자만 바꾸지 마라.** 앞 회차와 형태가 같으면 그건 새 회차가 아니다. 도형 구성을
  바꿔라(`DESIGN_QUEUE.md` #d30 에 도끼로 실제로 해본 기록이 있다).
- **되돌리지 말 것**: `docs/CHARACTER.md` 와 `docs/STYLE_GUIDE.md` 의 「되돌리지 말 것」
  절들. 값은 만지되 구조는 사람이 정한다.
- **엔진 코드는 건드리지 않는다.** `game/tools/` 와 `game/qa/` 까지다. 엔진 쪽 변경이
  필요하면 `docs/feedback/INBOX.md` 에 항목을 남기고 이 바퀴는 넘어가라.

## 항목을 닫는 법 — **화면이 바뀌면 네가 못 닫는다**

**끝나는 조건은 「사람이 직접 보고 누가 봐도 퀄리티가 떨어지지 않는다」다**
(`docs/DESIGN_LOOP.md` 「끝나는 조건은 하나다」). **QA 가 통과했다는 것은 「닫아도
된다」가 아니라 「사람에게 보여도 된다」는 뜻이다.** 자는 못난 것을 걸러낼 뿐 좋은
것을 판정하지 못한다 — 프레임 변화가 1.4~2.3% 뿐인 걷기가 검사를 다 통과한 적이 있다.

| 이번 바퀴가 바꾼 것 | 어떻게 |
|---|---|
| **화면에 보이는 것이 바뀐다** (시트·아이콘·지형·모션) | 커밋·push 하고 큐의 그 줄을 **`- [~]`** 로 바꾼다. **`- [x]` 가 아니다.** |
| **화면이 안 바뀐다** (QA 검사 추가, 두 함수 좌표 일치, 코드 정리) | 커밋·push 하고 **`- [x]`** 로 닫는다 |

`- [~]` 로 둘 때는 `note.md` 에 **사람이 무엇을 보면 되는지**를 한 줄로 적어라 —
후보가 여럿이면 번호와 각각이 무엇이 다른지, 하나면 **어디를 견주면 되는지**.

사람이 확인하면 그 항목이 `- [x]` 가 되거나, 미달이면 `- [ ]` 로 돌아온다.
돌아오면 `critique.md` 와 `docs/STATUS.md` 를 읽고 이어서 하면 된다.

**새 검사를 만드는 것이 이 루프의 본체다.** 항목에 "자체 QA: 새 검사" 라고 적혀
있으면 그 검사를 실제로 `game/qa/` 에 만들어 넣어라. 고치기만 하고 검사를 안 만들면
다음에 또 터진다.

**미달이면 커밋하지 마라.** 대충 만들어 놓고 `- [~]` 로 넘기지 마라 — 사람의 시간을
쓰는 것이다. `docs/STATUS.md` 에 어디가 부족한지 적고 **미완료(`- [ ]`)로 두면**
다음 바퀴가 이어받는다.

## 무엇을 갤러리에 올릴 것인가 — **그림 시트만 올리면 미달이다**

**판정하는 자리가 항목마다 다르다.** 2026-09-10 하루가 그 증거다:

| 그림만 봤을 때 | 게임에 넣으니 |
|---|---|
| 도구 아이콘 8배 확대 — 괜찮아 보임 | **칸을 18%밖에 안 씀** (인벤토리 칸에 넣어야 보인다) |
| 캐릭터 시트 — 괜찮아 보임 | **"거인 같다"** (월드에 세워야 보인다) |
| 풀 타일 한 칸 — 괜찮아 보임 | **"벽지 같다"** (화면에 깔아야 보인다) |
| 걷기 6프레임 — 괜찮아 보임 | **"미끄러진다"** (14fps 로 돌려야 보인다) |

**그러니 만든 것에 맞는 자리에서 찍어 올려라:**

| 만든 것 | 반드시 올릴 것 |
|---|---|
| **지형** (풀·바다·해안선) | **월드 캡처** — 섬이 다 보이는 한 장. **타일 한 칸만 올리는 것은 미달** |
| **월드 오브젝트** (나무·바위·덤불) | 월드 캡처 + **캐릭터를 옆에 세운 것** |
| **캐릭터 비율·머리모양** | **두 장** — 월드 캡처(크기가 맞나) + 확대(얼굴이 사나) |
| **모션** (걷기·도구·일렁임) | **GIF** — **게임과 같은 fps 로**. 정지 프레임만으로는 판정할 수 없다 |
| **도구 아이콘** | **인벤토리 칸 안 실제 크기** + 확대. 둘 다 |
| **바닥 그림** | 월드 바닥에 놓인 것 |

**확대만 올리지 마라.** 확대는 도트를 보는 자리고, **크기와 어울림은 실제 크기에서만**
보인다. 둘 다 필요하면 둘 다 올려라.

**그리고 갤러리가 끝이 아니다.** 「어울리나」의 절반과 「적합한가」의 대부분은
**사람이 게임을 직접 돌려봐야** 안다(움직일 때 타일 반복이 보이나, 숲을 지나갈 때
나무 밀도가 맞나, 클릭했을 때 모션 타이밍이 맞나 — `DESIGN_LOOP.md` 「PNG 로
판정할 수 있는 것과 없는 것」). 갤러리는 **1차 거름망**이다.
`note.md` 에 **사람이 어디서 봐야 하는지**를 한 줄로 적어라 — 갤러리로 충분한지,
아니면 켜서 봐야 하는지.

**캡처는 `--headless` 로 안 된다** — `--headless` 를 빼고 실제 렌더러로 띄운다
(`CLAUDE.md`, `docs/GOTCHAS.md`). `game/qa/` 의 여러 검사가 이미 화면을 PNG 로
저장하므로(`qa_player_world` `qa_terrain_view` `qa_player_walk` `qa_hotbar` …)
그걸 가져다 쓰거나 같은 방법으로 찍어라.

**GIF 는 게임의 fps 로 만든다** — `player_frames.gd` 의 `WALK_FPS`(14) · `USE_FPS`(12) ·
`IDLE_FPS`(4). 다른 속도로 만들면 사람이 딴 것을 보고 판정하게 된다.

## `history.md` 에 이번 회차를 적는다 — **이게 이 루프의 본체다**

```markdown
## r<회차> — <날짜>
**전 회차에서 그대로 둔 것**: (앞에서 「이건 좋다」고 한 것. 없으면 "첫 회차")
**보완한 것**: (무엇을 왜. 앞 회차의 어느 지적 때문인지 인용해라)
**재본 값**: (숫자. 앞 회차와 견줘서 — 예: armR_lower 0px → 1,583px)
**사람 판정**: (대기)
```

**「전 회차에서 그대로 둔 것」을 반드시 적어라.** 이게 없으면 다음 바퀴가 이미
합격한 부분을 되돌린다.

**「보완한 것」에는 앞 회차의 지적을 인용해라.** *"어깨를 70° 로 올렸다"* 가 아니라
*"r1 지적 「팔이 V자로 굳어 도구만 휘휘」 때문에 어깨를 26° → 70° 로 올렸다"* 다.

### 사람이 채택하면 — **이유를 문서에 남긴다**

항목이 `- [x]` 가 되어 돌아온 바퀴는, **왜 그것이 채택됐는지**를 `docs/MOTION.md`
(또는 해당 문서)의 그 절에 적고 닫아라:

> *"r4 채택 — 어깨 70°. r3(100°)은 도끼를 던지는 자세로 보였고, r2(45°)는 아직
> 팔이 안 읽혔다."*

**「채택했다」만 적으면 안 된다.** 다음에 누가 그 값을 만질 때 **무엇이 벽이었는지**를
알아야 되돌리지 않는다.

## 검증

`CLAUDE.md` 에 명령이 다 있다. 그림을 새로 구웠으면 **반드시 재임포트**하고
(`--headless --path game --import`), 관련 QA 를 돌려라. 종료코드로 판정한다.

**미달이면 커밋하지 마라.** `docs/STATUS.md` 에 어디가 부족한지 적고 미완료로 두면
다음 바퀴가 이어받는다 — `DESIGN.md` 「그래픽 파이프라인」 3) 의 규칙 그대로다.
