# START — 새 세션이 맨 먼저 읽는 문서

> **이 저장소를 처음 여는 AI 세션을 위한 지도다.** 무엇이 어디 있고, 무엇이
> 돌아가고 있으며, 무엇을 건드리면 안 되는지가 전부 여기 있다.
> (2026-09-10 기준. 하네스는 그대로 두고 새 세션이 이어받는 상황을 위해 다시 썼다.)

---

## 0. 한 줄 요약

**Gunfarm — Godot 4 탑다운 2D 도트 게임**(힐링·농사 + 총). 사람(감독)이 방향을
정하고, **`claude -p` 헤드리스 세션을 도는 자율 루프 둘**이 실제 작업을 한다.
세션은 이전 바퀴를 기억하지 못한다 — **기억은 전부 아래 md 파일이 대신한다.**

```
게임:   /Applications/Godot.app/Contents/MacOS/Godot --path game
그림:   .venv/bin/python                      (Pillow · numpy — 시스템 python3 엔 없다)
```

---

## 1. 루프가 둘이다 — 이것부터 이해해라

**기능과 그림은 「끝나는 방식」이 다르다.** 기능은 「통과/불통과」인데 그림은
「후보 중 고르기」다. 같은 큐에 두면 도끼 날 하나로 열 바퀴를 도는 동안 기능이 멈춘다.

| | **기능 루프** | **그림·모션 루프** |
|---|---|---|
| 명령 | `./ctl.sh start` | `./ctl.sh design start` |
| 실행기 | `loop.sh` | `loop_design.sh` |
| 큐 | `docs/feedback/INBOX.md` | `docs/feedback/DESIGN_QUEUE.md` |
| 규칙 | `PROMPT.md` | **`docs/DESIGN_LOOP.md`** (반드시 읽어라) |
| 세션 프롬프트 | `PROMPT.md` | `PROMPT_DESIGN.md` + `PROMPT_CRITIC.md` |
| 한 바퀴 | 세션 **하나** | 세션 **둘** (만드는 쪽 → 보는 쪽) |
| 만드는 것 | 엔진 코드 · 로직 · UI | **그림 파일** |
| 고치는 코드 | `game/scripts/*.gd` · `project.godot` | **`game/tools/*.py`** |
| 페이지 | [대시보드](https://kimtuna.github.io/Gunfarm/) | [갤러리](https://kimtuna.github.io/Gunfarm/design.html) |
| launchd | `com.gunfarm.loop` | `com.gunfarm.design` |
| 로그 | `.harness/loop.log` | `.harness/design.log` |

**서로의 영역을 건드리지 않는다.** 기능 루프가 그림 문제를 발견하면 `DESIGN_QUEUE` 에
항목으로 넘기고, 그림 루프가 엔진 코드가 필요하면 `INBOX` 로 넘긴다.

### QA 는 「누가 만든 것을 검사하나」로 갈린다 — 언어가 아니다

**모션은 그림 루프가 만드니 모션 QA(`qa_player_walk`)도 그림 루프 것이다.** 그건
엔진을 띄우는 `.gd` 다. 전체 목록은 `docs/DESIGN_LOOP.md` 「QA 를 어느 루프가 보나」.

| | |
|---|---|
| **그림 루프** | `qa_sprite_check.py` · `qa_character_sheets.py` · `qa_player_sprite` · `qa_player_walk` · `qa_character_sprite` · `qa_terrain_view` · `qa_hotbar`(든 도구가 보이는가) · `qa_ground_items`(바닥 그림) · `qa_player_appearance`(외형) |
| **기능 루프** | `qa_inventory` · `qa_gun_ammo` · `qa_health` · `qa_death_box` · `qa_bullets` · `qa_map` · `qa_main_menu` · `qa_settings` · `qa_character_slots` · `qa_slot_store` · `qa_character_customize` · `qa_world_entry` · `qa_world_gen` · `qa_player_world` |
| **공통** | 「자연스러운가」 · 「엔진에서 돌렸을 때 멀쩡한가」 · `qa_uid_files` |

---

## 2. 문서 지도 — 무엇이 어디 있나

### 매번 읽는 것

| 파일 | 무엇 |
|---|---|
| **`CLAUDE.md`** | **이 기계에서 무엇을 어떤 명령으로 돌리는가.** Godot 경로 · venv · ComfyUI · git 규칙(`.uid`/`.import` 커밋) |
| **`docs/DESIGN.md`** | 무엇을 만드는가. **맨 위에 「문서 지도」가 있다** — 영역별 문서와 언제 읽는지 |
| 큐 (`INBOX.md` / `DESIGN_QUEUE.md`) | 이번 바퀴에 할 일 |

### 영역별 — 그 영역을 건드릴 때만

| 파일 | 무엇 |
|---|---|
| `docs/WORLD.md` | 월드 생성 · 지형 · 오브젝트 · 지도 · 시야 · 건축 |
| `docs/COMBAT.md` | 총 · 투사체 · 조준 · 체력 · 죽음 · 데스드롭 |
| `docs/ITEMS.md` | 도구와 추가 절차 · 인벤토리 · 바닥 드롭 · 농사 · 생산 |
| `docs/UI.md` | 화면 흐름 · 커스터마이징 · 조작 |
| **`docs/CHARACTER.md`** | **캐릭터 그림 파이프라인(리그).** 「되돌리지 말 것」이 많다 |
| **`docs/STYLE_GUIDE.md`** | 도트 그림체 기준 — 팔레트 · 음영 · 도구 아이콘 |
| **`docs/GOTCHAS.md`** | **에러가 나면 여기부터 검색** — 헤드리스 캡처 안 됨 등 |

### 루프 자체

| 파일 | 무엇 |
|---|---|
| `README.md` | 하네스 설계 — 왜 이렇게 만들었나 |
| **`docs/DESIGN_LOOP.md`** | **그림 루프의 규칙 전부** — 판정자 셋 · 회차 쌓기 · 끝나는 조건 · 갤러리 |
| `PROMPT.md` · `PROMPT_DESIGN.md` · `PROMPT_CRITIC.md` | 세션에게 실제로 넘어가는 지시서 |
| `docs/STATUS.md` | 지난 바퀴가 남긴 메모 |
| `docs/feedback/LATER.md` | 나중에 할 것 |
| `docs/VISION.md` | 얼리억세스 이후 장기 비전 — **지금 범위 아님** |

---

## 3. 그림 루프가 도는 방식 — 이게 이 저장소의 핵심이다

### 한 바퀴

```
① 그림 생성      숫자를 고치고 굽는다 (game/tools/*.py)
② 캡처           게임을 띄워 찍는다 → docs/design_reference/d<번호>_<이름>_r<회차>.png
③ 자(QA)         **루프가 직접 돌린다** — 세션의 말을 믿지 않는다
④ 피드백         보는 세션이 「잴 것」을 낸다 · 사람이 갤러리/게임에서 본다
⑤ 커밋           **괜찮든 안 괜찮든 커밋한다**
      괜찮다  →  항목 닫고 다음
      미달    →  같은 항목 다시. history.md 를 물고 r+1 회차
```

### 반드시 알아야 하는 셋

**(가) 판정자가 셋이고 역할이 겹치면 안 된다.**
- **자(QA)** — 못난 것을 걸러낸다. **좋은 것을 판정하지 못한다.**
- **눈(보는 세션)** — **감상 금지.** 출력은 「무엇을 어떻게 재라」뿐이다.
- **감독(사람)** — 최종. **끝나는 조건은 「사람이 직접 보고 누가 봐도 퀄리티가
  떨어지지 않는다」 하나다.** 돈도 바퀴 수도 아니다.

> 근거: 2026-09-09~10 에 진짜 문제를 잡아낸 것은 **전부 측정**이었고(어깨 뼈 45px
> 어긋남 · 아래팔 배정 0px · 프레임 변화 1.4~2.3% · 몸이 위아래로 안 움직임),
> 눈으로 「괜찮아 보인다」고 한 판단은 **전부 틀렸다.**

**(나) 한 바퀴에 한 장만 그린다.** 후보 여섯을 나란히 늘어놓는 방식은 실패했다 —
「같은 형태의 숫자 변주」가 여섯 장 늘어서 고를 것이 없었다. 대신 **회차를 쌓는다**:
`.harness/design/d<번호>/history.md` 에 회차마다 **전 회차에서 그대로 둔 것 / 보완한
것(앞 지적을 인용) / 재본 값 / 사람 판정**을 적는다. 채택될 때는 **왜 그것이
채택됐는지**를 문서에 남긴다.

**(다) 그림 시트만 올리면 미달이다.** 판정하는 자리가 항목마다 다르다 — 지형은
**월드 캡처**, 모션은 **게임과 같은 fps 의 GIF**, 아이콘은 **인벤토리 칸 안 실제
크기**. 그리고 **「어울리나」의 절반과 「적합한가」의 대부분은 사람이 돌려봐야 안다** —
그래서 단계마다 `[플레이]` 체크포인트가 있고 **루프는 거기서 세션을 안 열고 멈춘다.**

### 항목 상태 셋

```
- [ ]  미완료            루프가 집는다
- [~]  사람 확인 대기      화면에 보이는 것이 바뀌었다. 루프는 건너뛰고 다음으로
- [x]  완료              화면이 안 바뀌는 것(검사 추가·코드 정리)만 루프가 닫는다
```

**갈래**: `[바탕]` 없으면 아무것도 평가 못 함 · `[자]` 잴 수 있음 · `[취향]` 사람 눈 ·
`[플레이]` **사람이 게임을 켜서** — 루프가 아예 손대지 않는다.

---

## 4. 사이트 둘

| | 주소 | 무엇 | 그리는 것 |
|---|---|---|---|
| **대시보드** | https://kimtuna.github.io/Gunfarm/ | INBOX 진행 · STATUS · 커밋 | `scripts/render_dashboard.py` → `docs/index.html` |
| **갤러리** | https://kimtuna.github.io/Gunfarm/design.html | 그림 후보 · 회차 이력 · 자(QA) 결과 | `scripts/render_design.py` → `docs/design.html` |

**둘은 서로 안 섞는다** — 링크 한 줄씩만 건다(*"과정을 계속 봐야 하는데 페이지가
길어진다"*). GitHub Pages `main` 브랜치 `/docs` 다.

**갤러리가 보여주는 것**: 항목마다 ① **누가 판정하는가**(플레이/취향/기계) ②
`note.md`(사람이 무엇을 보면 되는지) ③ **회차마다 [그림 | 왜 이 회차인가]** ④ 자(QA)
결과 ⑤ 보는 세션의 「잴 것」. **세션이 도는 동안 90초마다 갱신된다.**

---

## 5. 지금 상태 (2026-09-10)

### 이번 판은 **재설정**이다

사람이 *"디자인 전체적으로 다시 만들어줘"* 라고 했고, **구워진 그림 41장과 참고 이미지
43장을 전부 지웠다가** 새 규격으로 다시 구운 참이다.

| | 지금 | 목표 |
|---|---|---|
| 논리 해상도 | **1920×1080** ✅ | (1080p 1배 · 4K 2배로 정수) |
| 보이는 칸 | **40 × 22.5** ✅ | |
| 타일 아트 | 48px | 그대로 |
| 시트 칸 | 96px | 그대로 |
| 인물 키 | **72px (1.5칸)** ✅ | |
| **머리** | **21px (3.43등신)** ❌ | **26px (2.8등신)** — `comfy.FIG_HEADS` 가 3.6 그대로다 |

**`docs/design_reference/farmer_sheet4.png` 은 절대 지우지 마라** — 리그가 부품 10개를
잘라내는 밑그림이다. 이게 없으면 캐릭터를 다시 못 만든다.

### 큐

- **`DESIGN_QUEUE`** 완료 1 / 남음 33. 순서: **배경(#d2~4) → 캐릭터 바탕(#d10~15) →
  캐릭터 그림(#d20~21) → 모션(#d30~39) → 오브젝트(#d40~43) → 도구(#d50~52) → 전체(#d60)**
  단계마다 `[플레이]` 체크포인트(`#d5` `#d25` `#d45` `#d55` `#d60`).
  **배경과 캐릭터가 기준이고 나머지는 거기 맞춘다** — 오브젝트·도구를 먼저 만들면
  기준이 바뀔 때 또 고쳐야 한다.
- **`INBOX`** 완료 68 / 남음 3. `#76` 이 「새 규격에서 엔진이 멀쩡한가」다.

### 바로 다음에 할 일

1. **`#d2` r2** — 초원. r1 이 미달이었다(덤불을 더 깔아 **빽빽하게** 만들었는데,
   사람이 원한 것은 **비슷한 계열의 초원 타일을 여러 벌 만들어 붙이는 것**이다).
2. **`#d3`** — 바다를 **sprite 로 움직이게**(물이 흐르거나 파도가 치게). 지금은
   완전한 정지 그림이고 타일 애니메이션 기능이 아예 없다.
3. **`#d20`** — `FIG_HEADS` 를 2.77 로 내려 머리를 26px 로 되돌린다.

### 커밋 안 된 것

기능 루프가 `INBOX #72` 를 하다 멈춰서 **6개 파일이 커밋 안 된 채** 남아 있다
(`player_frames.gd` · `player.gd` · `bullets_view.gd` · QA 셋). 살릴지 버릴지 정해야 한다.

---

## 6. 함정 — 값을 치르고 알아낸 것들

- **캡처는 `--headless` 로 안 된다.** 빼고 실제 렌더러로 띄운다(`docs/GOTCHAS.md`).
- **새 PNG 를 만들었으면 확인 전에 반드시 재임포트**: `--headless --path game --import`.
  `game/.godot/imported/` 에 옛 캐시가 남아 옛 그림이 로드된다.
- **`.gd.uid` 와 `.import` 는 커밋한다** — 이유는 `CLAUDE.md` 에 길게 적혀 있다.
- **세션을 죽일 때 자식까지 죽여야 한다** — `kill $pid` 는 claude 만 죽여서 그 밑의
  Godot 이 고아로 남는다(실측: 38분 된 것이 CPU 를 먹고 있었다). `loop_design.sh` 의
  `kill_tree()` 참고.
- **루프가 둘이라 git 이 겹칠 수 있다.** 대시보드·갤러리 갱신은 `mkdir` 자물쇠로
  감쌌고, 세션이 직접 하는 커밋은 *"Another git process"* 가 나오면 30초 뒤 재시도한다.
- **AI 로 그림을 뽑아 자산으로 쓰지 않는다** (2026-09-09 끝까지 시험하고 접었다).
  형태를 프롬프트로 못 이기고, 그림자를 못 없앤다. 참고 자료로만 쓴다 —
  근거는 `docs/DESIGN.md` 「그래픽 파이프라인」.

---

## 7. 새 세션이 맨 처음 할 것

1. **이 파일을 다 읽었으면** `CLAUDE.md` → `docs/DESIGN.md`(문서 지도 포함) 순으로 읽어라.
2. **그림 일이면** `docs/DESIGN_LOOP.md` 를 **반드시** 읽어라 — 판정자 셋과 회차 쌓기를
   모르면 이 루프를 못 돌린다.
3. **큐에서 번호가 가장 작은 미완료 항목**을 집고, 그 항목의 「읽을 것:」 줄에 적힌
   문서를 읽어라.
4. **사람에게 물어야 할 것**: 되돌릴 수 없는 것(칸 규격 · 논리 해상도 · 팔레트 기준색)
   과 취향이 갈리는 자리. 그 외에는 스스로 판단하고 근거를 문서에 남겨라.
